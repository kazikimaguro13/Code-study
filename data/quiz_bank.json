{
  "meta": {
    "repo": "axis-knowledge-rag",
    "repo_commit": "0ec909d",
    "generator": "claude (cowork chat)",
    "schema_version": 1,
    "difficulty_levels": {
      "L1": "場所当て",
      "L2": "役割・振る舞い",
      "L3": "データフロー",
      "L4": "設計意図"
    },
    "axis_commit": "0ec909d"
  },
  "questions": [
    {
      "id": "q_search_001",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/search.py",
      "tags": [
        "fusion",
        "bm25"
      ],
      "prompt": "ベクトル類似度スコアとBM25スコアを重み付き和で融合し最終ランキングを作る処理は、どのクラスのどのメソッド？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/search.py",
        "symbol": {
          "kind": "method",
          "name": "SearchEngine.search"
        },
        "evidence_substring": "final = (1.0 - bm25_weight) * r.score + bm25_weight * bm25",
        "adr_ref": null,
        "snippet": "if use_bm25:\n    bm25_scores = self._bm25_index.score(query)\n    for r in results:\n        bm25 = bm25_scores.get(r.id, 0.0)\n        final = (1.0 - bm25_weight) * r.score + bm25_weight * bm25",
        "source": "    def search(\n        self,\n        query: str | None,\n        *,\n        filters: dict[str, Any] | None = None,\n        top_k: int = 5,\n        bm25_weight: float = 0.5,\n        graph_expand: bool = False,\n        graph_hop: int = 1,\n        graph_max_neighbors: int = 10,\n    ) -> list[SearchResult]:\n        \"\"\"Hybrid search (axis + vector + optional BM25 fusion + optional graph expand).\n\n        Args:\n            query: Natural-language query. If None, axis-only search (top_k arbitrary).\n            filters: User-friendly axis filters (`{\"category\": \"技術記事\"}`).\n            top_k: Maximum results to return.\n            bm25_weight: Weight of the BM25 score in the weighted-sum fusion\n                with the vector cosine score. ``0.0`` falls back to the v0.5\n                vector-only behaviour, ``1.0`` ranks purely by BM25. Ignored\n                when no ``bm25_index`` is wired into the engine or when the\n                query is ``None``.\n            graph_expand: spec_040. If True and the engine has a graph\n                wired, merge 1-hop refs neighbours of the top 5 hits into\n                the result set with a 0.7× score decay relative to their\n                source.\n            graph_hop: BFS depth for graph expansion (default 1).\n            graph_max_neighbors: per-source neighbour cap.\n\n        Query / filters are normalized before being passed to Chroma so that\n        the index (which stores `axis_*_norm` keys) can match across writing\n        variants (全角/半角・カタカナ/ひらがな・大文字小文字).\n        \"\"\"\n        norm_filters = (\n            {k: self._normalizer(str(v)) for k, v in filters.items()}\n            if filters\n            else None\n        )\n        where = _build_where_norm(norm_filters or {})\n        use_bm25 = (\n            query is not None\n            and self._bm25_index is not None\n            and bm25_weight > 0.0\n        )\n\n        if self._parent_doc_enabled:\n            pd_results = self._search_parent_doc(\n                query, where=where, top_k=top_k, bm25_weight=bm25_weight\n            )\n            if graph_expand:\n                pd_results = self._expand_with_graph(\n                    pd_results, hop=graph_hop, max_neighbors=graph_max_neighbors\n                )\n            self._record_gap(query, pd_results)\n            return pd_results\n\n        if query is None:\n            # Axis-only path: use a zero embedding (Chroma will then sort by\n            # distance from zero, which is arbitrary — but we mostly care about\n            # the filter). top_k is bounded by collection size.\n            n = min(top_k, max(self._store.count(), 1))\n            embedding = [0.0] * self._embedder.dim\n        else:\n            q_norm = self._normalizer(query)\n            embedding = self._embedder.embed(q_norm)\n            # Over-fetch when fusing so BM25 can re-rank candidates that\n            # ranked just outside the original vector top_k.\n            n = max(top_k * 2, 20) if use_bm25 else top_k\n\n        raw = self._store.query(embedding=embedding, n_results=n, where=where)\n        results = _to_results(raw)\n\n        if use_bm25:\n            assert query is not None and self._bm25_index is not None\n            bm25_scores = self._bm25_index.score(query)\n            fused: list[SearchResult] = []\n            for r in results:\n                bm25 = bm25_scores.get(r.id, 0.0)\n                final = (1.0 - bm25_weight) * r.score + bm25_weight * bm25\n                fused.append(\n                    SearchResult(\n                        id=r.id,\n                        title=r.title,\n                        score=final,\n                        axes=r.axes,\n                        body_snippet=r.body_snippet,\n                        path=r.path,\n                        refs=r.refs,\n                        metadata=r.metadata,\n                    )\n                )\n            fused.sort(key=lambda r: r.score, reverse=True)\n            results = fused[:top_k]\n\n        # spec_035: apply time-weighted decay after fusion (re-sort needed).\n        td = self._time_decay_config\n        if td is not None and td.enabled and td.weight > 0:\n            results = _apply_time_decay(results, td)\n            results.sort(key=lambda r: r.score, reverse=True)\n            results = results[:top_k]\n\n        # spec_040: graph expansion (post-decay). Adds 1-hop refs neighbours\n        # of the top hits with a 0.7× decay multiplier. Caller-opt-in only.\n        if graph_expand:\n            results = self._expand_with_graph(\n                results, hop=graph_hop, max_neighbors=graph_max_neighbors\n            )\n\n        logger.info(\n            \"search(query=%r, filters=%s, bm25_weight=%.2f, graph_expand=%s) -> %d results\",\n            query,\n            filters,\n            bm25_weight,\n            graph_expand,\n            len(results),\n        )\n        self._record_gap(query, results)\n        return results",
        "start_line": 191,
        "end_line": 308
      },
      "answer": {
        "file": "backend/src/search.py",
        "symbol": "SearchEngine.search"
      },
      "grading": {
        "symbol_aliases": [
          "search",
          "fuse_results",
          "fuse"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "融合はSearchEngine.search()のインライン処理。vectorとBM25をbm25_weightで線形結合し再ソート。"
    },
    {
      "id": "q_search_002",
      "difficulty": "L2",
      "type": "fill_blank",
      "module": "backend/src/search.py",
      "tags": [
        "fusion"
      ],
      "prompt": "融合スコアの空欄を埋めよ:\n    final = (1.0 - bm25_weight) * r.score + ____ * bm25",
      "citation": {
        "file": "backend/src/search.py",
        "symbol": {
          "kind": "method",
          "name": "SearchEngine.search"
        },
        "evidence_substring": "bm25_weight * bm25",
        "adr_ref": null,
        "snippet": "final = (1.0 - bm25_weight) * r.score + bm25_weight * bm25",
        "source": "    def search(\n        self,\n        query: str | None,\n        *,\n        filters: dict[str, Any] | None = None,\n        top_k: int = 5,\n        bm25_weight: float = 0.5,\n        graph_expand: bool = False,\n        graph_hop: int = 1,\n        graph_max_neighbors: int = 10,\n    ) -> list[SearchResult]:\n        \"\"\"Hybrid search (axis + vector + optional BM25 fusion + optional graph expand).\n\n        Args:\n            query: Natural-language query. If None, axis-only search (top_k arbitrary).\n            filters: User-friendly axis filters (`{\"category\": \"技術記事\"}`).\n            top_k: Maximum results to return.\n            bm25_weight: Weight of the BM25 score in the weighted-sum fusion\n                with the vector cosine score. ``0.0`` falls back to the v0.5\n                vector-only behaviour, ``1.0`` ranks purely by BM25. Ignored\n                when no ``bm25_index`` is wired into the engine or when the\n                query is ``None``.\n            graph_expand: spec_040. If True and the engine has a graph\n                wired, merge 1-hop refs neighbours of the top 5 hits into\n                the result set with a 0.7× score decay relative to their\n                source.\n            graph_hop: BFS depth for graph expansion (default 1).\n            graph_max_neighbors: per-source neighbour cap.\n\n        Query / filters are normalized before being passed to Chroma so that\n        the index (which stores `axis_*_norm` keys) can match across writing\n        variants (全角/半角・カタカナ/ひらがな・大文字小文字).\n        \"\"\"\n        norm_filters = (\n            {k: self._normalizer(str(v)) for k, v in filters.items()}\n            if filters\n            else None\n        )\n        where = _build_where_norm(norm_filters or {})\n        use_bm25 = (\n            query is not None\n            and self._bm25_index is not None\n            and bm25_weight > 0.0\n        )\n\n        if self._parent_doc_enabled:\n            pd_results = self._search_parent_doc(\n                query, where=where, top_k=top_k, bm25_weight=bm25_weight\n            )\n            if graph_expand:\n                pd_results = self._expand_with_graph(\n                    pd_results, hop=graph_hop, max_neighbors=graph_max_neighbors\n                )\n            self._record_gap(query, pd_results)\n            return pd_results\n\n        if query is None:\n            # Axis-only path: use a zero embedding (Chroma will then sort by\n            # distance from zero, which is arbitrary — but we mostly care about\n            # the filter). top_k is bounded by collection size.\n            n = min(top_k, max(self._store.count(), 1))\n            embedding = [0.0] * self._embedder.dim\n        else:\n            q_norm = self._normalizer(query)\n            embedding = self._embedder.embed(q_norm)\n            # Over-fetch when fusing so BM25 can re-rank candidates that\n            # ranked just outside the original vector top_k.\n            n = max(top_k * 2, 20) if use_bm25 else top_k\n\n        raw = self._store.query(embedding=embedding, n_results=n, where=where)\n        results = _to_results(raw)\n\n        if use_bm25:\n            assert query is not None and self._bm25_index is not None\n            bm25_scores = self._bm25_index.score(query)\n            fused: list[SearchResult] = []\n            for r in results:\n                bm25 = bm25_scores.get(r.id, 0.0)\n                final = (1.0 - bm25_weight) * r.score + bm25_weight * bm25\n                fused.append(\n                    SearchResult(\n                        id=r.id,\n                        title=r.title,\n                        score=final,\n                        axes=r.axes,\n                        body_snippet=r.body_snippet,\n                        path=r.path,\n                        refs=r.refs,\n                        metadata=r.metadata,\n                    )\n                )\n            fused.sort(key=lambda r: r.score, reverse=True)\n            results = fused[:top_k]\n\n        # spec_035: apply time-weighted decay after fusion (re-sort needed).\n        td = self._time_decay_config\n        if td is not None and td.enabled and td.weight > 0:\n            results = _apply_time_decay(results, td)\n            results.sort(key=lambda r: r.score, reverse=True)\n            results = results[:top_k]\n\n        # spec_040: graph expansion (post-decay). Adds 1-hop refs neighbours\n        # of the top hits with a 0.7× decay multiplier. Caller-opt-in only.\n        if graph_expand:\n            results = self._expand_with_graph(\n                results, hop=graph_hop, max_neighbors=graph_max_neighbors\n            )\n\n        logger.info(\n            \"search(query=%r, filters=%s, bm25_weight=%.2f, graph_expand=%s) -> %d results\",\n            query,\n            filters,\n            bm25_weight,\n            graph_expand,\n            len(results),\n        )\n        self._record_gap(query, results)\n        return results",
        "start_line": 191,
        "end_line": 308
      },
      "answer": {
        "accepted": [
          "bm25_weight"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "vectorに(1-w)、BM25にwを掛ける重み付き和。w=bm25_weight。"
    },
    {
      "id": "q_search_003",
      "difficulty": "L2",
      "type": "fill_blank",
      "module": "backend/src/search.py",
      "tags": [
        "scoring"
      ],
      "prompt": "Chromaの距離distをスコアに変換する式の空欄を埋めよ:\n    score = max(0.0, min(1.0, 1.0 - ____))",
      "citation": {
        "file": "backend/src/search.py",
        "symbol": {
          "kind": "function",
          "name": "_to_results"
        },
        "evidence_substring": "score = max(0.0, min(1.0, 1.0 - dist))",
        "adr_ref": null,
        "snippet": "dist = distances[i] if distances else 0.0\n        score = max(0.0, min(1.0, 1.0 - dist))",
        "source": "def _to_results(raw: dict[str, Any]) -> list[SearchResult]:\n    \"\"\"Convert Chroma query() output to SearchResult list.\"\"\"\n    ids = raw.get(\"ids\", [[]])[0]\n    distances = raw.get(\"distances\", [[]])[0]\n    metadatas = raw.get(\"metadatas\", [[]])[0]\n    documents = raw.get(\"documents\", [[]])[0]\n\n    out: list[SearchResult] = []\n    for i, doc_id in enumerate(ids):\n        md = metadatas[i] or {}\n        dist = distances[i] if distances else 0.0\n        score = max(0.0, min(1.0, 1.0 - dist))\n        axes = {\n            k.removeprefix(\"axis_\"): v\n            for k, v in md.items()\n            if k.startswith(\"axis_\") and not k.endswith(\"_norm\")\n        }\n        refs = [r for r in (md.get(\"refs\") or \"\").split(\",\") if r]\n        out.append(\n            SearchResult(\n                id=doc_id,\n                title=str(md.get(\"title\", \"\")),\n                score=score,\n                axes=axes,\n                body_snippet=_snippet(documents[i] if documents else \"\"),\n                path=str(md.get(\"path\", \"\")),\n                refs=refs,\n                metadata=dict(md),\n            )\n        )\n    return out",
        "start_line": 87,
        "end_line": 117
      },
      "answer": {
        "accepted": [
          "dist",
          "distance"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "コサイン距離distを1-distで類似度化し[0,1]にクランプ。"
    },
    {
      "id": "q_search_004",
      "difficulty": "L2",
      "type": "freetext",
      "module": "backend/src/search.py",
      "tags": [
        "gap-detection"
      ],
      "prompt": "SearchEngine._record_gap の役割を一文で説明せよ。",
      "citation": {
        "file": "backend/src/search.py",
        "symbol": {
          "kind": "method",
          "name": "SearchEngine._record_gap"
        },
        "evidence_substring": "reason=\"no_results\"",
        "adr_ref": null,
        "snippet": "if not results:\n    self._gap_store.record(query=query, reason=\"no_results\", n_results=0)\ntop = float(results[0].score)\nif top < self._gap_low_score_threshold:\n    self._gap_store.record(query=query, reason=\"low_score\", ...)",
        "source": "    def _record_gap(\n        self, query: str | None, results: list[SearchResult]\n    ) -> None:\n        \"\"\"Post-hoc hook: log when a search returned nothing useful.\n\n        Skipped when the engine wasn't built with a ``gap_store`` (which\n        is the case in tests + when ``gap.enabled=false``). Two events\n        get recorded: empty hit list → ``no_results``; non-empty but the\n        top score is below ``low_score_threshold`` → ``low_score``.\n        Axis-only queries (``query is None``) are skipped — those are\n        listing/filter calls, not knowledge questions.\n        \"\"\"\n        if self._gap_store is None or not query:\n            return\n        try:\n            if not results:\n                self._gap_store.record(\n                    query=query, reason=\"no_results\", n_results=0\n                )\n                return\n            top = float(results[0].score)\n            if top < self._gap_low_score_threshold:\n                self._gap_store.record(\n                    query=query,\n                    reason=\"low_score\",\n                    top_score=top,\n                    n_results=len(results),\n                )\n        except Exception as e:  # noqa: BLE001\n            # The gap path must never fail the search. Log + swallow.\n            logger.warning(\"gap store record failed: %s\", e)",
        "start_line": 314,
        "end_line": 344
      },
      "answer": {
        "model_answer": "検索が無結果またはスコアが低かった時にナレッジギャップとして記録するフック。",
        "required_keywords": [
          {
            "any_of": [
              "no_results",
              "無結果",
              "0件",
              "結果がない"
            ]
          },
          {
            "any_of": [
              "low_score",
              "スコアが低い",
              "しきい値",
              "閾値"
            ]
          },
          {
            "any_of": [
              "記録",
              "ログ",
              "record",
              "ギャップ"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.66
      },
      "explanation": "検索品質が低いクエリを記録しナレッジの穴を分析する。gap_store=Noneならno-op。"
    },
    {
      "id": "q_search_005",
      "difficulty": "L3",
      "type": "dataflow",
      "module": "backend/src/search.py",
      "tags": [
        "pipeline"
      ],
      "prompt": "自然文クエリのsearch()が結果を返すまでの主要ステップを順に並べよ。",
      "citation": {
        "file": "backend/src/search.py",
        "symbol": {
          "kind": "method",
          "name": "SearchEngine.search"
        },
        "evidence_substring": "embedding = self._embedder.embed(q_norm)",
        "adr_ref": null,
        "snippet": "q_norm = self._normalizer(query)\nembedding = self._embedder.embed(q_norm)\nraw = self._store.query(embedding=embedding, ...)\nbm25_scores = self._bm25_index.score(query)\n# fuse, then time decay",
        "source": "    def search(\n        self,\n        query: str | None,\n        *,\n        filters: dict[str, Any] | None = None,\n        top_k: int = 5,\n        bm25_weight: float = 0.5,\n        graph_expand: bool = False,\n        graph_hop: int = 1,\n        graph_max_neighbors: int = 10,\n    ) -> list[SearchResult]:\n        \"\"\"Hybrid search (axis + vector + optional BM25 fusion + optional graph expand).\n\n        Args:\n            query: Natural-language query. If None, axis-only search (top_k arbitrary).\n            filters: User-friendly axis filters (`{\"category\": \"技術記事\"}`).\n            top_k: Maximum results to return.\n            bm25_weight: Weight of the BM25 score in the weighted-sum fusion\n                with the vector cosine score. ``0.0`` falls back to the v0.5\n                vector-only behaviour, ``1.0`` ranks purely by BM25. Ignored\n                when no ``bm25_index`` is wired into the engine or when the\n                query is ``None``.\n            graph_expand: spec_040. If True and the engine has a graph\n                wired, merge 1-hop refs neighbours of the top 5 hits into\n                the result set with a 0.7× score decay relative to their\n                source.\n            graph_hop: BFS depth for graph expansion (default 1).\n            graph_max_neighbors: per-source neighbour cap.\n\n        Query / filters are normalized before being passed to Chroma so that\n        the index (which stores `axis_*_norm` keys) can match across writing\n        variants (全角/半角・カタカナ/ひらがな・大文字小文字).\n        \"\"\"\n        norm_filters = (\n            {k: self._normalizer(str(v)) for k, v in filters.items()}\n            if filters\n            else None\n        )\n        where = _build_where_norm(norm_filters or {})\n        use_bm25 = (\n            query is not None\n            and self._bm25_index is not None\n            and bm25_weight > 0.0\n        )\n\n        if self._parent_doc_enabled:\n            pd_results = self._search_parent_doc(\n                query, where=where, top_k=top_k, bm25_weight=bm25_weight\n            )\n            if graph_expand:\n                pd_results = self._expand_with_graph(\n                    pd_results, hop=graph_hop, max_neighbors=graph_max_neighbors\n                )\n            self._record_gap(query, pd_results)\n            return pd_results\n\n        if query is None:\n            # Axis-only path: use a zero embedding (Chroma will then sort by\n            # distance from zero, which is arbitrary — but we mostly care about\n            # the filter). top_k is bounded by collection size.\n            n = min(top_k, max(self._store.count(), 1))\n            embedding = [0.0] * self._embedder.dim\n        else:\n            q_norm = self._normalizer(query)\n            embedding = self._embedder.embed(q_norm)\n            # Over-fetch when fusing so BM25 can re-rank candidates that\n            # ranked just outside the original vector top_k.\n            n = max(top_k * 2, 20) if use_bm25 else top_k\n\n        raw = self._store.query(embedding=embedding, n_results=n, where=where)\n        results = _to_results(raw)\n\n        if use_bm25:\n            assert query is not None and self._bm25_index is not None\n            bm25_scores = self._bm25_index.score(query)\n            fused: list[SearchResult] = []\n            for r in results:\n                bm25 = bm25_scores.get(r.id, 0.0)\n                final = (1.0 - bm25_weight) * r.score + bm25_weight * bm25\n                fused.append(\n                    SearchResult(\n                        id=r.id,\n                        title=r.title,\n                        score=final,\n                        axes=r.axes,\n                        body_snippet=r.body_snippet,\n                        path=r.path,\n                        refs=r.refs,\n                        metadata=r.metadata,\n                    )\n                )\n            fused.sort(key=lambda r: r.score, reverse=True)\n            results = fused[:top_k]\n\n        # spec_035: apply time-weighted decay after fusion (re-sort needed).\n        td = self._time_decay_config\n        if td is not None and td.enabled and td.weight > 0:\n            results = _apply_time_decay(results, td)\n            results.sort(key=lambda r: r.score, reverse=True)\n            results = results[:top_k]\n\n        # spec_040: graph expansion (post-decay). Adds 1-hop refs neighbours\n        # of the top hits with a 0.7× decay multiplier. Caller-opt-in only.\n        if graph_expand:\n            results = self._expand_with_graph(\n                results, hop=graph_hop, max_neighbors=graph_max_neighbors\n            )\n\n        logger.info(\n            \"search(query=%r, filters=%s, bm25_weight=%.2f, graph_expand=%s) -> %d results\",\n            query,\n            filters,\n            bm25_weight,\n            graph_expand,\n            len(results),\n        )\n        self._record_gap(query, results)\n        return results",
        "start_line": 191,
        "end_line": 308
      },
      "answer": {
        "sequence": [
          "normalizer",
          "embedder",
          "vector_store.query",
          "bm25_index.score",
          "fuse",
          "time_decay"
        ]
      },
      "grading": {
        "mode": "subsequence",
        "pass_threshold": 0.66,
        "aliases": [
          [
            "normalizer",
            "正規化"
          ],
          [
            "embedder",
            "embed",
            "埋め込み"
          ],
          [
            "vector_store.query",
            "vector_store",
            "chroma",
            "vector"
          ],
          [
            "bm25_index.score",
            "bm25"
          ],
          [
            "fuse",
            "融合",
            "重み付き和"
          ],
          [
            "time_decay",
            "decay",
            "時間減衰"
          ]
        ]
      },
      "pool": [
        "bm25_index.score",
        "time_decay",
        "normalizer",
        "vector_store.query",
        "embedder",
        "fuse"
      ],
      "explanation": "正規化→埋め込み→ベクトル検索→BM25→重み付き和で融合・再ソート→時間減衰。"
    },
    {
      "id": "q_search_006",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/search.py",
      "tags": [
        "fusion"
      ],
      "prompt": "search(bm25_weight=0.0) を呼ぶと挙動はどうなる？",
      "citation": {
        "file": "backend/src/search.py",
        "symbol": {
          "kind": "method",
          "name": "SearchEngine.search"
        },
        "evidence_substring": "falls back to the v0.5",
        "adr_ref": null,
        "snippet": "bm25_weight: 0.0 falls back to the v0.5 vector-only behaviour, 1.0 ranks purely by BM25.",
        "source": "    def search(\n        self,\n        query: str | None,\n        *,\n        filters: dict[str, Any] | None = None,\n        top_k: int = 5,\n        bm25_weight: float = 0.5,\n        graph_expand: bool = False,\n        graph_hop: int = 1,\n        graph_max_neighbors: int = 10,\n    ) -> list[SearchResult]:\n        \"\"\"Hybrid search (axis + vector + optional BM25 fusion + optional graph expand).\n\n        Args:\n            query: Natural-language query. If None, axis-only search (top_k arbitrary).\n            filters: User-friendly axis filters (`{\"category\": \"技術記事\"}`).\n            top_k: Maximum results to return.\n            bm25_weight: Weight of the BM25 score in the weighted-sum fusion\n                with the vector cosine score. ``0.0`` falls back to the v0.5\n                vector-only behaviour, ``1.0`` ranks purely by BM25. Ignored\n                when no ``bm25_index`` is wired into the engine or when the\n                query is ``None``.\n            graph_expand: spec_040. If True and the engine has a graph\n                wired, merge 1-hop refs neighbours of the top 5 hits into\n                the result set with a 0.7× score decay relative to their\n                source.\n            graph_hop: BFS depth for graph expansion (default 1).\n            graph_max_neighbors: per-source neighbour cap.\n\n        Query / filters are normalized before being passed to Chroma so that\n        the index (which stores `axis_*_norm` keys) can match across writing\n        variants (全角/半角・カタカナ/ひらがな・大文字小文字).\n        \"\"\"\n        norm_filters = (\n            {k: self._normalizer(str(v)) for k, v in filters.items()}\n            if filters\n            else None\n        )\n        where = _build_where_norm(norm_filters or {})\n        use_bm25 = (\n            query is not None\n            and self._bm25_index is not None\n            and bm25_weight > 0.0\n        )\n\n        if self._parent_doc_enabled:\n            pd_results = self._search_parent_doc(\n                query, where=where, top_k=top_k, bm25_weight=bm25_weight\n            )\n            if graph_expand:\n                pd_results = self._expand_with_graph(\n                    pd_results, hop=graph_hop, max_neighbors=graph_max_neighbors\n                )\n            self._record_gap(query, pd_results)\n            return pd_results\n\n        if query is None:\n            # Axis-only path: use a zero embedding (Chroma will then sort by\n            # distance from zero, which is arbitrary — but we mostly care about\n            # the filter). top_k is bounded by collection size.\n            n = min(top_k, max(self._store.count(), 1))\n            embedding = [0.0] * self._embedder.dim\n        else:\n            q_norm = self._normalizer(query)\n            embedding = self._embedder.embed(q_norm)\n            # Over-fetch when fusing so BM25 can re-rank candidates that\n            # ranked just outside the original vector top_k.\n            n = max(top_k * 2, 20) if use_bm25 else top_k\n\n        raw = self._store.query(embedding=embedding, n_results=n, where=where)\n        results = _to_results(raw)\n\n        if use_bm25:\n            assert query is not None and self._bm25_index is not None\n            bm25_scores = self._bm25_index.score(query)\n            fused: list[SearchResult] = []\n            for r in results:\n                bm25 = bm25_scores.get(r.id, 0.0)\n                final = (1.0 - bm25_weight) * r.score + bm25_weight * bm25\n                fused.append(\n                    SearchResult(\n                        id=r.id,\n                        title=r.title,\n                        score=final,\n                        axes=r.axes,\n                        body_snippet=r.body_snippet,\n                        path=r.path,\n                        refs=r.refs,\n                        metadata=r.metadata,\n                    )\n                )\n            fused.sort(key=lambda r: r.score, reverse=True)\n            results = fused[:top_k]\n\n        # spec_035: apply time-weighted decay after fusion (re-sort needed).\n        td = self._time_decay_config\n        if td is not None and td.enabled and td.weight > 0:\n            results = _apply_time_decay(results, td)\n            results.sort(key=lambda r: r.score, reverse=True)\n            results = results[:top_k]\n\n        # spec_040: graph expansion (post-decay). Adds 1-hop refs neighbours\n        # of the top hits with a 0.7× decay multiplier. Caller-opt-in only.\n        if graph_expand:\n            results = self._expand_with_graph(\n                results, hop=graph_hop, max_neighbors=graph_max_neighbors\n            )\n\n        logger.info(\n            \"search(query=%r, filters=%s, bm25_weight=%.2f, graph_expand=%s) -> %d results\",\n            query,\n            filters,\n            bm25_weight,\n            graph_expand,\n            len(results),\n        )\n        self._record_gap(query, results)\n        return results",
        "start_line": 191,
        "end_line": 308
      },
      "answer": {
        "options": [
          "BM25のみでランキング",
          "v0.5相当のベクトル検索のみにフォールバック",
          "エラーになる",
          "軸メタデータのみで絞り込む"
        ],
        "correct_index": 1
      },
      "explanation": "0.0でBM25寄与ゼロ＝ベクトルのみ(v0.5)。1.0で純BM25。"
    },
    {
      "id": "q_bm25_001",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/bm25_index.py",
      "tags": [
        "bm25",
        "tokenize"
      ],
      "prompt": "形態素解析なしで日本語を扱う『文字n-gramトークナイザ』はどのファイルのどの関数？",
      "citation": {
        "file": "backend/src/bm25_index.py",
        "symbol": {
          "kind": "function",
          "name": "_tokenize"
        },
        "evidence_substring": "tokens.append(text[i : i + 2])",
        "adr_ref": null,
        "snippet": "def _tokenize(text):\n    tokens.extend(text)  # n=1\n    for i in range(len(text)-1):\n        tokens.append(text[i : i + 2])  # n=2",
        "source": "def _tokenize(text: str) -> list[str]:\n    \"\"\"Character n-gram tokenizer (n=1, 2) over already-normalized text.\n\n    Examples:\n        \"あいう\" -> [\"あ\", \"い\", \"う\", \"あい\", \"いう\"]\n    \"\"\"\n    if not text:\n        return []\n    tokens: list[str] = []\n    tokens.extend(text)  # n=1\n    for i in range(len(text) - 1):\n        tokens.append(text[i : i + 2])  # n=2\n    return tokens",
        "start_line": 21,
        "end_line": 33
      },
      "answer": {
        "file": "backend/src/bm25_index.py",
        "symbol": "_tokenize"
      },
      "grading": {
        "symbol_aliases": [
          "_tokenize",
          "tokenize"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "_tokenizeはn=1,2の文字n-gram。形態素解析器の依存を避ける。"
    },
    {
      "id": "q_bm25_002",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/bm25_index.py",
      "tags": [
        "bm25",
        "design"
      ],
      "prompt": "BM25に形態素解析でなく文字n-gramを選んだ主な理由は？",
      "citation": {
        "file": "backend/src/bm25_index.py",
        "symbol": {
          "kind": "function",
          "name": "_tokenize"
        },
        "evidence_substring": "avoids",
        "adr_ref": null,
        "snippet": "character n-gram (n=1,2) on normalized text — avoids the morphological analyzer dependency while still usable for Japanese.",
        "source": "def _tokenize(text: str) -> list[str]:\n    \"\"\"Character n-gram tokenizer (n=1, 2) over already-normalized text.\n\n    Examples:\n        \"あいう\" -> [\"あ\", \"い\", \"う\", \"あい\", \"いう\"]\n    \"\"\"\n    if not text:\n        return []\n    tokens: list[str] = []\n    tokens.extend(text)  # n=1\n    for i in range(len(text) - 1):\n        tokens.append(text[i : i + 2])  # n=2\n    return tokens",
        "start_line": 21,
        "end_line": 33
      },
      "answer": {
        "options": [
          "n-gramが常に高精度",
          "形態素解析器への依存を避けつつ日本語で使える",
          "英語専用化",
          "Chromaの要求"
        ],
        "correct_index": 1
      },
      "explanation": "追加依存(形態素解析器)を増やさない方針。"
    },
    {
      "id": "q_bm25_003",
      "difficulty": "L2",
      "type": "freetext",
      "module": "backend/src/bm25_index.py",
      "tags": [
        "bm25",
        "scoring"
      ],
      "prompt": "BM25Index.scoreが生スコアをmin-max正規化して[0,1]にするのはなぜ？",
      "citation": {
        "file": "backend/src/bm25_index.py",
        "symbol": {
          "kind": "method",
          "name": "BM25Index.score"
        },
        "evidence_substring": "so they can be summed with cosine similarity",
        "adr_ref": null,
        "snippet": "Raw BM25 scores are min-max normalized to [0,1] so they can be summed with cosine similarity.\nnorm_scores = (scores - s_min) / (s_max - s_min)",
        "source": "    def score(self, query: str) -> dict[str, float]:\n        \"\"\"Return `{doc_id: bm25_score}` for the query.\n\n        The query is normalized via `self.normalizer` then tokenized with\n        the same n-gram scheme as the corpus. Raw BM25 scores are min-max\n        normalized to `[0, 1]` so they can be summed with cosine similarity.\n        \"\"\"\n        q_tokens = _tokenize(self.normalizer(query))\n        if not q_tokens:\n            return {}\n        if not self.doc_ids:\n            return {}\n        scores = self._model.get_scores(q_tokens)\n        if not len(scores):\n            return {}\n        s_min = float(scores.min())\n        s_max = float(scores.max())\n        if s_max - s_min < 1e-9:\n            return {d: 0.0 for d in self.doc_ids}\n        norm_scores = (scores - s_min) / (s_max - s_min)\n        return {\n            d: float(s)\n            for d, s in zip(self.doc_ids, norm_scores, strict=True)\n        }",
        "start_line": 61,
        "end_line": 84
      },
      "answer": {
        "model_answer": "コサイン類似度(0〜1)と重み付き和で足せるようスケールを揃えるため。",
        "required_keywords": [
          {
            "any_of": [
              "コサイン",
              "cosine",
              "ベクトル",
              "類似度"
            ]
          },
          {
            "any_of": [
              "足",
              "加算",
              "和",
              "融合",
              "sum",
              "合算",
              "スケール",
              "揃え"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.5
      },
      "explanation": "スケールを[0,1]に揃えてコサインと同じ土俵で重み付き和。"
    },
    {
      "id": "q_bm25_004",
      "difficulty": "L1",
      "type": "fill_blank",
      "module": "backend/src/bm25_index.py",
      "tags": [
        "bm25",
        "library"
      ],
      "prompt": "BM25モデル構築の空欄(使用クラス名)を埋めよ:\n    model = ____(tokenized)",
      "citation": {
        "file": "backend/src/bm25_index.py",
        "symbol": {
          "kind": "method",
          "name": "BM25Index.build"
        },
        "evidence_substring": "model = BM25Okapi(tokenized)",
        "adr_ref": null,
        "snippet": "from rank_bm25 import BM25Okapi\nmodel = BM25Okapi(tokenized)",
        "source": "    def build(\n        cls, docs: list[tuple[str, str]], normalizer: Normalizer\n    ) -> BM25Index:\n        \"\"\"Build from `(doc_id, normalized_body)` pairs.\n\n        The body should already be normalized (the same Normalizer that the\n        index uses for queries) so that tokens match consistently.\n        \"\"\"\n        ids = [d[0] for d in docs]\n        tokenized = [_tokenize(d[1]) for d in docs]\n        if not tokenized:\n            tokenized = [[\"\"]]  # rank_bm25 needs non-empty corpus\n        model = BM25Okapi(tokenized)\n        logger.info(\"BM25Index built (n_docs=%d)\", len(ids))\n        return cls(doc_ids=ids, normalizer=normalizer, _model=model)",
        "start_line": 45,
        "end_line": 59
      },
      "answer": {
        "accepted": [
          "BM25Okapi",
          "BM25Okapi(tokenized)"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "rank_bm25のBM25Okapiを使用。"
    },
    {
      "id": "q_decay_001",
      "difficulty": "L1",
      "type": "fill_blank",
      "module": "backend/src/_decay.py",
      "tags": [
        "time-decay"
      ],
      "prompt": "指数減衰係数の空欄を埋めよ:\n    return math.exp(-math.log(2) * age_days / ____)",
      "citation": {
        "file": "backend/src/_decay.py",
        "symbol": {
          "kind": "function",
          "name": "decay_factor"
        },
        "evidence_substring": "age_days / half_life_days",
        "adr_ref": null,
        "snippet": "return math.exp(-math.log(2) * age_days / half_life_days)",
        "source": "def decay_factor(\n    updated_at: Any,\n    *,\n    now: datetime | None = None,\n    half_life_days: float = 180.0,\n) -> float:\n    \"\"\"Return decay coefficient in (0.0, 1.0].\n\n    - decay = exp(-ln(2) * age_days / half_life_days)\n    - At age=half_life_days: 0.5; at age=2*half_life_days: 0.25\n    - updated_at = None / empty / unparseable → 1.0 (no penalty)\n    \"\"\"\n    if not updated_at:\n        return 1.0\n    now = now or datetime.now(UTC)\n    try:\n        dt = _parse_datetime(updated_at)\n    except Exception:  # noqa: BLE001\n        _log.warning(\"time_decay: failed to parse updated_at=%r, using 1.0\", updated_at)\n        return 1.0\n    age_seconds = (now - dt).total_seconds()\n    if age_seconds < 0:\n        return 1.0  # future-dated doc → no penalty\n    age_days = age_seconds / 86400.0\n    return math.exp(-math.log(2) * age_days / half_life_days)",
        "start_line": 13,
        "end_line": 37
      },
      "answer": {
        "accepted": [
          "half_life_days"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "半減期ベースの指数減衰。age=half_lifeで0.5。"
    },
    {
      "id": "q_decay_002",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/_decay.py",
      "tags": [
        "time-decay"
      ],
      "prompt": "経過日数がちょうどhalf_life_daysのとき減衰係数は？",
      "citation": {
        "file": "backend/src/_decay.py",
        "symbol": {
          "kind": "function",
          "name": "decay_factor"
        },
        "evidence_substring": "At age=half_life_days: 0.5",
        "adr_ref": null,
        "snippet": "At age=half_life_days: 0.5; at age=2*half_life_days: 0.25",
        "source": "def decay_factor(\n    updated_at: Any,\n    *,\n    now: datetime | None = None,\n    half_life_days: float = 180.0,\n) -> float:\n    \"\"\"Return decay coefficient in (0.0, 1.0].\n\n    - decay = exp(-ln(2) * age_days / half_life_days)\n    - At age=half_life_days: 0.5; at age=2*half_life_days: 0.25\n    - updated_at = None / empty / unparseable → 1.0 (no penalty)\n    \"\"\"\n    if not updated_at:\n        return 1.0\n    now = now or datetime.now(UTC)\n    try:\n        dt = _parse_datetime(updated_at)\n    except Exception:  # noqa: BLE001\n        _log.warning(\"time_decay: failed to parse updated_at=%r, using 1.0\", updated_at)\n        return 1.0\n    age_seconds = (now - dt).total_seconds()\n    if age_seconds < 0:\n        return 1.0  # future-dated doc → no penalty\n    age_days = age_seconds / 86400.0\n    return math.exp(-math.log(2) * age_days / half_life_days)",
        "start_line": 13,
        "end_line": 37
      },
      "answer": {
        "options": [
          "1.0",
          "0.5",
          "0.25",
          "0.0"
        ],
        "correct_index": 1
      },
      "explanation": "半減期の定義どおり0.5。"
    },
    {
      "id": "q_decay_003",
      "difficulty": "L2",
      "type": "location",
      "module": "backend/src/_decay.py",
      "tags": [
        "time-decay"
      ],
      "prompt": "ベーススコアと減衰係数を重み付きでブレンドする関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/_decay.py",
        "symbol": {
          "kind": "function",
          "name": "blend_score"
        },
        "evidence_substring": "return base_score * (1.0 - w * (1.0 - decay))",
        "adr_ref": null,
        "snippet": "def blend_score(base_score, decay, weight):\n    w = max(0.0, min(1.0, weight))\n    return base_score * (1.0 - w * (1.0 - decay))",
        "source": "def blend_score(base_score: float, decay: float, weight: float) -> float:\n    \"\"\"Blend base score with decay factor.\n\n    final = base * (1 - w) + (base * decay) * w\n          = base * (1 - w * (1 - decay))\n    \"\"\"\n    w = max(0.0, min(1.0, weight))\n    return base_score * (1.0 - w * (1.0 - decay))",
        "start_line": 40,
        "end_line": 47
      },
      "answer": {
        "file": "backend/src/_decay.py",
        "symbol": "blend_score"
      },
      "grading": {
        "symbol_aliases": [
          "blend_score"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "final = base*(1-w*(1-decay))。weightで減衰の効きを調整。"
    },
    {
      "id": "q_decay_004",
      "difficulty": "L4",
      "type": "mcq",
      "module": "docs/adr/ADR-021-time-weighted-decay.md",
      "tags": [
        "time-decay",
        "design"
      ],
      "prompt": "Time-Weighted Decayをデフォルト無効(opt-in)にした設計判断の理由は？(ADR-021)",
      "citation": {
        "file": "docs/adr/ADR-021-time-weighted-decay.md",
        "symbol": {
          "kind": "section",
          "name": "Decision"
        },
        "evidence_substring": "enabled: true",
        "adr_ref": "ADR-021",
        "snippet": "config.ymlでenabled: trueにすると有効。opt-in(デフォルト無効)。新しさ優遇が全ユーザーに望ましいとは限らないため。"
      },
      "answer": {
        "options": [
          "未完成だから",
          "新しさ優遇が常に望ましいとは限らず、望む人だけ有効化できるように",
          "計算コストが高すぎる",
          "Chroma非対応"
        ],
        "correct_index": 1
      },
      "explanation": "鮮度バイアスは用途依存。デフォルト無効で必要な人だけ有効化。"
    },
    {
      "id": "q_norm_001",
      "difficulty": "L1",
      "type": "freetext",
      "module": "backend/src/normalizer.py",
      "tags": [
        "japanese"
      ],
      "prompt": "Normalizerが表記ゆれ吸収のために行う3つの変換を挙げよ。",
      "citation": {
        "file": "backend/src/normalizer.py",
        "symbol": {
          "kind": "function",
          "name": "normalize_text"
        },
        "evidence_substring": "unicodedata.normalize(\"NFKC\", s)",
        "adr_ref": null,
        "snippet": "s = unicodedata.normalize(\"NFKC\", s)\ns = _katakana_to_hiragana(s)\ns = s.lower()",
        "source": "def normalize_text(text: str, options: NormalizerOptions | None = None) -> str:\n    opts = options or NormalizerOptions()\n    s = text\n    if opts.nfkc:\n        s = unicodedata.normalize(\"NFKC\", s)\n    if opts.katakana_to_hiragana:\n        s = _katakana_to_hiragana(s)\n    if opts.lowercase:\n        s = s.lower()\n    return s",
        "start_line": 38,
        "end_line": 47
      },
      "answer": {
        "model_answer": "NFKC正規化、カタカナ→ひらがな、小文字化。",
        "required_keywords": [
          {
            "any_of": [
              "nfkc"
            ]
          },
          {
            "any_of": [
              "カタカナ",
              "かな",
              "ひらがな"
            ]
          },
          {
            "any_of": [
              "小文字",
              "lowercase",
              "lower"
            ]
          }
        ],
        "forbidden_keywords": [
          "形態素",
          "ステミング",
          "stemming"
        ]
      },
      "grading": {
        "min_keyword_coverage": 0.66
      },
      "explanation": "NFKC+カナ→かな+lowercase。クエリと索引対象の両方にかけて表記ゆれ吸収。"
    },
    {
      "id": "q_norm_002",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/normalizer.py",
      "tags": [
        "japanese"
      ],
      "prompt": "カタカナをひらがなにコードポイントシフトで変換する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/normalizer.py",
        "symbol": {
          "kind": "function",
          "name": "_katakana_to_hiragana"
        },
        "evidence_substring": "cp - _KATAKANA_START + _HIRAGANA_START",
        "adr_ref": null,
        "snippet": "if _KATAKANA_START <= cp <= _KATAKANA_END:\n    out.append(chr(cp - _KATAKANA_START + _HIRAGANA_START))",
        "source": "def _katakana_to_hiragana(text: str) -> str:\n    out: list[str] = []\n    for ch in text:\n        cp = ord(ch)\n        if _KATAKANA_START <= cp <= _KATAKANA_END:\n            out.append(chr(cp - _KATAKANA_START + _HIRAGANA_START))\n        else:\n            out.append(ch)\n    return \"\".join(out)",
        "start_line": 20,
        "end_line": 28
      },
      "answer": {
        "file": "backend/src/normalizer.py",
        "symbol": "_katakana_to_hiragana"
      },
      "grading": {
        "symbol_aliases": [
          "_katakana_to_hiragana",
          "katakana_to_hiragana"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "カタカナブロックをひらがな開始へコードポイント差分でシフト。"
    },
    {
      "id": "q_norm_003",
      "difficulty": "L4",
      "type": "mcq",
      "module": "backend/src/normalizer.py",
      "tags": [
        "design",
        "dependencies"
      ],
      "prompt": "正規化を外部ライブラリでなく自前実装している理由は？",
      "citation": {
        "file": "backend/src/normalizer.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "LangChain / 外部ライブラリ非依存",
        "adr_ref": "ADR-001",
        "snippet": "NFKC + カタカナ→ひらがな + lowercase。LangChain / 外部ライブラリ非依存。"
      },
      "answer": {
        "options": [
          "外部の方が遅い",
          "依存を薄くし内部挙動を読める方針(LangChain不採用)",
          "ライセンス制約",
          "日本語は外部では不可能"
        ],
        "correct_index": 1
      },
      "explanation": "『自前実装・薄い依存』がaxisの核方針。"
    },
    {
      "id": "q_chunk_001",
      "difficulty": "L1",
      "type": "mcq",
      "module": "backend/src/chunker.py",
      "tags": [
        "parent-doc"
      ],
      "prompt": "Parent Document Retrievalで parent と child はそれぞれ何の単位？",
      "citation": {
        "file": "backend/src/chunker.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "H2-level sections",
        "adr_ref": "ADR-017",
        "snippet": "parents: H2-level sections (or whole doc when no H2). children: smaller sub-blocks within each parent (H3+/paragraph/token cap)."
      },
      "answer": {
        "options": [
          "parent=ファイル全体/child=H2",
          "parent=H2セクション/child=その中の小ブロック",
          "parent=段落/child=文",
          "ともにファイル単位"
        ],
        "correct_index": 1
      },
      "explanation": "parent=H2(LLMに渡す塊)、child=小ブロック(検索・embedの単位)。"
    },
    {
      "id": "q_chunk_002",
      "difficulty": "L2",
      "type": "location",
      "module": "backend/src/chunker.py",
      "tags": [
        "parent-doc"
      ],
      "prompt": "parentの一意ID({doc_id}#{slug})を生成する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/chunker.py",
        "symbol": {
          "kind": "function",
          "name": "_make_parent_id"
        },
        "evidence_substring": "return f\"{doc_id}#{slug}\"",
        "adr_ref": null,
        "snippet": "slug = _slugify(title)\nif not _is_strong_slug(slug):\n    slug = hashlib.md5(...).hexdigest()[:8]\nreturn f\"{doc_id}#{slug}\"",
        "source": "def _make_parent_id(doc_id: str, title: str) -> str:\n    \"\"\"Deterministic ``{doc_id}#{slug}`` parent identifier.\n\n    The slug is ASCII-folded from the title; titles that fold to a \"weak\"\n    slug (empty, or numeric/symbol-only with no ASCII letters — e.g.\n    JP \"## 1. 目的\" → \"1\") fall back to an md5-hex prefix of the\n    NFKC-normalized title. This keeps the id stable across runs and\n    avoids parent_id collisions when two JP H2s share the same numeric\n    prefix (spec_055). ASCII-strong slugs like \"rag-patterns\" are kept\n    verbatim for backwards compatibility.\n    \"\"\"\n    slug = _slugify(title)\n    if not _is_strong_slug(slug):\n        normalized = unicodedata.normalize(\"NFKC\", title or \"\")\n        slug = hashlib.md5(normalized.encode(\"utf-8\")).hexdigest()[:8]\n    return f\"{doc_id}#{slug}\"",
        "start_line": 320,
        "end_line": 335
      },
      "answer": {
        "file": "backend/src/chunker.py",
        "symbol": "_make_parent_id"
      },
      "grading": {
        "symbol_aliases": [
          "_make_parent_id",
          "make_parent_id"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "弱いslug(ASCII英字なし)はmd5 8桁にフォールバック。"
    },
    {
      "id": "q_chunk_003",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/chunker.py",
      "tags": [
        "parent-doc",
        "bugfix"
      ],
      "prompt": "日本語見出しで_make_parent_idがmd5ハッシュにフォールバックするのはなぜ?(spec_055)",
      "citation": {
        "file": "backend/src/chunker.py",
        "symbol": {
          "kind": "function",
          "name": "_make_parent_id"
        },
        "evidence_substring": "parent_id collisions",
        "adr_ref": null,
        "snippet": "JP \"## 1. 目的\" → \"1\" weak slug. fall back to md5-hex to avoid parent_id collisions when two JP H2s share the same numeric prefix.",
        "source": "def _make_parent_id(doc_id: str, title: str) -> str:\n    \"\"\"Deterministic ``{doc_id}#{slug}`` parent identifier.\n\n    The slug is ASCII-folded from the title; titles that fold to a \"weak\"\n    slug (empty, or numeric/symbol-only with no ASCII letters — e.g.\n    JP \"## 1. 目的\" → \"1\") fall back to an md5-hex prefix of the\n    NFKC-normalized title. This keeps the id stable across runs and\n    avoids parent_id collisions when two JP H2s share the same numeric\n    prefix (spec_055). ASCII-strong slugs like \"rag-patterns\" are kept\n    verbatim for backwards compatibility.\n    \"\"\"\n    slug = _slugify(title)\n    if not _is_strong_slug(slug):\n        normalized = unicodedata.normalize(\"NFKC\", title or \"\")\n        slug = hashlib.md5(normalized.encode(\"utf-8\")).hexdigest()[:8]\n    return f\"{doc_id}#{slug}\"",
        "start_line": 320,
        "end_line": 335
      },
      "answer": {
        "options": [
          "md5が短い",
          "数字だけのslugで別見出しが衝突するため回避",
          "セキュリティ",
          "ASCII禁止"
        ],
        "correct_index": 1
      },
      "explanation": "『1.目的』『1.結論』が共にslug『1』で衝突する問題をmd5で回避。"
    },
    {
      "id": "q_chunk_004",
      "difficulty": "L2",
      "type": "freetext",
      "module": "backend/src/chunker.py",
      "tags": [
        "parent-doc",
        "slug"
      ],
      "prompt": "_is_strong_slug が True を返す条件は？",
      "citation": {
        "file": "backend/src/chunker.py",
        "symbol": {
          "kind": "function",
          "name": "_is_strong_slug"
        },
        "evidence_substring": "at least one ASCII",
        "adr_ref": null,
        "snippet": "A slug is \"strong\" iff it contains at least one ASCII [a-z] letter. Empty/numeric-only are weak.",
        "source": "def _is_strong_slug(slug: str) -> bool:\n    \"\"\"A slug is \"strong\" iff it contains at least one ASCII [a-z] letter.\n\n    Empty slugs and numeric/symbol-only slugs (\"1\", \"3-1\", \"2-\") are weak\n    and must be replaced with a hash to avoid collisions across JP titles\n    that ASCII-fold to the same residue.\n    \"\"\"\n    if not slug:\n        return False\n    return bool(_ASCII_ALPHA_RE.search(slug))",
        "start_line": 338,
        "end_line": 347
      },
      "answer": {
        "model_answer": "slugが空でなくASCII英字[a-z]を1文字以上含むとき。",
        "required_keywords": [
          {
            "any_of": [
              "ascii",
              "英字",
              "アルファベット",
              "a-z",
              "英文字"
            ]
          },
          {
            "any_of": [
              "1文字",
              "少なくとも",
              "含",
              "一文字"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.5
      },
      "explanation": "ASCII英字を含むslugだけ『強い』。数字/記号のみや空は弱い。"
    },
    {
      "id": "q_vs_001",
      "difficulty": "L1",
      "type": "mcq",
      "module": "backend/src/vector_store.py",
      "tags": [
        "storage"
      ],
      "prompt": "VectorStoreがベクトルと軸メタデータの保存に使うのは？",
      "citation": {
        "file": "backend/src/vector_store.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "ChromaDB wrapper",
        "adr_ref": null,
        "snippet": "ChromaDB wrapper for storing Documents with axis metadata."
      },
      "answer": {
        "options": [
          "FAISS",
          "ChromaDB",
          "Pinecone",
          "SQLiteのみ"
        ],
        "correct_index": 1
      },
      "explanation": "ChromaDBをローカル永続で使用。軸メタデータも格納し検索前に絞り込む。"
    },
    {
      "id": "q_vs_002",
      "difficulty": "L2",
      "type": "location",
      "module": "backend/src/vector_store.py",
      "tags": [
        "live-ingest",
        "spec_056"
      ],
      "prompt": "チャンク単位で特定docを索引から削除する(live ingestの冪等更新用)メソッドは？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/vector_store.py",
        "symbol": {
          "kind": "method",
          "name": "VectorStore.delete_doc"
        },
        "evidence_substring": "def delete_doc(self, doc_id",
        "adr_ref": null,
        "snippet": "def delete_doc(self, doc_id: str) -> int:\n    # spec_056: chunk-level removal for idempotent re-ingest",
        "source": "    def delete_doc(self, doc_id: str) -> int:\n        \"\"\"Remove every chunk + parent belonging to ``doc_id``. Returns deleted child count.\n\n        Used by the live-ingest path (spec_056) to make memo re-ingest behave\n        as upsert: re-chunking with the same ``doc_id`` produces deterministic\n        ``parent_id`` / ``child_id`` (v0.9.3, spec_055), so a plain ``add``\n        would trip Chroma's DuplicateIDError. We drop both sides — children\n        from the Chroma collection (matched on the ``doc_id`` metadata) and\n        parents from the SQLite sidecar — so the next ``add_chunks`` is a\n        clean insert. No-op on unknown ids; safe to call before every add.\n        \"\"\"\n        deleted = 0\n        try:\n            existing = self._collection.get(where={\"doc_id\": doc_id}, include=[])\n            ids = list(existing.get(\"ids\") or [])\n            if ids:\n                self._collection.delete(ids=ids)\n                deleted = len(ids)\n        except Exception as e:  # noqa: BLE001\n            logger.warning(\"delete_doc: chroma delete failed for %s: %s\", doc_id, e)\n        try:\n            removed_parents = self._parent_storage_delete_by_doc(doc_id)\n            if removed_parents:\n                self._parents = {\n                    pid: p for pid, p in self._parents.items() if p.doc_id != doc_id\n                }\n        except Exception as e:  # noqa: BLE001\n            logger.warning(\"delete_doc: parent storage delete failed for %s: %s\", doc_id, e)\n        return deleted",
        "start_line": 242,
        "end_line": 270
      },
      "answer": {
        "file": "backend/src/vector_store.py",
        "symbol": "delete_doc"
      },
      "grading": {
        "symbol_aliases": [
          "delete_doc"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "既存メモ更新はdelete_doc+再ingestで冪等(spec_056)。"
    },
    {
      "id": "q_vs_003",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/vector_store.py",
      "tags": [
        "parent-doc",
        "storage"
      ],
      "prompt": "Parent Document方式で、childはChromaにembedされるがparentはどこに永続化される?(spec_037)",
      "citation": {
        "file": "backend/src/vector_store.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "SQLite sidecar",
        "adr_ref": "ADR-023",
        "snippet": "add_chunks() embeds child sub-blocks into Chroma while persisting parents to a SQLite sidecar (parents.db). spec_037 migrates from parents.json to SQLite."
      },
      "answer": {
        "options": [
          "Chromaにparentもembed",
          "SQLiteサイドカー(parents.db)",
          "メモリのみ",
          "localStorage"
        ],
        "correct_index": 1
      },
      "explanation": "childだけembed、parentはSQLiteサイドカー。spec_037でparents.jsonから移行。"
    },
    {
      "id": "q_graph_001",
      "difficulty": "L1",
      "type": "mcq",
      "module": "backend/src/graph.py",
      "tags": [
        "graphrag"
      ],
      "prompt": "KnowledgeGraphはどのライブラリのどんなグラフ型で構築される？",
      "citation": {
        "file": "backend/src/graph.py",
        "symbol": {
          "kind": "method",
          "name": "KnowledgeGraph.__init__"
        },
        "evidence_substring": "nx.DiGraph",
        "adr_ref": "ADR-024",
        "snippet": "def __init__(self, graph: nx.DiGraph): ...  # networkx>=3.0. source->target means source.refs contains target.",
        "source": "    def __init__(self, graph: nx.DiGraph) -> None:\n        self._g = graph\n        self._node_cache: dict[str, GraphNode] = {}",
        "start_line": 48,
        "end_line": 50
      },
      "answer": {
        "options": [
          "Neo4jの無向グラフ",
          "networkxの有向グラフ(DiGraph)",
          "Chromaのグラフ索引",
          "自前隣接リスト"
        ],
        "correct_index": 1
      },
      "explanation": "networkxのDiGraph。refs:からsource→targetの有向辺。"
    },
    {
      "id": "q_graph_002",
      "difficulty": "L2",
      "type": "location",
      "module": "backend/src/graph.py",
      "tags": [
        "graphrag"
      ],
      "prompt": "あるノードから指定ホップ内の隣接ノードをBFSで集めるメソッドは？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/graph.py",
        "symbol": {
          "kind": "method",
          "name": "KnowledgeGraph.neighbors_within_hop"
        },
        "evidence_substring": "def neighbors_within_hop",
        "adr_ref": null,
        "snippet": "def neighbors_within_hop(self, doc_id, hop=1, max_neighbors=10):\n    # BFS over the DiGraph",
        "source": "    def neighbors_within_hop(\n        self,\n        doc_id: str,\n        *,\n        hop: int = 1,\n        max_neighbors: int = 20,\n        direction: str = \"both\",\n    ) -> list[str]:\n        \"\"\"Return neighbour ids within ``hop`` BFS levels (deduplicated).\n\n        ``direction`` is one of ``\"out\"`` (successors only), ``\"in\"``\n        (predecessors only) or ``\"both\"`` (default; combines them).\n        Returns up to ``max_neighbors`` ids in BFS order; the search\n        center itself is never included.\n        \"\"\"\n        if hop < 1 or max_neighbors < 1:\n            return []\n        if doc_id not in self._g:\n            return []\n        visited: set[str] = {doc_id}\n        frontier: list[str] = [doc_id]\n        results: list[str] = []\n        for _ in range(hop):\n            next_frontier: list[str] = []\n            for node in frontier:\n                for n in self._collect_neighbors(node, direction):\n                    if n in visited:\n                        continue\n                    visited.add(n)\n                    results.append(n)\n                    next_frontier.append(n)\n                    if len(results) >= max_neighbors:\n                        return results\n            frontier = next_frontier\n            if not frontier:\n                break\n        return results",
        "start_line": 105,
        "end_line": 141
      },
      "answer": {
        "file": "backend/src/graph.py",
        "symbol": "neighbors_within_hop"
      },
      "grading": {
        "symbol_aliases": [
          "neighbors_within_hop"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "search(graph_expand=True)がtop hitsのこれを呼び隣接docをマージ。"
    },
    {
      "id": "q_graph_003",
      "difficulty": "L2",
      "type": "fill_blank",
      "module": "backend/src/search.py",
      "tags": [
        "graphrag"
      ],
      "prompt": "グラフ拡張で隣接ノードをマージする際、元ヒットのスコアに掛ける減衰係数は？\n    score = source.score × ____",
      "citation": {
        "file": "backend/src/search.py",
        "symbol": {
          "kind": "method",
          "name": "SearchEngine.search"
        },
        "evidence_substring": "0.7",
        "adr_ref": "ADR-024",
        "snippet": "merge 1-hop refs neighbours of the top 5 hits with a 0.7x score decay relative to their source.",
        "source": "    def search(\n        self,\n        query: str | None,\n        *,\n        filters: dict[str, Any] | None = None,\n        top_k: int = 5,\n        bm25_weight: float = 0.5,\n        graph_expand: bool = False,\n        graph_hop: int = 1,\n        graph_max_neighbors: int = 10,\n    ) -> list[SearchResult]:\n        \"\"\"Hybrid search (axis + vector + optional BM25 fusion + optional graph expand).\n\n        Args:\n            query: Natural-language query. If None, axis-only search (top_k arbitrary).\n            filters: User-friendly axis filters (`{\"category\": \"技術記事\"}`).\n            top_k: Maximum results to return.\n            bm25_weight: Weight of the BM25 score in the weighted-sum fusion\n                with the vector cosine score. ``0.0`` falls back to the v0.5\n                vector-only behaviour, ``1.0`` ranks purely by BM25. Ignored\n                when no ``bm25_index`` is wired into the engine or when the\n                query is ``None``.\n            graph_expand: spec_040. If True and the engine has a graph\n                wired, merge 1-hop refs neighbours of the top 5 hits into\n                the result set with a 0.7× score decay relative to their\n                source.\n            graph_hop: BFS depth for graph expansion (default 1).\n            graph_max_neighbors: per-source neighbour cap.\n\n        Query / filters are normalized before being passed to Chroma so that\n        the index (which stores `axis_*_norm` keys) can match across writing\n        variants (全角/半角・カタカナ/ひらがな・大文字小文字).\n        \"\"\"\n        norm_filters = (\n            {k: self._normalizer(str(v)) for k, v in filters.items()}\n            if filters\n            else None\n        )\n        where = _build_where_norm(norm_filters or {})\n        use_bm25 = (\n            query is not None\n            and self._bm25_index is not None\n            and bm25_weight > 0.0\n        )\n\n        if self._parent_doc_enabled:\n            pd_results = self._search_parent_doc(\n                query, where=where, top_k=top_k, bm25_weight=bm25_weight\n            )\n            if graph_expand:\n                pd_results = self._expand_with_graph(\n                    pd_results, hop=graph_hop, max_neighbors=graph_max_neighbors\n                )\n            self._record_gap(query, pd_results)\n            return pd_results\n\n        if query is None:\n            # Axis-only path: use a zero embedding (Chroma will then sort by\n            # distance from zero, which is arbitrary — but we mostly care about\n            # the filter). top_k is bounded by collection size.\n            n = min(top_k, max(self._store.count(), 1))\n            embedding = [0.0] * self._embedder.dim\n        else:\n            q_norm = self._normalizer(query)\n            embedding = self._embedder.embed(q_norm)\n            # Over-fetch when fusing so BM25 can re-rank candidates that\n            # ranked just outside the original vector top_k.\n            n = max(top_k * 2, 20) if use_bm25 else top_k\n\n        raw = self._store.query(embedding=embedding, n_results=n, where=where)\n        results = _to_results(raw)\n\n        if use_bm25:\n            assert query is not None and self._bm25_index is not None\n            bm25_scores = self._bm25_index.score(query)\n            fused: list[SearchResult] = []\n            for r in results:\n                bm25 = bm25_scores.get(r.id, 0.0)\n                final = (1.0 - bm25_weight) * r.score + bm25_weight * bm25\n                fused.append(\n                    SearchResult(\n                        id=r.id,\n                        title=r.title,\n                        score=final,\n                        axes=r.axes,\n                        body_snippet=r.body_snippet,\n                        path=r.path,\n                        refs=r.refs,\n                        metadata=r.metadata,\n                    )\n                )\n            fused.sort(key=lambda r: r.score, reverse=True)\n            results = fused[:top_k]\n\n        # spec_035: apply time-weighted decay after fusion (re-sort needed).\n        td = self._time_decay_config\n        if td is not None and td.enabled and td.weight > 0:\n            results = _apply_time_decay(results, td)\n            results.sort(key=lambda r: r.score, reverse=True)\n            results = results[:top_k]\n\n        # spec_040: graph expansion (post-decay). Adds 1-hop refs neighbours\n        # of the top hits with a 0.7× decay multiplier. Caller-opt-in only.\n        if graph_expand:\n            results = self._expand_with_graph(\n                results, hop=graph_hop, max_neighbors=graph_max_neighbors\n            )\n\n        logger.info(\n            \"search(query=%r, filters=%s, bm25_weight=%.2f, graph_expand=%s) -> %d results\",\n            query,\n            filters,\n            bm25_weight,\n            graph_expand,\n            len(results),\n        )\n        self._record_gap(query, results)\n        return results",
        "start_line": 191,
        "end_line": 308
      },
      "answer": {
        "accepted": [
          "0.7",
          "0.7倍",
          "x0.7",
          "×0.7"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "隣接は元の0.7倍スコアで追加され再ソート。"
    },
    {
      "id": "q_graph_004",
      "difficulty": "L4",
      "type": "mcq",
      "module": "docs/adr/ADR-024-graphrag-retrieval-expansion.md",
      "tags": [
        "graphrag",
        "design"
      ],
      "prompt": "グラフ層にNeo4jでなくin-processのnetworkxを選んだ理由は?(ADR-024)",
      "citation": {
        "file": "docs/adr/ADR-024-graphrag-retrieval-expansion.md",
        "symbol": {
          "kind": "section",
          "name": "Alternatives"
        },
        "evidence_substring": "local-first ethos",
        "adr_ref": "ADR-024",
        "snippet": "Neo4j rejected: sidecar daemon for <=10k edges doesn't fit the local-first ethos. In-process networkx chosen: zero extra processes."
      },
      "answer": {
        "options": [
          "networkxが高速",
          "別プロセス不要でlocal-first方針に合い<=10k辺に十分",
          "Neo4jは日本語非対応",
          "Chroma統合済み"
        ],
        "correct_index": 1
      },
      "explanation": "数千〜1万辺にdaemonは過剰。プロセス追加ゼロのnetworkxがlocal-firstに合致。"
    },
    {
      "id": "q_embed_001",
      "difficulty": "L1",
      "type": "mcq",
      "module": "backend/src/embedder.py",
      "tags": [
        "embedder"
      ],
      "prompt": "EmbedderがProtocolの背後に持つ3バックエンドは？",
      "citation": {
        "file": "backend/src/embedder.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "Gemini / Ollama / Dummy",
        "adr_ref": "ADR-026",
        "snippet": "Embedder backends (Gemini / Ollama / Dummy) behind a Protocol."
      },
      "answer": {
        "options": [
          "OpenAI/Cohere/HF",
          "Gemini/Ollama/Dummy",
          "Claude/Gemini/GPT",
          "FAISS/Chroma/Pinecone"
        ],
        "correct_index": 1
      },
      "explanation": "Gemini(既定)/Ollama(オンプレ)/Dummy(キー無し)をProtocolで抽象化。"
    },
    {
      "id": "q_embed_002",
      "difficulty": "L2",
      "type": "location",
      "module": "backend/src/embedder.py",
      "tags": [
        "embedder",
        "factory"
      ],
      "prompt": "設定に応じて適切なEmbedder実装を選んで返すファクトリ関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/embedder.py",
        "symbol": {
          "kind": "function",
          "name": "make_embedder"
        },
        "evidence_substring": "def make_embedder",
        "adr_ref": null,
        "snippet": "def make_embedder(cfg=None) -> Embedder:\n    # picks Gemini/Ollama/Dummy based on config/keys",
        "source": "def make_embedder(cfg: EmbedderConfig | None = None) -> Embedder:\n    \"\"\"Build an embedder backend from ``EmbedderConfig``.\n\n    - ``cfg is None`` → load defaults (``backend=\"gemini\"``).\n    - ``backend=\"gemini\"`` (default): Gemini API; auto-falls back to dummy\n      vectors when ``GEMINI_API_KEY`` is missing (v0.8.1 behaviour).\n    - ``backend=\"ollama\"``: spec_045 fully on-prem path. If the optional\n      ``ollama`` package is missing or the server is unreachable, logs a\n      warning and falls back to ``DummyEmbedder`` so app startup doesn't\n      block.\n    - ``backend=\"dummy\"``: force dummy vectors.\n    \"\"\"\n    if cfg is None:\n        from backend.src.config import EmbedderConfig as _EC\n\n        cfg = _EC()\n    backend = (cfg.backend or \"gemini\").lower()\n    if backend == \"ollama\":\n        try:\n            return OllamaEmbedder(model=cfg.ollama.model, url=cfg.ollama.url)\n        except Exception as e:  # noqa: BLE001 — never block startup\n            logger.warning(\n                \"Ollama embedder failed (%s), falling back to DummyEmbedder\", e\n            )\n            return DummyEmbedder()\n    if backend == \"dummy\":\n        return DummyEmbedder()\n    if backend != \"gemini\":\n        logger.warning(\"Unknown embedder backend %r, falling back to gemini\", backend)\n    return GeminiEmbedder()",
        "start_line": 251,
        "end_line": 280
      },
      "answer": {
        "file": "backend/src/embedder.py",
        "symbol": "make_embedder"
      },
      "grading": {
        "symbol_aliases": [
          "make_embedder"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "configと利用可能キーから選択。キー無しはDummyEmbedder。"
    },
    {
      "id": "q_embed_003",
      "difficulty": "L4",
      "type": "mcq",
      "module": "backend/src/embedder.py",
      "tags": [
        "embedder",
        "design"
      ],
      "prompt": "GEMINI_API_KEY未設定時にDummyEmbedder(ハッシュ由来の決定的ベクトル)へフォールバックする狙いは？",
      "citation": {
        "file": "backend/src/embedder.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "without hitting the network",
        "adr_ref": null,
        "snippet": "falls back to DummyEmbedder — deterministic hash-derived 768-dim vectors so downstream paths can be exercised in CI / offline dev without hitting the network."
      },
      "answer": {
        "options": [
          "本番でもDummyで十分",
          "キー無し/CI/オフラインでも下流コードパスを動作確認できるように",
          "Geminiコストを無くす",
          "セキュリティ要件"
        ],
        "correct_index": 1
      },
      "explanation": "DUMMYモードでキー無しでもUI・検索を通せる。CI/オフライン用。"
    },
    {
      "id": "q_rag_001",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/rag.py",
      "tags": [
        "rag",
        "context"
      ],
      "prompt": "検索ヒットのparent本文を連結してLLMに渡すコンテキストを組み立てる関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/rag.py",
        "symbol": {
          "kind": "function",
          "name": "build_context"
        },
        "evidence_substring": "def build_context",
        "adr_ref": "ADR-017",
        "snippet": "def build_context(results, max_chars=8000):\n    # concatenate parent bodies, trimmed",
        "source": "def build_context(\n    results: list[SearchResult],\n    *,\n    max_chars: int = 8000,\n) -> str:\n    \"\"\"Concatenate parent (or doc) bodies with citation headers, capped by chars.\n\n    Uses ``body_full`` when populated (parent-doc mode) — that's the entire\n    H2 section text. Falls back to ``body_snippet`` otherwise, so the legacy\n    file-level path keeps working without changes. The hard ``max_chars``\n    budget protects the LLM context window — blocks beyond the budget are\n    dropped silently rather than truncated mid-sentence.\n    \"\"\"\n    out: list[str] = []\n    used = 0\n    for i, r in enumerate(results, 1):\n        body = r.body_full or r.body_snippet or \"\"\n        block = (\n            f\"## 出典 {i}: [{r.id}] {r.title}\\n\"\n            f\"(file: {r.path})\\n\"\n            f\"axes: {r.axes}\\n\\n\"\n            f\"{body}\\n\\n\"\n        )\n        if used + len(block) > max_chars:\n            break\n        out.append(block)\n        used += len(block)\n    return \"\".join(out)",
        "start_line": 111,
        "end_line": 138
      },
      "answer": {
        "file": "backend/src/rag.py",
        "symbol": "build_context"
      },
      "grading": {
        "symbol_aliases": [
          "build_context",
          "_format_context"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "parent全文を連結しmax_charsでトリムしてプロンプトへ。"
    },
    {
      "id": "q_rag_002",
      "difficulty": "L2",
      "type": "fill_blank",
      "module": "backend/src/rag.py",
      "tags": [
        "rag",
        "gemini"
      ],
      "prompt": "GeminiBackendの既定モデル名(DEFAULT_MODEL)を答えよ。",
      "citation": {
        "file": "backend/src/rag.py",
        "symbol": {
          "kind": "class",
          "name": "GeminiBackend"
        },
        "evidence_substring": "DEFAULT_MODEL = \"gemini-2.5-flash\"",
        "adr_ref": "ADR-031",
        "snippet": "class GeminiBackend:\n    DEFAULT_MODEL = \"gemini-2.5-flash\"",
        "source": "class GeminiBackend:\n    \"\"\"Gemini chat backend (spec_052).\n\n    Reuses ``google-generativeai`` (already a hard dep for the embedder) so\n    deployments that have ``GEMINI_API_KEY`` set but lack\n    ``ANTHROPIC_API_KEY`` can still get a real generation step instead of\n    falling all the way to DUMMY. The factory wires this in automatically\n    via ``generation.backend=\"auto\"``.\n    \"\"\"\n\n    DEFAULT_MODEL = \"gemini-2.5-flash\"\n\n    def __init__(\n        self,\n        *,\n        model: str = DEFAULT_MODEL,\n        api_key: str | None = None,\n    ) -> None:\n        import google.generativeai as genai\n\n        self._api_key = api_key or settings.gemini_api_key or os.environ.get(\n            \"GEMINI_API_KEY\", \"\"\n        )\n        if not self._api_key:\n            raise RuntimeError(\"GEMINI_API_KEY not set for GeminiBackend\")\n        genai.configure(api_key=self._api_key)\n        self._model = model\n        self._client = genai.GenerativeModel(model)\n\n    @property\n    def model_name(self) -> str:\n        return self._model\n\n    @property\n    def is_dummy(self) -> bool:\n        return False\n\n    def generate(\n        self,\n        system: str,\n        messages: list[dict[str, str]],\n        *,\n        max_tokens: int = 1024,\n    ) -> str:\n        # Gemini has a ``system_instruction`` parameter, but encoding the\n        # system + roles into the prompt body keeps the call site\n        # provider-agnostic and avoids per-SDK-version drift in how\n        # ``system_instruction`` is consumed. Citation behaviour is identical.\n        parts: list[str] = []\n        if system:\n            parts.append(f\"[SYSTEM]\\n{system}\\n\\n\")\n        for m in messages:\n            role = (m.get(\"role\") or \"user\").upper()\n            content = m.get(\"content\", \"\")\n            parts.append(f\"[{role}]\\n{content}\\n\\n\")\n        prompt = \"\".join(parts).rstrip()\n\n        resp = self._client.generate_content(\n            prompt,\n            generation_config={\n                \"max_output_tokens\": max_tokens,\n                \"temperature\": 0.2,\n            },\n        )\n        return getattr(resp, \"text\", \"\") or \"\"",
        "start_line": 250,
        "end_line": 314
      },
      "answer": {
        "accepted": [
          "gemini-2.5-flash",
          "\"gemini-2.5-flash\""
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "gemini-2.5-flashをtemperature=0.2で使用。"
    },
    {
      "id": "q_rag_003",
      "difficulty": "L1",
      "type": "mcq",
      "module": "backend/src/rag.py",
      "tags": [
        "rag",
        "backends"
      ],
      "prompt": "GenerationBackend Protocolに実装された4バックエンドは？",
      "citation": {
        "file": "backend/src/rag.py",
        "symbol": {
          "kind": "module",
          "name": "backends"
        },
        "evidence_substring": "class DummyGenerationBackend",
        "adr_ref": "ADR-031",
        "snippet": "class ClaudeBackend: ...\nclass GeminiBackend: ...\nclass OllamaBackend: ...\nclass DummyGenerationBackend: ..."
      },
      "answer": {
        "options": [
          "Claude/GPT-4/Gemini/Llama",
          "Claude/Gemini/Ollama/Dummy",
          "Gemini/Ollama/FAISS/Dummy",
          "Claude/Ollama/Dummyの3つ"
        ],
        "correct_index": 1
      },
      "explanation": "Claude(既定)/Gemini/Ollama/Dummy。autoモードで実行時選択。"
    },
    {
      "id": "q_rag_004",
      "difficulty": "L4",
      "type": "mcq",
      "module": "docs/adr/ADR-031-gemini-generation-backend.md",
      "tags": [
        "rag",
        "design"
      ],
      "prompt": "v0.9.1でGemini生成バックエンドとautoモードを追加した主な動機は?(ADR-031)",
      "citation": {
        "file": "docs/adr/ADR-031-gemini-generation-backend.md",
        "symbol": {
          "kind": "section",
          "name": "Context"
        },
        "evidence_substring": "already holding a Gemini key",
        "adr_ref": "ADR-031",
        "snippet": "Many users come into the Cowork plugin already holding a Gemini key (the embedder requires it). google-generativeai is already a dependency, so adding Gemini generation costs zero new packages."
      },
      "answer": {
        "options": [
          "GeminiがClaudeより高性能",
          "埋め込み用にGeminiキーを既に持つ人が追加依存ゼロで生成も賄えるため",
          "Anthropic廃止",
          "オフライン化"
        ],
        "correct_index": 1
      },
      "explanation": "2つ目のキー無しで回答を得られるonboarding改善。"
    },
    {
      "id": "q_rag_005",
      "difficulty": "L3",
      "type": "dataflow",
      "module": "backend/src/rag.py",
      "tags": [
        "rag",
        "pipeline"
      ],
      "prompt": "RAGPipeline.answerが回答を返すまでの主要ステップを順に並べよ。",
      "citation": {
        "file": "backend/src/rag.py",
        "symbol": {
          "kind": "method",
          "name": "RAGPipeline.answer"
        },
        "evidence_substring": "build_context",
        "adr_ref": null,
        "snippet": "results = self._engine.search(question)\ncontext = build_context(results)\ntext = self._call_generation(system, messages)\n# parse [N] citations -> cited_ids",
        "source": "    def answer(\n        self,\n        question: str,\n        *,\n        filters: dict[str, Any] | None = None,\n        top_k: int = 5,\n        max_tokens: int = 1024,\n    ) -> Answer:\n        results = self._engine.search(question, filters=filters, top_k=top_k)\n        if self._use_dummy:\n            ans = _dummy_answer(question, results)\n            self._record_llm_gap(question, ans.text, results)\n            return ans\n\n        # Prefer build_context() — uses full parent text in parent-doc mode,\n        # falls back to snippets in legacy mode. Same prompt either way.\n        if any(r.body_full for r in results):\n            context = build_context(results, max_chars=self._context_max_chars)\n        else:\n            context = _format_context(results)\n        user_msg = (\n            f\"# 質問\\n{question}\\n\\n\"\n            f\"# 提供された資料 (上位 {len(results)} 件)\\n\\n{context}\\n\\n\"\n            \"上記の資料のみを根拠に、出典マーク [N] (N は 1 始まり、上の 出典 i の i と一致) を付けて回答してください。\"\n        )\n        raw = self._call_generation(\n            system=SYSTEM_PROMPT,\n            messages=[{\"role\": \"user\", \"content\": user_msg}],\n            max_tokens=max_tokens,\n        )\n        text, used = parse_and_validate_citations(raw, n_sources=len(results))\n        cited_ids = [results[i].id for i in sorted(used)]\n        self._record_llm_gap(question, text, results)\n        return Answer(\n            text=text,\n            sources=results,\n            cited_ids=cited_ids,\n            is_dummy=False,\n            model=self._backend.model_name,\n        )",
        "start_line": 547,
        "end_line": 586
      },
      "answer": {
        "sequence": [
          "search",
          "build_context",
          "generate",
          "citation"
        ]
      },
      "grading": {
        "mode": "subsequence",
        "pass_threshold": 0.75,
        "aliases": [
          [
            "search",
            "retrieve",
            "検索"
          ],
          [
            "build_context",
            "context",
            "コンテキスト",
            "文脈"
          ],
          [
            "generate",
            "生成",
            "llm"
          ],
          [
            "citation",
            "引用",
            "cited_ids",
            "出典"
          ]
        ]
      },
      "pool": [
        "build_context",
        "citation",
        "search",
        "generate"
      ],
      "explanation": "検索→build_contextで連結→LLM生成→[N]引用パースしてcited_ids/sources。"
    },
    {
      "id": "q_mcp_001",
      "difficulty": "L1",
      "type": "mcq",
      "module": "mcp_server/server.py",
      "tags": [
        "mcp"
      ],
      "prompt": "MCPサーバが使うトランスポートは？",
      "citation": {
        "file": "mcp_server/server.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "stdio transport",
        "adr_ref": null,
        "snippet": "axis_knowledge_rag MCP server (stdio transport, FastMCP-based)."
      },
      "answer": {
        "options": [
          "HTTP/SSE",
          "stdio",
          "WebSocket",
          "gRPC"
        ],
        "correct_index": 1
      },
      "explanation": "FastMCPベースのstdio。python -m mcp_serverで起動。"
    },
    {
      "id": "q_mcp_002",
      "difficulty": "L2",
      "type": "location",
      "module": "mcp_server/server.py",
      "tags": [
        "mcp",
        "live-ingest"
      ],
      "prompt": "メモを受け取りbackend経由のlive ingestを起動するMCPツール関数は？（ファイルとシンボル）",
      "citation": {
        "file": "mcp_server/server.py",
        "symbol": {
          "kind": "function",
          "name": "axis_ingest_memo"
        },
        "evidence_substring": "async def axis_ingest_memo",
        "adr_ref": null,
        "snippet": "async def axis_ingest_memo(params: IngestInput) -> str:\n    # spec_056: triggers live ingest via backend",
        "source": "async def axis_ingest_memo(params: IngestInput) -> str:\n    \"\"\"Convert a raw memo text into axis-knowledge-rag YAML-frontmatter Markdown.\n\n    Pipeline: read existing knowledge_dir → next doc_NNN id + existing ref list →\n    Claude fills axes/tags/title/body under strict axes_constraints from config.yml.\n    Returns rendered Markdown by default; pass `response_format='json'` for the\n    structured `IngestResult` payload plus the rendered Markdown.\n\n    spec_056: ``live_ingest=true`` (default) POSTs the rendered markdown to the\n    running backend's ``/api/ingest/memo`` so the memo becomes searchable + visible\n    in /api/graph without a backend restart. If the backend is not reachable,\n    the tool still returns the rendered markdown (``indexed=false``) so callers\n    can save it manually + run ``build_index --rebuild`` later.\n\n    DUMMY mode (no `ANTHROPIC_API_KEY`) returns a deterministic mock — useful\n    for plumbing tests but not for actual ingestion.\n    \"\"\"\n    try:\n        import os\n\n        from backend.src.ingester import Ingester, render_markdown\n        from backend.src.ingester_schemas import IngestOptions\n\n        ingester = Ingester()\n        opts = IngestOptions(\n            knowledge_dir=params.knowledge_dir,\n            suggested_category=params.suggested_category,\n            max_tokens=params.max_tokens,\n        )\n        result = ingester.ingest(params.raw_text, opts)\n        md = render_markdown(result)\n\n        live_resp: dict | None = None\n        if params.live_ingest:\n            backend_url = (\n                params.backend_url\n                or os.environ.get(\"AXIS_BACKEND_URL\", \"\").strip()\n                or _DEFAULT_BACKEND_URL\n            )\n            live_resp = _live_ingest_via_backend(md, backend_url)\n\n        if params.response_format == ResponseFormat.JSON:\n            payload = {\n                \"id\": result.id,\n                \"title\": result.title,\n                \"axes\": result.axes,\n                \"tags\": result.tags,\n                \"refs\": result.refs,\n                \"rendered_md\": md,\n                \"is_dummy\": ingester.is_dummy,\n                \"indexed\": bool(live_resp and live_resp.get(\"indexed\")),\n            }\n            if live_resp:\n                payload[\"live_ingest\"] = live_resp\n            return json.dumps(payload, ensure_ascii=False, indent=2)\n        if live_resp and live_resp.get(\"indexed\"):\n            return (\n                f\"<!-- indexed: {live_resp.get('parents', 0)} parents / \"\n                f\"{live_resp.get('children', 0)} children -->\\n{md}\"\n            )\n        return md\n    except Exception as e:\n        return make_error_response(\"axis_ingest_memo\", e)",
        "start_line": 486,
        "end_line": 548
      },
      "answer": {
        "file": "mcp_server/server.py",
        "symbol": "axis_ingest_memo"
      },
      "grading": {
        "symbol_aliases": [
          "axis_ingest_memo"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "_live_ingest_via_backendを呼びリビルド無しで約1秒で検索可能に(spec_056)。"
    },
    {
      "id": "q_fe_001",
      "difficulty": "L1",
      "type": "location",
      "module": "frontend/src/lib/api.ts",
      "tags": [
        "frontend"
      ],
      "prompt": "フロントがFastAPIを叩くAPIクライアント(search/answer/axes等)はどのファイル？",
      "citation": {
        "file": "frontend/src/lib/api.ts",
        "symbol": {
          "kind": "const",
          "name": "api"
        },
        "evidence_substring": "export const api =",
        "adr_ref": null,
        "snippet": "export const api = {\n  axes: () => jsonFetch('/api/axes'),\n  search: (body) => jsonFetch('/api/search', {...}),\n};",
        "source": "export const api = {\n  health: () =>\n    jsonFetch<{ status: string; embedder_mode: string; rag_mode: string }>(\n      \"/api/health\",\n    ),\n  axes: () => jsonFetch<AxesResponse>(\"/api/axes\"),\n  search: (body: {\n    query?: string | null;\n    filters?: Record<string, string | number>;\n    top_k?: number;\n  }) =>\n    jsonFetch<SearchResponse>(\"/api/search\", {\n      method: \"POST\",\n      body: JSON.stringify(body),\n    }),\n  answer: (body: {\n    question: string;\n    filters?: Record<string, string | number>;\n    top_k?: number;\n    max_tokens?: number;\n  }) =>\n    jsonFetch<AnswerResponse>(\"/api/answer\", {\n      method: \"POST\",\n      body: JSON.stringify(body),",
        "start_line": 52,
        "end_line": 75
      },
      "answer": {
        "file": "frontend/src/lib/api.ts",
        "symbol": "api"
      },
      "grading": {
        "symbol_aliases": [
          "api",
          "jsonFetch"
        ],
        "accept_file_only_partial": 0.7
      },
      "explanation": "lib/api.tsのapiオブジェクト。型はbackend/src/schemas.pyをミラー。"
    },
    {
      "id": "q_fe_002",
      "difficulty": "L2",
      "type": "mcq",
      "module": "frontend/src/lib/citations.ts",
      "tags": [
        "frontend",
        "citation"
      ],
      "prompt": "citations.tsが[N]マーカーをパースする際、コードフェンスやインラインコード内の[1]をどう扱う?(spec_039)",
      "citation": {
        "file": "frontend/src/lib/citations.ts",
        "symbol": {
          "kind": "function",
          "name": "buildSkipRanges"
        },
        "evidence_substring": "code fences",
        "adr_ref": null,
        "snippet": "Markers inside Markdown code fences or inline code spans are preserved as literal text (spec_039) — arr[1] in a code block is not a citation.",
        "source": "function buildSkipRanges(text: string): [number, number][] {\n  const ranges: [number, number][] = [];\n  RE_CODE_SPANS.lastIndex = 0;\n  let m: RegExpExecArray | null;\n  while ((m = RE_CODE_SPANS.exec(text)) !== null) {\n    ranges.push([m.index, m.index + m[0].length]);\n  }\n  return ranges;\n}\n\nfunction isInSkipRange(pos: number, ranges: [number, number][]): boolean {\n  for (const [s, e] of ranges) {\n    if (s <= pos && pos < e) return true;\n  }\n  return false;\n}\n\nexport function parseCitations(text: string): Segment[] {\n  const segments: Segment[] = [];\n  const skipRanges = buildSkipRanges(text);\n  let lastIndex = 0;\n  let m: RegExpExecArray | null;\n  RE_MARKER.lastIndex = 0;\n  while ((m = RE_MARKER.exec(text)) !== null) {",
        "start_line": 26,
        "end_line": 49
      },
      "answer": {
        "options": [
          "引用として扱う",
          "リテラル文字として扱い引用にしない(skip range)",
          "エラー",
          "全削除"
        ],
        "correct_index": 1
      },
      "explanation": "コード領域をskip rangeで除外。Python側_build_skip_rangesをミラー。"
    },
    {
      "id": "q_fe_003",
      "difficulty": "L1",
      "type": "fill_blank",
      "module": "frontend/src/lib/api.ts",
      "tags": [
        "frontend",
        "config"
      ],
      "prompt": "API_BASEのデフォルト値(環境変数未設定時)を答えよ。",
      "citation": {
        "file": "frontend/src/lib/api.ts",
        "symbol": {
          "kind": "const",
          "name": "API_BASE"
        },
        "evidence_substring": "http://localhost:8000",
        "adr_ref": null,
        "snippet": "const API_BASE = process.env.NEXT_PUBLIC_API_BASE ?? \"http://localhost:8000\";",
        "source": "const API_BASE = process.env.NEXT_PUBLIC_API_BASE ?? \"http://localhost:8000\";\n\nexport interface AxisDef {\n  name: string;\n  type: string;\n  values?: string[];\n  required?: boolean;\n}\n\nexport interface AxesResponse {\n  axes: AxisDef[];\n}\n\nexport interface SearchResultPayload {\n  id: string;\n  title: string;\n  score: number;\n  axes: Record<string, string | number>;\n  body_snippet: string;\n  path: string;\n  refs: string[];\n}\n\nexport interface SearchResponse {",
        "start_line": 3,
        "end_line": 26
      },
      "answer": {
        "accepted": [
          "\"http://localhost:8000\"",
          "http://localhost:8000"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "未設定時はhttp://localhost:8000(ローカルFastAPI)。"
    },
    {
      "id": "q_design_001",
      "difficulty": "L4",
      "type": "mcq",
      "module": "docs/adr/ADR-017-parent-document-retrieval.md",
      "tags": [
        "design",
        "parent-doc"
      ],
      "prompt": "Parent Document Retrievalで『parentもchildも両方embed』案(Hierarchical Embeddings)が却下された理由は?(ADR-017)",
      "citation": {
        "file": "docs/adr/ADR-017-parent-document-retrieval.md",
        "symbol": {
          "kind": "section",
          "name": "Alternatives"
        },
        "evidence_substring": "ストレージコスト 2 倍",
        "adr_ref": "ADR-017",
        "snippet": "(c) Hierarchical Embeddings 却下: ストレージコスト 2 倍 + 埋め込みAPIコスト2倍。検索フローが複雑化。"
      },
      "answer": {
        "options": [
          "精度が下がる",
          "ストレージと埋め込みAPIが2倍になり検索フローも複雑化",
          "Chroma非対応",
          "日本語不可"
        ],
        "correct_index": 1
      },
      "explanation": "childのみembed、parentはsidecar。両方embedはコスト2倍・複雑化で不利。"
    },
    {
      "id": "q_design_002",
      "difficulty": "L4",
      "type": "freetext",
      "module": "README.md",
      "tags": [
        "design",
        "philosophy"
      ],
      "prompt": "axisがLangChain/LlamaIndexを使わず自前実装している利点を、READMEの主張に沿って説明せよ。",
      "citation": {
        "file": "README.md",
        "symbol": {
          "kind": "section",
          "name": "特徴"
        },
        "evidence_substring": "依存が薄く、内部挙動が読める",
        "adr_ref": null,
        "snippet": "LangChain / LlamaIndex 不使用、自前実装 — 依存が薄く、内部挙動が読める。薄いラッパで構成。"
      },
      "answer": {
        "model_answer": "依存が薄く内部挙動を読んで理解・デバッグできる。薄いラッパで学習・保守しやすい。",
        "required_keywords": [
          {
            "any_of": [
              "依存",
              "ライブラリ",
              "dependency"
            ]
          },
          {
            "any_of": [
              "読める",
              "理解",
              "挙動",
              "追える",
              "把握",
              "デバッグ",
              "透明"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.5
      },
      "explanation": "薄い依存=内部が読める=学習・デバッグ・改造しやすい。axisのコア哲学。"
    },
    {
      "id": "q_rewrite_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/question_rewriter.py",
      "tags": [
        "conversational-rag"
      ],
      "prompt": "follow-up質問をstandaloneクエリに書き換えた後、その書き換え後の文は何に使われる？",
      "citation": {
        "file": "backend/src/question_rewriter.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "used as the retrieval query",
        "adr_ref": "ADR-018",
        "snippet": "The standalone form is used as the retrieval query; the original question is still used for answer generation."
      },
      "answer": {
        "options": [
          "生成にも検索にも書き換え後",
          "検索に使い、生成には元の質問",
          "ログだけ",
          "表示だけ"
        ],
        "correct_index": 1
      },
      "explanation": "standaloneは検索用。生成には元質問を渡しユーザーの言い回しを保つ。"
    },
    {
      "id": "q_rewrite_002",
      "difficulty": "L4",
      "type": "mcq",
      "module": "backend/src/question_rewriter.py",
      "tags": [
        "conversational-rag",
        "robustness"
      ],
      "prompt": "rewrite_questionがAPIキー無し・ネットワークエラー等で失敗したときの挙動は？",
      "citation": {
        "file": "backend/src/question_rewriter.py",
        "symbol": {
          "kind": "function",
          "name": "rewrite_question"
        },
        "evidence_substring": "fall back to the original question",
        "adr_ref": "ADR-018",
        "snippet": "All failures (no API key, network error, quota, oversized output) fall back to the original question so chat UX never blocks.",
        "source": "def rewrite_question(\n    question: str,\n    history: list[Message],\n    *,\n    model_name: str = DEFAULT_MODEL,\n    enabled: bool = True,\n    backend: str = \"gemini\",\n    ollama_url: str = \"http://localhost:11434\",\n) -> str:\n    \"\"\"Rewrite ``question`` into a standalone query using chat history.\n\n    spec_045 added ``backend`` so the rewriter can run fully on-prem via\n    Ollama. ``backend=\"gemini\"`` (default) keeps v0.8.1 behaviour exactly.\n\n    Returns the original ``question`` unchanged when:\n    - ``enabled=False`` / ``history`` is empty / ``question`` is blank\n    - the chosen backend isn't configured (no GEMINI_API_KEY for gemini,\n      no ``ollama`` package / unreachable server for ollama)\n    - the model call raises\n    - the response is empty or >500 chars (likely a hallucination)\n    \"\"\"\n    if not enabled or not history or not question.strip():\n        return question\n    prompt = REWRITE_PROMPT.format(\n        history=_format_history(history),\n        question=question,\n    )\n    backend_l = (backend or \"gemini\").lower()\n    try:\n        if backend_l == \"ollama\":\n            rewritten = _rewrite_with_ollama(prompt, model_name, ollama_url)\n        else:\n            if not settings.gemini_api_key:\n                return question\n            rewritten = _rewrite_with_gemini(prompt, model_name)\n    except Exception as e:  # noqa: BLE001 — fall back on any failure\n        logger.warning(\"question rewriter failed, using original query: %s\", e)\n        return question\n\n    if not rewritten or len(rewritten) > MAX_REWRITE_LEN:\n        return question\n    rewritten = _clean_rewritten(rewritten)\n    return rewritten or question",
        "start_line": 84,
        "end_line": 126
      },
      "answer": {
        "options": [
          "例外でチャット停止",
          "元の質問にフォールバックしUXを止めない",
          "空文字で検索",
          "前回回答を再利用"
        ],
        "correct_index": 1
      },
      "explanation": "書き換えは最適化。失敗時は元質問で検索しUXをブロックしない。"
    },
    {
      "id": "q_cite_001",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/_citations.py",
      "tags": [
        "citation"
      ],
      "prompt": "RAG出力の[N]引用マーカーをパース・検証する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/_citations.py",
        "symbol": {
          "kind": "function",
          "name": "parse_and_validate_citations"
        },
        "evidence_substring": "def parse_and_validate_citations",
        "adr_ref": null,
        "snippet": "def parse_and_validate_citations(text, n_sources, ...):\n    # normalize [1,2]->[1][2], strip out-of-range, skip code spans",
        "source": "def parse_and_validate_citations(\n    text: str, *, n_sources: int\n) -> tuple[str, set[int]]:\n    \"\"\"Return (cleaned_text, set_of_used_source_indices_0based).\n\n    * Strips markers whose N is outside ``1..n_sources`` (logs a warning\n      so we can monitor LLM compliance over time).\n    * Canonicalises ``[1, 2]`` to ``[1][2]`` so the frontend parser only\n      has one shape to deal with.\n    * If every N in a marker is invalid, the marker is removed entirely.\n    * Markers that fall inside Markdown code fences / inline code are\n      preserved verbatim (treated as literal text, not citations).\n    \"\"\"\n    used: set[int] = set()\n    skip_ranges = _build_skip_ranges(text)\n\n    def _replace(m: re.Match[str]) -> str:\n        if _is_in_skip_range(m.start(), skip_ranges):\n            return m.group(0)\n        nums_str = m.group(1)\n        nums = [int(x.strip()) for x in nums_str.split(\",\")]\n        valid = [n for n in nums if 1 <= n <= n_sources]\n        invalid = [n for n in nums if n not in valid]\n        if invalid:\n            _log.warning(\n                \"citation out of range: %s (n_sources=%d)\", invalid, n_sources\n            )\n        if not valid:\n            return \"\"\n        used.update(n - 1 for n in valid)\n        return \"\".join(f\"[{n}]\" for n in valid)\n\n    cleaned = _RE_MARKER.sub(_replace, text)\n    return cleaned, used",
        "start_line": 53,
        "end_line": 86
      },
      "answer": {
        "file": "backend/src/_citations.py",
        "symbol": "parse_and_validate_citations"
      },
      "grading": {
        "symbol_aliases": [
          "parse_and_validate_citations",
          "extract_citations"
        ],
        "accept_file_only_partial": 0.6
      },
      "explanation": "マーカー正規化・範囲外除去・コード領域スキップ。フロントcitations.tsがミラー。"
    },
    {
      "id": "q_cite_002",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/_citations.py",
      "tags": [
        "citation",
        "robustness"
      ],
      "prompt": "LLMが出典数を超える範囲外のN(例:出典3件なのに[5])を出した場合、_citationsはどうする？",
      "citation": {
        "file": "backend/src/_citations.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "strips markers that reference an N out of range",
        "adr_ref": null,
        "snippet": "strips markers that reference an N out of range (the LLM occasionally hallucinates indices beyond the source count)"
      },
      "answer": {
        "options": [
          "そのまま表示",
          "範囲外マーカーを除去しユーザーに漏らさない",
          "エラー",
          "出典を自動増加"
        ],
        "correct_index": 1
      },
      "explanation": "ハルシネートされた範囲外indexは見せず、範囲内だけ残す。"
    },
    {
      "id": "q_loader_001",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/loader.py",
      "tags": [
        "loader"
      ],
      "prompt": "YAML frontmatter付きMarkdown1ファイルを読み込みDocumentに変換する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/loader.py",
        "symbol": {
          "kind": "function",
          "name": "load_document"
        },
        "evidence_substring": "def load_document",
        "adr_ref": null,
        "snippet": "def load_document(path, normalizer=None) -> Document:\n    # parse YAML frontmatter + body via `frontmatter`",
        "source": "def load_document(path: Path, normalizer: Normalizer | None = None) -> Document:\n    \"\"\"Load a single Markdown file with YAML frontmatter.\n\n    If a Normalizer is provided, the `normalized_*` fields are populated.\n    Otherwise they remain empty (Day 1 behavior, kept for back-compat).\n    \"\"\"\n    if not path.exists():\n        raise LoaderError(f\"File not found: {path}\")\n    if not path.is_file():\n        raise LoaderError(f\"Not a file: {path}\")\n\n    try:\n        post = frontmatter.load(path)\n    except Exception as e:\n        raise LoaderError(f\"Failed to parse frontmatter for {path}: {e}\") from e\n\n    meta = post.metadata\n    if \"id\" not in meta:\n        raise LoaderError(f\"Missing required 'id' field in {path}\")\n    if \"title\" not in meta:\n        raise LoaderError(f\"Missing required 'title' field in {path}\")\n\n    doc = Document(\n        id=str(meta[\"id\"]),\n        title=str(meta[\"title\"]),\n        axes=dict(meta.get(\"axes\", {})),\n        tags=list(meta.get(\"tags\", [])),\n        refs=list(meta.get(\"refs\", [])),\n        body=post.content,\n        path=path,\n        raw_meta=dict(meta),\n    )\n    if normalizer is not None:\n        doc.normalized_title = normalizer(doc.title)\n        doc.normalized_body = normalizer(doc.body)\n        doc.normalized_axes = {k: normalizer(str(v)) for k, v in doc.axes.items()}\n        doc.normalized_tags = [normalizer(t) for t in doc.tags]\n    return doc",
        "start_line": 57,
        "end_line": 94
      },
      "answer": {
        "file": "backend/src/loader.py",
        "symbol": "load_document"
      },
      "grading": {
        "symbol_aliases": [
          "load_document",
          "load_directory"
        ],
        "accept_file_only_partial": 0.6
      },
      "explanation": "load_documentが単一、load_directoryが一括。Day1の主成果物。"
    },
    {
      "id": "q_integ_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/integrity.py",
      "tags": [
        "integrity"
      ],
      "prompt": "integrity.pyの主な役割は？",
      "citation": {
        "file": "backend/src/integrity.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "points to an existing",
        "adr_ref": null,
        "snippet": "Reference integrity checker. Validates that every refs entry points to an existing document id, surfaces orphans / cycles."
      },
      "answer": {
        "options": [
          "ベクトル次元検証",
          "各refsの参照先実在を検証しorphan/cycleを報告",
          "APIキー検証",
          "Markdown文法検証"
        ],
        "correct_index": 1
      },
      "explanation": "refsの壊れた参照・孤立・循環を検出。参照健全性チェック専用。"
    },
    {
      "id": "q_conv_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/conversation.py",
      "tags": [
        "conversational-rag",
        "storage"
      ],
      "prompt": "ConversationStore Protocolの3実装は？",
      "citation": {
        "file": "backend/src/conversation.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "RedisStore",
        "adr_ref": "ADR-022",
        "snippet": "Three implementations: MemoryStore (in-memory), SqliteStore (file-backed, default v0.8+), RedisStore (optional, multi-host)."
      },
      "answer": {
        "options": [
          "Memory/File/S3",
          "Memory/Sqlite/Redis",
          "Sqlite/Postgres/Redis",
          "Memory/Chroma/Redis"
        ],
        "correct_index": 1
      },
      "explanation": "Memory/Sqlite/Redis。make_conversation_storeがbackendで選択、失敗時Memoryへ。"
    },
    {
      "id": "q_conv_002",
      "difficulty": "L4",
      "type": "mcq",
      "module": "backend/src/conversation.py",
      "tags": [
        "conversational-rag",
        "design"
      ],
      "prompt": "v0.8+で会話履歴のデフォルトbackendをSqliteStoreにした理由は？",
      "citation": {
        "file": "backend/src/conversation.py",
        "symbol": {
          "kind": "class",
          "name": "SqliteStore"
        },
        "evidence_substring": "Survives process restarts",
        "adr_ref": "ADR-022",
        "snippet": "SqliteStore — file-backed sqlite3 (WAL mode), default for v0.8+. Survives process restarts and is safe across uvicorn workers.",
        "source": "class SqliteStore:\n    \"\"\"File-backed session store using stdlib ``sqlite3``.\n\n    Schema:\n        sessions(session_id TEXT PK, last_access REAL)\n        messages(id INTEGER PK AUTOINCREMENT, session_id TEXT FK,\n                 role TEXT, content TEXT, sources_json TEXT, timestamp REAL)\n        INDEX: messages(session_id, id)\n\n    - WAL journal mode enables concurrent reads while a writer holds the lock\n    - FOREIGN KEYS ensures DELETE FROM sessions also drops messages\n    - ``check_same_thread=False`` because FastAPI's threadpool dispatches\n      requests across threads; we guard mutations with our own ``Lock``.\n    \"\"\"\n\n    SCHEMA = \"\"\"\n    CREATE TABLE IF NOT EXISTS sessions (\n        session_id TEXT PRIMARY KEY,\n        last_access REAL NOT NULL\n    );\n    -- spec_042 MID #3: TTL eviction + LRU pick both scan by last_access;\n    -- without this index they become O(N) over the whole sessions table.\n    CREATE INDEX IF NOT EXISTS idx_sessions_last_access ON sessions(last_access);\n    CREATE TABLE IF NOT EXISTS messages (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        session_id TEXT NOT NULL,\n        role TEXT NOT NULL,\n        content TEXT NOT NULL,\n        sources_json TEXT,\n        timestamp REAL NOT NULL,\n        FOREIGN KEY (session_id) REFERENCES sessions(session_id) ON DELETE CASCADE\n    );\n    CREATE INDEX IF NOT EXISTS idx_messages_session ON messages(session_id, id);\n    \"\"\"\n\n    def __init__(\n        self,\n        db_path: str = \"~/.axis_chat.db\",\n        *,\n        max_sessions: int = 1000,\n        ttl_seconds: int = 86400,\n    ) -> None:\n        self._db_path = os.path.expanduser(db_path)\n        # Ensure parent dir exists (so e.g. data/chat.db works on a fresh checkout).\n        parent = os.path.dirname(self._db_path)\n        if parent and not os.path.isdir(parent):\n            os.makedirs(parent, exist_ok=True)\n        self._max = max_sessions\n        self._ttl = ttl_seconds\n        self._conn: sqlite3.Connection | None = sqlite3.connect(\n            self._db_path, check_same_thread=False\n        )\n        self._conn.executescript(self.SCHEMA)\n        # WAL for concurrent reads; FK pragma is per-connection.\n        self._conn.execute(\"PRAGMA journal_mode=WAL\")\n        self._conn.execute(\"PRAGMA foreign_keys=ON\")\n        self._conn.commit()\n        self._lock = Lock()\n        # Purge any sessions already past TTL when we re-open the DB.\n        self._evict_expired_locked()\n\n    # ----- public API ----------------------------------------------------\n\n    def get_or_create(self, session_id: str | None = None) -> Session:\n        with self._lock:\n            self._evict_expired_locked()\n            now = _utcnow()\n            conn = self._require_conn()\n            if session_id:\n                row = conn.execute(\n                    \"SELECT last_access FROM sessions WHERE session_id = ?\",\n                    (session_id,),\n                ).fetchone()\n                if row is not None:\n                    conn.execute(\n                        \"UPDATE sessions SET last_access = ? WHERE session_id = ?\",\n                        (now.timestamp(), session_id),\n                    )\n                    conn.commit()\n                    return self._load_session_locked(session_id, now)\n            new_id = session_id or str(uuid.uuid4())\n            conn.execute(\n                \"INSERT INTO sessions (session_id, last_access) VALUES (?, ?)\",\n                (new_id, now.timestamp()),\n            )\n            conn.commit()\n            self._evict_lru_locked()\n            return Session(session_id=new_id, last_access=now)\n\n    def append(self, session_id: str, msg: Message) -> None:\n        with self._lock:\n            conn = self._require_conn()\n            now = _utcnow()\n            # \"append on unknown id\" → silently create the parent row so the\n            # FK doesn't blow up. Mirrors MemoryStore behaviour.\n            row = conn.execute(\n                \"SELECT session_id FROM sessions WHERE session_id = ?\",\n                (session_id,),\n            ).fetchone()\n            if row is None:\n                conn.execute(\n                    \"INSERT INTO sessions (session_id, last_access) VALUES (?, ?)\",\n                    (session_id, now.timestamp()),\n                )\n                self._evict_lru_locked()\n            conn.execute(\n                \"INSERT INTO messages \"\n                \"(session_id, role, content, sources_json, timestamp) \"\n                \"VALUES (?, ?, ?, ?, ?)\",\n                (\n                    session_id,\n                    msg.role,\n                    msg.content,\n                    json.dumps(msg.sources, ensure_ascii=False),\n                    msg.timestamp.timestamp(),\n                ),\n            )\n            conn.execute(\n                \"UPDATE sessions SET last_access = ? WHERE session_id = ?\",\n                (now.timestamp(), session_id),\n            )\n            conn.commit()\n\n    def get_history(\n        self, session_id: str, *, last_n_turns: int = 6\n    ) -> list[Message]:\n        with self._lock:\n            conn = self._require_conn()\n            # Confirm the session exists — unknown id returns [] like MemoryStore.\n            row = conn.execute(\n                \"SELECT 1 FROM sessions WHERE session_id = ?\",\n                (session_id,),\n            ).fetchone()\n            if row is None:\n                return []\n            limit = max(0, last_n_turns) * 2\n            if limit == 0:\n                return []\n            rows = conn.execute(\n                \"SELECT role, content, sources_json, timestamp FROM messages \"\n                \"WHERE session_id = ? ORDER BY id DESC LIMIT ?\",\n                (session_id, limit),\n            ).fetchall()\n        # ORDER BY DESC LIMIT N gives the newest N rows; reverse to chronological.\n        return [self._row_to_msg(r) for r in reversed(rows)]\n\n    def delete(self, session_id: str) -> bool:\n        with self._lock:\n            conn = self._require_conn()\n            cur = conn.execute(\n                \"DELETE FROM sessions WHERE session_id = ?\", (session_id,)\n            )\n            conn.commit()\n            return cur.rowcount > 0\n\n    def has(self, session_id: str) -> bool:\n        with self._lock:\n            conn = self._require_conn()\n            row = conn.execute(\n                \"SELECT 1 FROM sessions WHERE session_id = ?\", (session_id,)\n            ).fetchone()\n            return row is not None\n\n    def __len__(self) -> int:\n        with self._lock:\n            conn = self._require_conn()\n            return int(conn.execute(\"SELECT COUNT(*) FROM sessions\").fetchone()[0])\n\n    def close(self) -> None:\n        with self._lock:\n            if self._conn is not None:\n                try:\n                    self._conn.close()\n                finally:\n                    self._conn = None\n\n    # ----- internals -----------------------------------------------------\n\n    def _require_conn(self) -> sqlite3.Connection:\n        if self._conn is None:\n            raise RuntimeError(\"SqliteStore connection is already closed\")\n        return self._conn\n\n    def _load_session_locked(self, session_id: str, last_access: datetime) -> Session:\n        conn = self._require_conn()\n        rows = conn.execute(\n            \"SELECT role, content, sources_json, timestamp FROM messages \"\n            \"WHERE session_id = ? ORDER BY id ASC\",\n            (session_id,),\n        ).fetchall()\n        msgs = [self._row_to_msg(r) for r in rows]\n        return Session(session_id=session_id, messages=msgs, last_access=last_access)\n\n    @staticmethod\n    def _row_to_msg(row: tuple[Any, ...]) -> Message:\n        role, content, sources_json, ts = row\n        sources = json.loads(sources_json) if sources_json else []\n        return Message(\n            role=role,\n            content=content,\n            sources=sources,\n            timestamp=datetime.fromtimestamp(ts, tz=UTC),\n        )\n\n    def _evict_expired_locked(self) -> None:\n        conn = self._require_conn()\n        cutoff = (_utcnow() - timedelta(seconds=self._ttl)).timestamp()\n        conn.execute(\"DELETE FROM sessions WHERE last_access < ?\", (cutoff,))\n        conn.commit()\n\n    def _evict_lru_locked(self) -> None:\n        conn = self._require_conn()\n        count = int(conn.execute(\"SELECT COUNT(*) FROM sessions\").fetchone()[0])\n        excess = count - self._max\n        if excess <= 0:\n            return\n        conn.execute(\n            \"DELETE FROM sessions WHERE session_id IN \"\n            \"(SELECT session_id FROM sessions ORDER BY last_access ASC LIMIT ?)\",\n            (excess,),\n        )\n        conn.commit()",
        "start_line": 177,
        "end_line": 398
      },
      "answer": {
        "options": [
          "メモリより高速",
          "再起動後も履歴が残り複数uvicorn worker間で安全",
          "Redis廃止",
          "暗号化"
        ],
        "correct_index": 1
      },
      "explanation": "WALのsqlite3で永続化＆マルチワーカー安全。Memoryは揮発・単一プロセス向け。"
    },
    {
      "id": "q_fb_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/feedback.py",
      "tags": [
        "feedback",
        "active-learning"
      ],
      "prompt": "feedback.py が記録しているものは？",
      "citation": {
        "file": "backend/src/feedback.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "Tracks 👍/👎 signals",
        "adr_ref": "ADR-028",
        "snippet": "User feedback storage for active learning (spec_047). Tracks 👍/👎 signals on search results / chat answers, plus the originating query, doc_id, session_id, and timestamp."
      },
      "answer": {
        "options": [
          "検索のレイテンシ",
          "回答/検索結果への 👍/👎 と元クエリ・doc_id・session_id・時刻",
          "LLMのトークン使用量",
          "ユーザーのログイン履歴"
        ],
        "correct_index": 1
      },
      "explanation": "active learning 用に 👍/👎 シグナルと文脈（query/doc_id/session_id/timestamp）を記録。集計は evaluation/feedback_report.py。"
    },
    {
      "id": "q_fb_002",
      "difficulty": "L4",
      "type": "mcq",
      "module": "backend/src/feedback.py",
      "tags": [
        "feedback",
        "design"
      ],
      "prompt": "FeedbackStore を Protocol にしている設計意図は？",
      "citation": {
        "file": "backend/src/feedback.py",
        "symbol": {
          "kind": "class",
          "name": "FeedbackStore"
        },
        "evidence_substring": "without breaking call sites",
        "adr_ref": "ADR-028",
        "snippet": "Keeping the store behind a small FeedbackStore Protocol leaves room for a PostgreSQL / Redis backend later without breaking call sites.",
        "source": "class FeedbackStore(Protocol):\n    def record(\n        self,\n        *,\n        query: str | None,\n        doc_id: str | None,\n        rating: int,\n        session_id: str | None = None,\n        note: str | None = None,\n    ) -> str: ...\n\n    def list_recent(\n        self, *, days: int = 7, limit: int = 1000\n    ) -> list[FeedbackRecord]: ...\n\n    def count(self) -> int: ...\n\n    def close(self) -> None: ...",
        "start_line": 37,
        "end_line": 54
      },
      "answer": {
        "options": [
          "テストを速くするため",
          "後で PostgreSQL/Redis バックエンドに差し替えても呼び出し側を壊さないため",
          "型チェックを無効化するため",
          "sqlite を禁止するため"
        ],
        "correct_index": 1
      },
      "explanation": "Protocol で実装を抽象化＝将来のバックエンド差し替えに対して呼び出し側非破壊。MVPは logging のみ（自動重み調整は v0.10）。"
    },
    {
      "id": "q_fb_003",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/feedback.py",
      "tags": [
        "feedback"
      ],
      "prompt": "フィードバックを sqlite に永続化する実装クラスはどれ？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/feedback.py",
        "symbol": {
          "kind": "class",
          "name": "SqliteFeedbackStore"
        },
        "evidence_substring": "class SqliteFeedbackStore",
        "adr_ref": null,
        "snippet": "class SqliteFeedbackStore:\n    def __init__(self, db_path: str = \"~/.axis_feedback.db\") -> None: ...",
        "source": "class SqliteFeedbackStore:\n    \"\"\"File-backed feedback store using stdlib ``sqlite3`` (no new deps).\n\n    The default path is ``~/.axis_feedback.db`` so the file lives next to\n    the existing ``~/.axis_chat.db`` from spec_036. A single shared\n    connection is reused under a ``threading.Lock`` because FastAPI may\n    call ``record`` from multiple worker threads.\n    \"\"\"\n\n    SCHEMA = \"\"\"\n    CREATE TABLE IF NOT EXISTS feedback (\n        feedback_id TEXT PRIMARY KEY,\n        query TEXT,\n        doc_id TEXT,\n        rating INTEGER NOT NULL,\n        session_id TEXT,\n        note TEXT,\n        timestamp REAL NOT NULL\n    );\n    CREATE INDEX IF NOT EXISTS idx_feedback_ts ON feedback(timestamp);\n    CREATE INDEX IF NOT EXISTS idx_feedback_doc ON feedback(doc_id);\n    \"\"\"\n\n    def __init__(self, db_path: str = \"~/.axis_feedback.db\") -> None:\n        self._db_path = Path(os.path.expanduser(db_path))\n        self._db_path.parent.mkdir(parents=True, exist_ok=True)\n        self._conn: sqlite3.Connection | None = sqlite3.connect(\n            str(self._db_path), check_same_thread=False\n        )\n        self._conn.executescript(self.SCHEMA)\n        self._conn.execute(\"PRAGMA journal_mode=WAL\")\n        self._conn.commit()\n        self._lock = threading.Lock()\n\n    def record(\n        self,\n        *,\n        query: str | None = None,\n        doc_id: str | None = None,\n        rating: int,\n        session_id: str | None = None,\n        note: str | None = None,\n    ) -> str:\n        if self._conn is None:\n            raise RuntimeError(\"feedback store has been closed\")\n        fid = str(uuid.uuid4())\n        ts = datetime.now(UTC).timestamp()\n        with self._lock:\n            self._conn.execute(\n                \"INSERT INTO feedback \"\n                \"(feedback_id, query, doc_id, rating, session_id, note, timestamp) \"\n                \"VALUES (?, ?, ?, ?, ?, ?, ?)\",\n                (fid, query, doc_id, int(rating), session_id, note, ts),\n            )\n            self._conn.commit()\n        return fid\n\n    def list_recent(\n        self, *, days: int = 7, limit: int = 1000\n    ) -> list[FeedbackRecord]:\n        if self._conn is None:\n            raise RuntimeError(\"feedback store has been closed\")\n        cutoff = datetime.now(UTC).timestamp() - days * 86400\n        rows = self._conn.execute(\n            \"SELECT feedback_id, query, doc_id, rating, session_id, note, timestamp \"\n            \"FROM feedback WHERE timestamp >= ? ORDER BY timestamp DESC LIMIT ?\",\n            (cutoff, limit),\n        ).fetchall()\n        return [\n            FeedbackRecord(\n                feedback_id=r[0],\n                query=r[1],\n                doc_id=r[2],\n                rating=int(r[3]),\n                session_id=r[4],\n                note=r[5],\n                timestamp=datetime.fromtimestamp(r[6], tz=UTC),\n            )\n            for r in rows\n        ]\n\n    def count(self) -> int:\n        if self._conn is None:\n            return 0\n        return int(\n            self._conn.execute(\"SELECT COUNT(*) FROM feedback\").fetchone()[0]\n        )\n\n    def close(self) -> None:\n        if self._conn is not None:\n            self._conn.close()\n            self._conn = None",
        "start_line": 57,
        "end_line": 148
      },
      "answer": {
        "file": "backend/src/feedback.py",
        "symbol": "SqliteFeedbackStore"
      },
      "grading": {
        "symbol_aliases": [
          "SqliteFeedbackStore"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "SqliteFeedbackStore が FeedbackStore Protocol の sqlite 実装。make_feedback_store がconfigで生成。"
    },
    {
      "id": "q_gap_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/gap_detection.py",
      "tags": [
        "gap-detection"
      ],
      "prompt": "gap_detection が記録するナレッジギャップの3種類は？",
      "citation": {
        "file": "backend/src/gap_detection.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "llm_no_info",
        "adr_ref": "ADR-029",
        "snippet": "no_results — search returned an empty hit list.\nlow_score — top hit fell below gap.low_score_threshold.\nllm_no_info — the LLM answered 資料に記載がありません or similar."
      },
      "answer": {
        "options": [
          "error / warning / info",
          "no_results / low_score / llm_no_info",
          "timeout / empty / slow",
          "404 / 500 / 503"
        ],
        "correct_index": 1
      },
      "explanation": "無結果 / 上位スコア低 / LLMが情報なしと回答、の3イベントを記録。集計は evaluation/gap_report.py。"
    },
    {
      "id": "q_gap_002",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/gap_detection.py",
      "tags": [
        "gap-detection"
      ],
      "prompt": "回答文が「資料に記載がありません」等の情報なし応答かを判定する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/gap_detection.py",
        "symbol": {
          "kind": "function",
          "name": "detect_no_info"
        },
        "evidence_substring": "def detect_no_info",
        "adr_ref": null,
        "snippet": "def detect_no_info(answer_text: str | None) -> bool:\n    # True if the LLM said 資料に記載がありません or similar",
        "source": "def detect_no_info(answer_text: str | None) -> bool:\n    \"\"\"Return ``True`` when the LLM seems to have answered \"I don't know\".\n\n    Empty / whitespace-only answers are treated as a gap as well — an\n    empty model response is equivalent to \"no information\" for the\n    purpose of populating the knowledge-gap report.\n    \"\"\"\n    if not answer_text or not answer_text.strip():\n        return True\n    return bool(_NO_INFO_RE.search(answer_text))",
        "start_line": 192,
        "end_line": 201
      },
      "answer": {
        "file": "backend/src/gap_detection.py",
        "symbol": "detect_no_info"
      },
      "grading": {
        "symbol_aliases": [
          "detect_no_info"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "detect_no_info が llm_no_info ギャップの判定に使われる。"
    },
    {
      "id": "q_marker_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/marker.py",
      "tags": [
        "marker"
      ],
      "prompt": "AUTO_GENERATED マーカーブロックの役割は？",
      "citation": {
        "file": "backend/src/marker.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "preserving everything else",
        "adr_ref": null,
        "snippet": "Allows human-written Markdown and AI-generated sections to coexist. Regenerating a block overwrites only the content between its delimiters, preserving everything else (= the human-written part)."
      },
      "answer": {
        "options": [
          "全文をAIで上書きする",
          "人間が書いた部分とAI生成部分を共存させ、再生成は区切り間だけ上書きする",
          "Markdownを禁止する",
          "コメントを全削除する"
        ],
        "correct_index": 1
      },
      "explanation": "人手とAI生成を1ファイルで共存。区切り（START/END）間だけ差し替え、人間の記述は保全。"
    },
    {
      "id": "q_marker_002",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/marker.py",
      "tags": [
        "marker"
      ],
      "prompt": "指定ブロックの中身だけを上書きする関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/marker.py",
        "symbol": {
          "kind": "function",
          "name": "update_block"
        },
        "evidence_substring": "def update_block",
        "adr_ref": null,
        "snippet": "def update_block(text: str, name: str, new_content: str) -> str:\n    # overwrite only the content between this block's delimiters",
        "source": "def update_block(text: str, name: str, new_content: str) -> str:\n    \"\"\"Replace an existing block's content. If absent, append at the end.\"\"\"\n    if not re.match(rf\"^{NAME_PATTERN}$\", name):\n        raise MarkerError(f\"Invalid marker name: {name!r}\")\n\n    new_content = new_content.rstrip(\"\\n\")\n    replacement = (\n        f\"<!-- AUTO_GENERATED_START: {name} -->\\n\"\n        f\"{new_content}\\n\"\n        f\"<!-- AUTO_GENERATED_END: {name} -->\"\n    )\n\n    block_re = re.compile(\n        r\"<!--\\s*AUTO_GENERATED_START:\\s*\" + re.escape(name) + r\"\\s*-->\\n\"\n        r\".*?\"\n        r\"\\n<!--\\s*AUTO_GENERATED_END:\\s*\" + re.escape(name) + r\"\\s*-->\",\n        re.DOTALL,\n    )\n    if block_re.search(text):\n        return block_re.sub(replacement, text, count=1)\n\n    # Append at end (with leading newline if needed)\n    sep = \"\" if text.endswith(\"\\n\\n\") else (\"\\n\" if text.endswith(\"\\n\") else \"\\n\\n\")\n    return text + sep + replacement + \"\\n\"",
        "start_line": 71,
        "end_line": 94
      },
      "answer": {
        "file": "backend/src/marker.py",
        "symbol": "update_block"
      },
      "grading": {
        "symbol_aliases": [
          "update_block",
          "extract_blocks"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "update_block が区切り間のみ置換。extract_blocks/validate_balance/strip_blocks も同モジュール。"
    },
    {
      "id": "q_marker_003",
      "difficulty": "L2",
      "type": "fill_blank",
      "module": "backend/src/marker.py",
      "tags": [
        "marker"
      ],
      "prompt": "ブロック開始の区切りコメントの形式の空欄を埋めよ:\n    <!-- ____: <name> -->",
      "citation": {
        "file": "backend/src/marker.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "AUTO_GENERATED_START",
        "adr_ref": null,
        "snippet": "Blocks are delimited by:\n    <!-- AUTO_GENERATED_START: <name> -->\n    ...\n    <!-- AUTO_GENERATED_END: <name> -->"
      },
      "answer": {
        "accepted": [
          "AUTO_GENERATED_START"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "開始 AUTO_GENERATED_START / 終了 AUTO_GENERATED_END で name 付きブロックを画定。"
    },
    {
      "id": "q_ps_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/parent_storage.py",
      "tags": [
        "parent-doc",
        "storage"
      ],
      "prompt": "ParentStorage の2実装は？",
      "citation": {
        "file": "backend/src/parent_storage.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "JsonParentStorage",
        "adr_ref": "ADR-023",
        "snippet": "Two implementations:\n- SqliteParentStorage (v0.8 default): file-backed sqlite, lazy SELECT per parent_id\n- JsonParentStorage (v0.7 fallback): full-JSON load at init."
      },
      "answer": {
        "options": [
          "Redis / Memcached",
          "SqliteParentStorage（既定） / JsonParentStorage（後方互換）",
          "Chroma / FAISS",
          "Postgres / MySQL"
        ],
        "correct_index": 1
      },
      "explanation": "v0.8 既定は sqlite（parent_id 単位の遅延SELECT）、v0.7 互換の JSON 全ロードも残す。"
    },
    {
      "id": "q_ps_002",
      "difficulty": "L4",
      "type": "mcq",
      "module": "backend/src/parent_storage.py",
      "tags": [
        "parent-doc",
        "design"
      ],
      "prompt": "parent 保存を JSON 全ロードから sqlite の遅延SELECTへ移行した利点は？",
      "citation": {
        "file": "backend/src/parent_storage.py",
        "symbol": {
          "kind": "class",
          "name": "SqliteParentStorage"
        },
        "evidence_substring": "lazy SELECT per parent_id",
        "adr_ref": "ADR-023",
        "snippet": "SqliteParentStorage (v0.8 default): file-backed sqlite, lazy SELECT per parent_id. JsonParentStorage (v0.7 fallback): full-JSON load at init.",
        "source": "class SqliteParentStorage:\n    \"\"\"SQLite-backed parent storage with lazy SELECT per parent_id.\n\n    Schema:\n        parents(parent_id TEXT PK, doc_id TEXT, title TEXT, text TEXT, metadata_json TEXT)\n        INDEX idx_parents_doc_id ON parents(doc_id)\n    \"\"\"\n\n    SCHEMA = \"\"\"\n    CREATE TABLE IF NOT EXISTS parents (\n        parent_id TEXT PRIMARY KEY,\n        doc_id TEXT NOT NULL,\n        title TEXT NOT NULL,\n        text TEXT NOT NULL,\n        metadata_json TEXT\n    );\n    CREATE INDEX IF NOT EXISTS idx_parents_doc_id ON parents(doc_id);\n    \"\"\"\n\n    # SQLite's default SQLITE_LIMIT_VARIABLE_NUMBER is 999 on builds <3.32 and\n    # 32766 on newer builds — but we target the conservative limit so 1000+ doc\n    # corpora work everywhere (spec_042 MID #2).\n    SQLITE_VARIABLE_LIMIT = 999\n\n    def __init__(self, db_path: str | Path) -> None:\n        self._db_path = Path(db_path) if str(db_path) != \":memory:\" else Path(\":memory:\")\n        if str(db_path) != \":memory:\":\n            self._db_path = Path(db_path)\n            self._db_path.parent.mkdir(parents=True, exist_ok=True)\n        self._conn = sqlite3.connect(str(db_path), check_same_thread=False)\n        self._conn.executescript(self.SCHEMA)\n        self._conn.execute(\"PRAGMA journal_mode=WAL\")\n        self._conn.commit()\n\n    def get(self, parent_id: str) -> ParentChunk | None:\n        row = self._conn.execute(\n            \"SELECT parent_id, doc_id, title, text, metadata_json FROM parents WHERE parent_id = ?\",\n            (parent_id,),\n        ).fetchone()\n        return self._row_to_parent(row) if row else None\n\n    def get_many(self, parent_ids: list[str]) -> list[ParentChunk]:\n        \"\"\"Fetch multiple parents in one or more queries.\n\n        Chunked at ``SQLITE_VARIABLE_LIMIT`` (999) so 1000+ docs don't trip\n        SQLite's bind-variable cap (spec_042 MID #2). Input order is preserved\n        and missing ids are silently dropped — same contract as the original\n        single-query implementation.\n        \"\"\"\n        if not parent_ids:\n            return []\n        by_id: dict[str, tuple] = {}\n        for i in range(0, len(parent_ids), self.SQLITE_VARIABLE_LIMIT):\n            chunk = parent_ids[i : i + self.SQLITE_VARIABLE_LIMIT]\n            placeholders = \",\".join(\"?\" * len(chunk))\n            rows = self._conn.execute(\n                f\"SELECT parent_id, doc_id, title, text, metadata_json FROM parents \"\n                f\"WHERE parent_id IN ({placeholders})\",\n                chunk,\n            ).fetchall()\n            for r in rows:\n                by_id[r[0]] = r\n        return [self._row_to_parent(by_id[pid]) for pid in parent_ids if pid in by_id]\n\n    def upsert_many(self, parents: list[ParentChunk]) -> int:\n        if not parents:\n            return 0\n        rows = [\n            (\n                p.parent_id,\n                p.doc_id,\n                p.title,\n                p.text,\n                json.dumps(p.metadata, ensure_ascii=False) if p.metadata else None,\n            )\n            for p in parents\n        ]\n        self._conn.executemany(\n            \"INSERT OR REPLACE INTO parents (parent_id, doc_id, title, text, metadata_json) \"\n            \"VALUES (?, ?, ?, ?, ?)\",\n            rows,\n        )\n        self._conn.commit()\n        return len(rows)\n\n    def has(self, parent_id: str) -> bool:\n        row = self._conn.execute(\n            \"SELECT 1 FROM parents WHERE parent_id = ?\", (parent_id,)\n        ).fetchone()\n        return row is not None\n\n    def count(self) -> int:\n        return self._conn.execute(\"SELECT COUNT(*) FROM parents\").fetchone()[0]\n\n    def clear(self) -> None:\n        self._conn.execute(\"DELETE FROM parents\")\n        self._conn.commit()\n\n    def close(self) -> None:\n        if self._conn:\n            self._conn.close()\n            self._conn = None  # type: ignore[assignment]\n\n    def list_all(self) -> list[ParentChunk]:\n        \"\"\"Return all stored parents. Not part of the Protocol; used by VectorStore.load_parents().\"\"\"\n        rows = self._conn.execute(\n            \"SELECT parent_id, doc_id, title, text, metadata_json FROM parents\"\n        ).fetchall()\n        return [self._row_to_parent(r) for r in rows]\n\n    def _row_to_parent(self, row: tuple) -> ParentChunk:\n        parent_id, doc_id, title, text, metadata_json = row\n        metadata = json.loads(metadata_json) if metadata_json else {}\n        return ParentChunk(\n            parent_id=parent_id,\n            doc_id=doc_id,\n            title=title,\n            text=text,\n            metadata=metadata,\n        )",
        "start_line": 34,
        "end_line": 153
      },
      "answer": {
        "options": [
          "JSONの方が速い",
          "起動時に全parentをメモリ展開せず、必要なparent_idだけ都度SELECTできる",
          "暗号化のため",
          "Chroma必須のため"
        ],
        "correct_index": 1
      },
      "explanation": "全ロード（起動コスト・メモリ）を避け、検索ヒットしたparentだけ遅延取得。spec_037の進化。"
    },
    {
      "id": "q_rev_001",
      "difficulty": "L2",
      "type": "freetext",
      "module": "backend/src/gap_detection.py",
      "tags": [
        "reverse",
        "gap-detection"
      ],
      "prompt": "次のコードの役割を一文で説明せよ:\n\n    def detect_no_info(answer_text: str | None) -> bool:\n        # returns True when the LLM answer indicates it has no info\n        # e.g. '資料に記載がありません'",
      "citation": {
        "file": "backend/src/gap_detection.py",
        "symbol": {
          "kind": "function",
          "name": "detect_no_info"
        },
        "evidence_substring": "no info",
        "adr_ref": null,
        "snippet": "def detect_no_info(answer_text): # returns True when the LLM answer indicates it has no info (資料に記載がありません 等)",
        "source": "def detect_no_info(answer_text: str | None) -> bool:\n    \"\"\"Return ``True`` when the LLM seems to have answered \"I don't know\".\n\n    Empty / whitespace-only answers are treated as a gap as well — an\n    empty model response is equivalent to \"no information\" for the\n    purpose of populating the knowledge-gap report.\n    \"\"\"\n    if not answer_text or not answer_text.strip():\n        return True\n    return bool(_NO_INFO_RE.search(answer_text))",
        "start_line": 192,
        "end_line": 201
      },
      "answer": {
        "model_answer": "LLMの回答が『情報がない/資料に記載がない』旨かを判定し、llm_no_info ギャップ検出に使う関数。",
        "required_keywords": [
          {
            "any_of": [
              "llm",
              "回答",
              "answer",
              "応答"
            ]
          },
          {
            "any_of": [
              "情報がない",
              "記載がない",
              "no info",
              "情報なし",
              "資料"
            ]
          },
          {
            "any_of": [
              "判定",
              "検出",
              "true",
              "ギャップ",
              "gap"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.66
      },
      "explanation": "逆向き問題：コードを見て役割を言語化。これが llm_no_info ギャップ判定の核。"
    },
    {
      "id": "q_rev_002",
      "difficulty": "L2",
      "type": "freetext",
      "module": "backend/src/marker.py",
      "tags": [
        "reverse",
        "marker"
      ],
      "prompt": "次の関数の役割を一文で説明せよ:\n\n    def update_block(text, name, new_content):\n        # find <!-- AUTO_GENERATED_START: name --> .. END\n        # replace only the inside, keep the rest",
      "citation": {
        "file": "backend/src/marker.py",
        "symbol": {
          "kind": "function",
          "name": "update_block"
        },
        "evidence_substring": "replace only the inside",
        "adr_ref": null,
        "snippet": "def update_block(text, name, new_content): # find AUTO_GENERATED_START:name .. END, replace only the inside, keep the rest",
        "source": "def update_block(text: str, name: str, new_content: str) -> str:\n    \"\"\"Replace an existing block's content. If absent, append at the end.\"\"\"\n    if not re.match(rf\"^{NAME_PATTERN}$\", name):\n        raise MarkerError(f\"Invalid marker name: {name!r}\")\n\n    new_content = new_content.rstrip(\"\\n\")\n    replacement = (\n        f\"<!-- AUTO_GENERATED_START: {name} -->\\n\"\n        f\"{new_content}\\n\"\n        f\"<!-- AUTO_GENERATED_END: {name} -->\"\n    )\n\n    block_re = re.compile(\n        r\"<!--\\s*AUTO_GENERATED_START:\\s*\" + re.escape(name) + r\"\\s*-->\\n\"\n        r\".*?\"\n        r\"\\n<!--\\s*AUTO_GENERATED_END:\\s*\" + re.escape(name) + r\"\\s*-->\",\n        re.DOTALL,\n    )\n    if block_re.search(text):\n        return block_re.sub(replacement, text, count=1)\n\n    # Append at end (with leading newline if needed)\n    sep = \"\" if text.endswith(\"\\n\\n\") else (\"\\n\" if text.endswith(\"\\n\") else \"\\n\\n\")\n    return text + sep + replacement + \"\\n\"",
        "start_line": 71,
        "end_line": 94
      },
      "answer": {
        "model_answer": "指定名のAUTO_GENERATEDブロックの区切り間だけを new_content で置換し、他（人間記述）は保持する関数。",
        "required_keywords": [
          {
            "any_of": [
              "ブロック",
              "block",
              "区切り",
              "delimiter",
              "auto_generated"
            ]
          },
          {
            "any_of": [
              "置換",
              "上書き",
              "replace",
              "差し替え"
            ]
          },
          {
            "any_of": [
              "他",
              "残り",
              "保持",
              "preserve",
              "keep",
              "人間"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.66
      },
      "explanation": "逆向き問題：区切り間だけ置換・他保全という marker の中核挙動を言語化させる。"
    },
    {
      "id": "q_api_001",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/api.py",
      "tags": [
        "api",
        "live-ingest"
      ],
      "prompt": "メモを受け取り live ingest を起動する HTTP エンドポイント関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/api.py",
        "symbol": {
          "kind": "function",
          "name": "post_ingest_memo"
        },
        "evidence_substring": "async def post_ingest_memo",
        "adr_ref": null,
        "snippet": "@app.post(\"/api/ingest/memo\", response_model=MemoIngestResponse)\nasync def post_ingest_memo(req: MemoIngestRequest, ...): ...",
        "source": "async def post_ingest_memo(\n    req: MemoIngestRequest,\n    x_axis_token: str | None = Header(default=None, alias=\"X-Axis-Token\"),\n) -> MemoIngestResponse:\n    \"\"\"Live ingest one memo. Same X-Axis-Token gate as /api/ingest.\n\n    Two modes:\n      * ``markdown``: full YAML+md text; written to ``<knowledge_dir>/<id>.md``\n        (overwrites if it exists — re-ingest is upsert).\n      * ``path``: relative or absolute path under ``knowledge_dir`` of an\n        already-saved file.\n\n    Either way, the file is loaded, chunked, embedded and merged into the\n    running ChromaDB collection — and the KnowledgeGraph is rebuilt — so\n    ``POST /api/search`` and ``GET /api/graph`` show the new memo\n    immediately, with no ``build_index --rebuild`` and no backend restart.\n    \"\"\"\n    _require_ingest_token(x_axis_token)\n    if not req.markdown and not req.path:\n        raise HTTPException(\n            status_code=400, detail=\"one of 'markdown' or 'path' is required\"\n        )\n\n    knowledge_dir = Path(_state.get(\"knowledge_dir\", \"./examples/knowledge\"))\n    target = _resolve_memo_path(req, knowledge_dir)\n    try:\n        result = _live_ingest_path(target)\n    except HTTPException:\n        raise\n    except Exception as e:  # noqa: BLE001\n        raise HTTPException(status_code=500, detail=f\"ingest failed: {e}\") from e\n    _rebuild_graph_state()\n    return MemoIngestResponse(\n        doc_id=result.doc_id,\n        saved_path=str(target),\n        parents=result.parents,\n        children=result.children,\n        deleted_existing=result.deleted_existing,\n        indexed=True,\n    )",
        "start_line": 444,
        "end_line": 483
      },
      "answer": {
        "file": "backend/src/api.py",
        "symbol": "post_ingest_memo"
      },
      "grading": {
        "symbol_aliases": [
          "post_ingest_memo"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "POST /api/ingest/memo のハンドラ。MCP の axis_ingest_memo もこの経路に繋がる（spec_056）。"
    },
    {
      "id": "q_api_002",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/api.py",
      "tags": [
        "api",
        "startup"
      ],
      "prompt": "FastAPI 起動時に1度だけグラフ・検索エンジン・各ストアを構築する仕組みは？",
      "citation": {
        "file": "backend/src/api.py",
        "symbol": {
          "kind": "function",
          "name": "lifespan"
        },
        "evidence_substring": "async def lifespan",
        "adr_ref": null,
        "snippet": "@asynccontextmanager\nasync def lifespan(app: FastAPI) -> AsyncIterator[None]:\n    # build graph / engine / stores once at startup, dispose at shutdown",
        "source": "async def lifespan(app: FastAPI) -> AsyncIterator[None]:\n    configure_logging()\n    logger.info(\"Initializing axis-knowledge-rag API...\")\n    store = VectorStore(path=settings.chroma_db_path)\n    app_cfg = load_app_config()\n    embedder = make_embedder(app_cfg.embedder)\n    normalizer = Normalizer.from_config(load_axes_config())\n    # spec_051 HIGH-1: detect embedder ↔ index dim mismatch at startup.\n    # Empty store (probe returns None) skips silently — first ingest will\n    # set the dim. A mismatch is fatal because every subsequent query\n    # would crash inside Chroma with an opaque shape error.\n    try:\n        store_dim = store.probe_dim()\n    except Exception as e:  # noqa: BLE001\n        logger.warning(\"dim verification skipped: %s\", e)\n        store_dim = None\n    if store_dim is not None and store_dim != embedder.dim:\n        raise RuntimeError(\n            f\"Embedding dim mismatch: chroma store has dim={store_dim}, \"\n            f\"embedder ({type(embedder).__name__}) reports dim={embedder.dim}. \"\n            f\"config.yml で embedder.backend を変更した場合は \"\n            f\"`PYTHONPATH=. python3 -m scripts.build_index examples/knowledge --rebuild` \"\n            f\"を実行して index を rebuild してください。\"\n        )\n    pd = app_cfg.retrieval.parent_doc\n    if pd.enabled and not store.has_parents():\n        logger.warning(\n            \"parent_doc.enabled=true but parents.json is missing — falling back \"\n            \"to legacy file-level search. Run scripts/build_index.py --rebuild \"\n            \"--mode parent_doc to populate it.\"\n        )\n        parent_doc_enabled = False\n    else:\n        parent_doc_enabled = pd.enabled\n    graph: KnowledgeGraph | None = None\n    if app_cfg.graph.enabled:\n        # spec_042 LOW #5: building the graph walks the knowledge dir + parses\n        # frontmatter, which can stall lifespan for seconds on 1000+ doc\n        # corpora. Run it on the default executor so the event loop stays\n        # responsive (e.g. for liveness probes).\n        loop = asyncio.get_running_loop()\n        try:\n            graph = await loop.run_in_executor(\n                None, build_default_graph, app_cfg.graph.knowledge_dir\n            )\n            logger.info(\"knowledge graph stats: %s\", graph.stats())\n        except Exception as e:  # noqa: BLE001\n            logger.warning(\"failed to build knowledge graph: %s — disabling /api/graph\", e)\n            graph = None\n    # spec_048: build the gap store first so both the search engine\n    # (no_results / low_score) and the RAG pipeline (llm_no_info) can\n    # share the same backing SQLite. Disabled config → both hooks become\n    # no-ops without any further conditionals at the call sites.\n    gap_store = make_gap_store(app_cfg.gap)\n    if gap_store is not None:\n        logger.info(\"gap store: %s\", type(gap_store).__name__)\n    else:\n        logger.info(\"gap store: disabled (gap.enabled=false)\")\n    engine = SearchEngine(\n        store,\n        embedder,\n        normalizer,\n        parent_doc_enabled=parent_doc_enabled,\n        top_k_children=pd.top_k_children,\n        graph=graph,\n        gap_store=gap_store,\n        gap_low_score_threshold=app_cfg.gap.low_score_threshold,\n    )\n    rag = RAGPipeline(\n        engine,\n        context_max_chars=app_cfg.rag.context_max_chars,\n        backend=make_generation_backend(app_cfg.generation),\n        gap_store=gap_store,\n    )\n    chat_store = make_conversation_store(app_cfg.chat)\n    configure_default_store(chat_store)\n    logger.info(\n        \"chat store: %s (backend=%s)\",\n        type(chat_store).__name__,\n        app_cfg.chat.storage.backend,\n    )\n    feedback_store = make_feedback_store(app_cfg.feedback)\n    if feedback_store is not None:\n        logger.info(\"feedback store: %s\", type(feedback_store).__name__)\n    else:\n        logger.info(\"feedback store: disabled (feedback.enabled=false)\")\n    _state[\"engine\"] = engine\n    _state[\"rag\"] = rag\n    _state[\"embedder\"] = embedder\n    # spec_056: keep the live VectorStore + Normalizer so /api/ingest/memo can\n    # add chunks against the same collection the engine is querying — drop+\n    # recreate would invalidate the engine's collection handle.\n    _state[\"store\"] = store\n    _state[\"normalizer\"] = normalizer\n    _state[\"retrieval_cfg\"] = app_cfg.retrieval\n    _state[\"axes_cfg\"] = load_axes_config()\n    _state[\"chat_store\"] = chat_store\n    _state[\"chat_cfg\"] = app_cfg.chat\n    _state[\"graph\"] = graph\n    _state[\"graph_cfg\"] = app_cfg.graph\n    _state[\"knowledge_dir\"] = app_cfg.graph.knowledge_dir\n    _state[\"feedback_store\"] = feedback_store\n    _state[\"feedback_cfg\"] = app_cfg.feedback\n    _state[\"gap_store\"] = gap_store\n    _state[\"gap_cfg\"] = app_cfg.gap\n    try:\n        yield\n    finally:\n        try:\n            chat_store.close()\n        except Exception:  # noqa: BLE001\n            logger.warning(\"chat store close failed\", exc_info=True)\n        if feedback_store is not None:\n            try:\n                feedback_store.close()\n            except Exception:  # noqa: BLE001\n                logger.warning(\"feedback store close failed\", exc_info=True)\n        if gap_store is not None:\n            try:\n                gap_store.close()\n            except Exception:  # noqa: BLE001\n                logger.warning(\"gap store close failed\", exc_info=True)\n        _state.clear()",
        "start_line": 68,
        "end_line": 190
      },
      "answer": {
        "options": [
          "各リクエストで毎回構築する",
          "@asynccontextmanager の lifespan で起動時に1度構築する",
          "モジュールimport時のグローバルで構築する",
          "フロントエンドが構築する"
        ],
        "correct_index": 1
      },
      "explanation": "lifespan（asynccontextmanager）で起動時に重い初期化を1回だけ行い、shutdownで後始末。"
    },
    {
      "id": "q_api_003",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/api.py",
      "tags": [
        "api",
        "security"
      ],
      "prompt": "ingest トークン（X-Axis-Token）を検証する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/api.py",
        "symbol": {
          "kind": "function",
          "name": "_require_ingest_token"
        },
        "evidence_substring": "def _require_ingest_token",
        "adr_ref": null,
        "snippet": "def _require_ingest_token(x_axis_token: str | None) -> None:\n    # compare against AXIS_INGEST_TOKEN env; 401 on mismatch",
        "source": "def _require_ingest_token(x_axis_token: str | None) -> None:\n    \"\"\"401 when AXIS_INGEST_TOKEN is configured and the header doesn't match.\n\n    Read at call-time (not at import) so tests can monkey-patch\n    ``os.environ`` after the app has been built.\n    \"\"\"\n    expected = (os.environ.get(\"AXIS_INGEST_TOKEN\", \"\") or \"\").strip()\n    if not expected:\n        return\n    if x_axis_token != expected:\n        raise HTTPException(status_code=401, detail=\"invalid or missing X-Axis-Token\")",
        "start_line": 279,
        "end_line": 289
      },
      "answer": {
        "file": "backend/src/api.py",
        "symbol": "_require_ingest_token"
      },
      "grading": {
        "symbol_aliases": [
          "_require_ingest_token"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "AXIS_INGEST_TOKEN と X-Axis-Token を照合。未設定時は v0.8 互換で無認証許容。"
    },
    {
      "id": "q_api_004",
      "difficulty": "L4",
      "type": "mcq",
      "module": "README.md",
      "tags": [
        "api",
        "security",
        "design"
      ],
      "prompt": "AXIS_INGEST_TOKEN が未設定のとき、POST /api/ingest の挙動は？",
      "citation": {
        "file": "README.md",
        "symbol": {
          "kind": "section",
          "name": "Security note"
        },
        "evidence_substring": "v0.8 互換 (無認証許容)",
        "adr_ref": null,
        "snippet": "`AXIS_INGEST_TOKEN` が未設定の場合は v0.8 互換 (無認証許容)。127.0.0.1 で動かす限りは問題ないが、LAN越し/外部公開時は必ず設定する。"
      },
      "answer": {
        "options": [
          "常に401で拒否",
          "v0.8 互換で無認証許容（ローカル前提、外部公開時は要設定）",
          "サーバ起動を停止する",
          "ランダムトークンを自動生成"
        ],
        "correct_index": 1
      },
      "explanation": "ローカル(127.0.0.1)前提なら無認証でOK。外部公開時のみトークン必須、という段階的セキュリティ設計。"
    },
    {
      "id": "q_api_005",
      "difficulty": "L3",
      "type": "dataflow",
      "module": "backend/src/api.py",
      "tags": [
        "api",
        "pipeline"
      ],
      "prompt": "POST /api/search がレスポンスを返すまでの主要ステップを順に並べよ（候補: searchエンドポイント, engine.search, _to_payload, SearchResponse）。",
      "citation": {
        "file": "backend/src/api.py",
        "symbol": {
          "kind": "function",
          "name": "search"
        },
        "evidence_substring": "_to_payload",
        "adr_ref": null,
        "snippet": "async def search(req: SearchRequest) -> SearchResponse:\n    results = engine.search(req.query, filters=req.filters, top_k=req.top_k, ...)\n    return SearchResponse(results=[_to_payload(r) for r in results])",
        "source": "async def search(req: SearchRequest) -> SearchResponse:\n    engine: SearchEngine = _state[\"engine\"]\n    graph_cfg = _state.get(\"graph_cfg\")\n    # config-level default (expand_on_search) is OR'd with the request flag,\n    # so admins can flip the default without touching callers.\n    do_expand = req.graph_expand or bool(graph_cfg and graph_cfg.expand_on_search)\n    try:\n        results = engine.search(\n            req.query,\n            filters=req.filters or None,\n            top_k=req.top_k,\n            graph_expand=do_expand,\n            graph_hop=req.graph_hop,\n            graph_max_neighbors=req.graph_max_neighbors,\n        )\n    except Exception as e:\n        raise HTTPException(status_code=500, detail=str(e)) from e\n    return SearchResponse(results=[_to_payload(r) for r in results])",
        "start_line": 487,
        "end_line": 504
      },
      "answer": {
        "sequence": [
          "search",
          "engine.search",
          "_to_payload",
          "SearchResponse"
        ]
      },
      "grading": {
        "mode": "subsequence",
        "pass_threshold": 0.75,
        "aliases": [
          [
            "search",
            "エンドポイント",
            "endpoint"
          ],
          [
            "engine.search",
            "searchengine",
            "検索エンジン",
            "engine"
          ],
          [
            "_to_payload",
            "payload",
            "変換"
          ],
          [
            "searchresponse",
            "response",
            "レスポンス"
          ]
        ]
      },
      "pool": [
        "_to_payload",
        "SearchResponse",
        "search",
        "engine.search"
      ],
      "explanation": "エンドポイントが SearchEngine.search を呼び、SearchResult を _to_payload で API 用に変換して SearchResponse に詰める。"
    },
    {
      "id": "q_eval_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "evaluation/judge.py",
      "tags": [
        "evaluation",
        "ragas"
      ],
      "prompt": "evaluation/judge.py の役割は？",
      "citation": {
        "file": "evaluation/judge.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "ragas LLM judge",
        "adr_ref": "ADR-019",
        "snippet": "Gemini 1.5 Flash adapter for ragas LLM judge.\ndef get_judge_llm() -> BaseChatModel: ..."
      },
      "answer": {
        "options": [
          "検索インデックスを作る",
          "ragas 評価の LLM judge として Gemini Flash を供給するアダプタ",
          "フロントの状態管理",
          "DBマイグレーション"
        ],
        "correct_index": 1
      },
      "explanation": "RAGAS（RAG品質自動評価）が使う judge LLM/embeddings に Gemini Flash を差し込むアダプタ。"
    },
    {
      "id": "q_eval_002",
      "difficulty": "L1",
      "type": "location",
      "module": "evaluation/stats.py",
      "tags": [
        "evaluation",
        "stats"
      ],
      "prompt": "RAGAS スコアの A/B 比較で「対応のある t 検定」を行う関数は？（ファイルとシンボル）",
      "citation": {
        "file": "evaluation/stats.py",
        "symbol": {
          "kind": "function",
          "name": "paired_t_test"
        },
        "evidence_substring": "def paired_t_test",
        "adr_ref": null,
        "snippet": "@dataclass(frozen=True)\nclass TTestResult: ...\ndef paired_t_test(scores_a, scores_b): # paired t-test for A/B metric comparison",
        "source": "def paired_t_test(\n    scores_a: list[float], scores_b: list[float], *, alpha: float = 0.05\n) -> TTestResult | None:\n    \"\"\"Run paired t-test on two RAGAS score arrays for the same metric.\n\n    Returns None if scipy not available (graceful degradation).\n    \"\"\"\n    if len(scores_a) != len(scores_b) or len(scores_a) < 2:\n        raise ValueError(\n            f\"need same-length arrays >= 2, got {len(scores_a)} vs {len(scores_b)}\"\n        )\n    try:\n        from scipy import stats\n    except ImportError:\n        return None\n    result = stats.ttest_rel(scores_b, scores_a)\n    diff = sum(scores_b) / len(scores_b) - sum(scores_a) / len(scores_a)\n    direction = \"tie\"\n    if result.pvalue < alpha:\n        direction = \"B>A\" if diff > 0 else \"A>B\"\n    return TTestResult(\n        metric=\"\",  # caller fills\n        mean_a=sum(scores_a) / len(scores_a),\n        mean_b=sum(scores_b) / len(scores_b),\n        diff=diff,\n        t_statistic=float(result.statistic),\n        p_value=float(result.pvalue),\n        significant=bool(result.pvalue < alpha),\n        direction=direction,\n    )",
        "start_line": 20,
        "end_line": 49
      },
      "answer": {
        "file": "evaluation/stats.py",
        "symbol": "paired_t_test"
      },
      "grading": {
        "symbol_aliases": [
          "paired_t_test"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "施策A/Bのスコア差が統計的に有意かを対応のあるt検定で判定（TTestResultを返す）。"
    },
    {
      "id": "q_eval_003",
      "difficulty": "L1",
      "type": "location",
      "module": "evaluation/run_ragas.py",
      "tags": [
        "evaluation",
        "ragas"
      ],
      "prompt": "RAGAS 評価を実行するエントリポイント関数は？（ファイルとシンボル）",
      "citation": {
        "file": "evaluation/run_ragas.py",
        "symbol": {
          "kind": "function",
          "name": "main"
        },
        "evidence_substring": "def main",
        "adr_ref": "ADR-019",
        "snippet": "def main() -> int:\n    # build pipeline, build_dataset, run ragas, compare to baseline, maybe update",
        "source": "def main() -> int:\n    p = argparse.ArgumentParser(description=\"Run RAGAS evaluation.\")\n    p.add_argument(\"--dataset\", type=Path, required=True)\n    p.add_argument(\n        \"--baseline\", type=Path, default=Path(\"evaluation/baseline.json\")\n    )\n    p.add_argument(\"--output\", type=Path, required=True)\n    p.add_argument(\"--update-baseline\", action=\"store_true\")\n    p.add_argument(\n        \"--regression-threshold\",\n        type=float,\n        default=0.05,\n        help=\"Warn if any metric drops by more than this fraction vs baseline.\",\n    )\n    p.add_argument(\n        \"--block-on-regression\",\n        action=\"store_true\",\n        help=\"Exit 1 if any metric drops > --regression-threshold from baseline (v0.8+)\",\n    )\n    args = p.parse_args()\n\n    engine, pipeline = _build_pipeline()\n    ds = build_dataset(args.dataset, engine, pipeline)\n\n    result = evaluate(\n        ds,\n        metrics=METRICS,\n        llm=get_judge_llm(),\n        embeddings=get_judge_embeddings(),\n        run_config=RunConfig(max_workers=2, max_retries=15, timeout=120),\n    )\n\n    # ragas v0.4 returns per-row lists; aggregate to scalar (ignore NaN from failed judge calls)\n    def _to_scalar(v):\n        if hasattr(v, '__iter__') and not isinstance(v, str):\n            arr = np.array(list(v), dtype=float)\n            return float(np.nanmean(arr)) if arr.size else float('nan')\n        return float(v)\n    scores = {m.name: _to_scalar(result[m.name]) for m in METRICS}\n    record: dict[str, Any] = {\n        \"timestamp\": datetime.now(UTC).isoformat(),\n        \"git_sha\": _git_sha(),\n        \"dataset\": str(args.dataset),\n        \"scores\": scores,\n    }\n    args.output.parent.mkdir(parents=True, exist_ok=True)\n    args.output.write_text(json.dumps(record, indent=2, ensure_ascii=False))\n\n    regressions = _check_regression(scores, args.baseline, args.regression_threshold)\n    _print_summary(scores, regressions)\n\n    if args.update_baseline:\n        args.baseline.write_text(json.dumps(record, indent=2, ensure_ascii=False))\n        print(f\"\\nBaseline updated: {args.baseline}\")\n\n    if args.block_on_regression and regressions:\n        print(f\"\\n## BLOCK: {len(regressions)} metrics regressed beyond threshold\")\n        return 1\n\n    return 0",
        "start_line": 88,
        "end_line": 147
      },
      "answer": {
        "file": "evaluation/run_ragas.py",
        "symbol": "main"
      },
      "grading": {
        "symbol_aliases": [
          "main",
          "run_pipeline"
        ],
        "accept_file_only_partial": 0.6
      },
      "explanation": "python -m evaluation.run_ragas のエントリ。dataset構築→ragas実行→baseline比較→（任意）更新。"
    },
    {
      "id": "q_eval_004",
      "difficulty": "L2",
      "type": "location",
      "module": "evaluation/run_ragas.py",
      "tags": [
        "evaluation",
        "regression"
      ],
      "prompt": "ベースラインと比較してスコアの退行（regression）を判定する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "evaluation/run_ragas.py",
        "symbol": {
          "kind": "function",
          "name": "_check_regression"
        },
        "evidence_substring": "def _check_regression",
        "adr_ref": "ADR-019",
        "snippet": "def _check_regression(current, baseline, ...): # fail if metrics dropped beyond tolerance vs baseline.json",
        "source": "def _check_regression(\n    scores: dict[str, float], baseline_path: Path, threshold: float\n) -> list[tuple[str, float, float]]:\n    if not baseline_path.exists():\n        return []\n    baseline = json.loads(baseline_path.read_text())[\"scores\"]\n    out = []\n    for k, v in scores.items():\n        b = baseline.get(k)\n        if b is None or b == 0.0:\n            continue\n        if (b - v) / b > threshold:\n            out.append((k, b, v))\n    return out",
        "start_line": 150,
        "end_line": 163
      },
      "answer": {
        "file": "evaluation/run_ragas.py",
        "symbol": "_check_regression"
      },
      "grading": {
        "symbol_aliases": [
          "_check_regression"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "baseline.json比で許容超の低下があればCIをfailさせ、品質退行のマージを止める。"
    },
    {
      "id": "q_eval_005",
      "difficulty": "L4",
      "type": "mcq",
      "module": "evaluation/run_ragas.py",
      "tags": [
        "evaluation",
        "design"
      ],
      "prompt": "evaluation/baseline.json を保持し毎回比較する設計の狙いは？",
      "citation": {
        "file": "evaluation/run_ragas.py",
        "symbol": {
          "kind": "module",
          "name": "usage docstring"
        },
        "evidence_substring": "--update-baseline",
        "adr_ref": "ADR-019",
        "snippet": "Usage: python -m evaluation.run_ragas --dataset ... --baseline evaluation/baseline.json --output ... --update-baseline"
      },
      "answer": {
        "options": [
          "デプロイを速くするため",
          "過去スコアを基準に固定し、変更で RAG 品質が退行していないか自動で検知するため",
          "APIキーを保存するため",
          "フロントのキャッシュ用"
        ],
        "correct_index": 1
      },
      "explanation": "RAG は変更で静かに精度が落ちやすい。baseline固定＋regression検知で“質を測りながら”改善する。"
    },
    {
      "id": "q_fe3d_001",
      "difficulty": "L1",
      "type": "location",
      "module": "frontend/src/components/Knowledge3DGraph.tsx",
      "tags": [
        "frontend",
        "graphrag",
        "3d"
      ],
      "prompt": "ナレッジグラフを3D力学シミュレーションで描画するコンポーネントは？（ファイルとシンボル）",
      "citation": {
        "file": "frontend/src/components/Knowledge3DGraph.tsx",
        "symbol": {
          "kind": "component",
          "name": "Knowledge3DGraph"
        },
        "evidence_substring": "ForceGraph3D",
        "adr_ref": "ADR-025",
        "snippet": "import ForceGraph3D from \"react-force-graph-3d\";\nexport function Knowledge3DGraph({ data, onNodeClick, highlightedId }: Props) { ... }",
        "source": "export function Knowledge3DGraph({ data, onNodeClick, highlightedId }: Props) {\n  const nodes = useMemo(\n    () =>\n      data.nodes.map((n) => ({\n        id: n.id,\n        name: n.title || n.id,\n        val: 2 + n.in_degree + n.out_degree,\n        color: AXIS_COLORS[String(n.axes.category ?? \"\")] ?? \"#9ca3af\",\n        __raw: n,\n      })),\n    [data.nodes],\n  );\n\n  const links = useMemo(\n    () => data.edges.map((e) => ({ source: e.source, target: e.target })),\n    [data.edges],\n  );\n\n  // ForceGraph3D defaults width/height to window.innerWidth/innerHeight, which\n  // overflows the column and breaks page layout (see ADR-032). Measure the\n  // wrapper and feed explicit size props so the canvas fits its container.\n  const wrapRef = useRef<HTMLDivElement | null>(null);\n  const [size, setSize] = useState<{ w: number; h: number }>({ w: 0, h: 0 });\n",
        "start_line": 20,
        "end_line": 43
      },
      "answer": {
        "file": "frontend/src/components/Knowledge3DGraph.tsx",
        "symbol": "Knowledge3DGraph"
      },
      "grading": {
        "symbol_aliases": [
          "Knowledge3DGraph"
        ],
        "accept_file_only_partial": 0.7
      },
      "explanation": "react-force-graph-3d を使った /graph ページの3D可視化（spec_040/ADR-025）。"
    },
    {
      "id": "q_fe3d_002",
      "difficulty": "L2",
      "type": "mcq",
      "module": "frontend/src/components/Knowledge3DGraph.tsx",
      "tags": [
        "frontend",
        "3d"
      ],
      "prompt": "3Dグラフのノードの色は何で決まる？",
      "citation": {
        "file": "frontend/src/components/Knowledge3DGraph.tsx",
        "symbol": {
          "kind": "const",
          "name": "AXIS_COLORS"
        },
        "evidence_substring": "AXIS_COLORS[String(n.axes.category",
        "adr_ref": null,
        "snippet": "const AXIS_COLORS = { 技術記事: '#3b82f6', メモ: '#a855f7', 議事録: '#22c55e', ToDo: '#f97316' };\ncolor: AXIS_COLORS[String(n.axes.category ?? \"\")] ?? \"#9ca3af\",",
        "source": "const AXIS_COLORS: Record<string, string> = {\n  \"技術記事\": \"#3b82f6\",\n  \"メモ\": \"#a855f7\",\n  \"議事録\": \"#22c55e\",\n  \"ToDo\": \"#f97316\",\n};\n\nexport function Knowledge3DGraph({ data, onNodeClick, highlightedId }: Props) {\n  const nodes = useMemo(\n    () =>\n      data.nodes.map((n) => ({\n        id: n.id,\n        name: n.title || n.id,\n        val: 2 + n.in_degree + n.out_degree,\n        color: AXIS_COLORS[String(n.axes.category ?? \"\")] ?? \"#9ca3af\",\n        __raw: n,\n      })),\n    [data.nodes],\n  );\n\n  const links = useMemo(\n    () => data.edges.map((e) => ({ source: e.source, target: e.target })),\n    [data.edges],\n  );",
        "start_line": 13,
        "end_line": 36
      },
      "answer": {
        "options": [
          "ランダム",
          "軸 category（AXIS_COLORS マップ）で色分け、未定義はグレー",
          "in_degree のみ",
          "ファイルサイズ"
        ],
        "correct_index": 1
      },
      "explanation": "category 軸（技術記事/メモ/議事録/ToDo）で色分け。該当なしは #9ca3af（グレー）。"
    },
    {
      "id": "q_fe3d_003",
      "difficulty": "L2",
      "type": "fill_blank",
      "module": "frontend/src/components/Knowledge3DGraph.tsx",
      "tags": [
        "frontend",
        "3d"
      ],
      "prompt": "ノードの大きさ val の式の空欄を埋めよ:\n    val: 2 + n.in_degree + n.____",
      "citation": {
        "file": "frontend/src/components/Knowledge3DGraph.tsx",
        "symbol": {
          "kind": "component",
          "name": "Knowledge3DGraph"
        },
        "evidence_substring": "2 + n.in_degree + n.out_degree",
        "adr_ref": null,
        "snippet": "val: 2 + n.in_degree + n.out_degree,",
        "source": "export function Knowledge3DGraph({ data, onNodeClick, highlightedId }: Props) {\n  const nodes = useMemo(\n    () =>\n      data.nodes.map((n) => ({\n        id: n.id,\n        name: n.title || n.id,\n        val: 2 + n.in_degree + n.out_degree,\n        color: AXIS_COLORS[String(n.axes.category ?? \"\")] ?? \"#9ca3af\",\n        __raw: n,\n      })),\n    [data.nodes],\n  );\n\n  const links = useMemo(\n    () => data.edges.map((e) => ({ source: e.source, target: e.target })),\n    [data.edges],\n  );\n\n  // ForceGraph3D defaults width/height to window.innerWidth/innerHeight, which\n  // overflows the column and breaks page layout (see ADR-032). Measure the\n  // wrapper and feed explicit size props so the canvas fits its container.\n  const wrapRef = useRef<HTMLDivElement | null>(null);\n  const [size, setSize] = useState<{ w: number; h: number }>({ w: 0, h: 0 });\n",
        "start_line": 20,
        "end_line": 43
      },
      "answer": {
        "accepted": [
          "out_degree",
          "n.out_degree"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "ノードサイズ＝2＋入次数＋出次数。参照が多い doc ほど大きく描かれる。"
    },
    {
      "id": "q_gc_001",
      "difficulty": "L1",
      "type": "location",
      "module": "frontend/src/lib/graphClient.ts",
      "tags": [
        "frontend",
        "graphrag"
      ],
      "prompt": "spec_040 のグラフ API（/api/graph 等）を叩くフロントのクライアント/型はどのファイル？",
      "citation": {
        "file": "frontend/src/lib/graphClient.ts",
        "symbol": {
          "kind": "module",
          "name": "module header"
        },
        "evidence_substring": "knowledge-graph endpoints",
        "adr_ref": "ADR-024",
        "snippet": "// API client for the spec_040 knowledge-graph endpoints.\n// Mirrors backend/src/schemas.py {Graph,Neighbor}Response.\nexport interface GraphNode { id; title; axes; in_degree; out_degree }"
      },
      "answer": {
        "file": "frontend/src/lib/graphClient.ts",
        "symbol": "graphClient"
      },
      "grading": {
        "symbol_aliases": [
          "graphClient",
          "GraphPayload",
          "GraphNode"
        ],
        "accept_file_only_partial": 0.8
      },
      "explanation": "graphClient.ts がグラフ系エンドポイントの型とfetchを担う。型は backend/src/schemas.py をミラー。"
    },
    {
      "id": "q_mcpf_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "mcp_server/formatters.py",
      "tags": [
        "mcp"
      ],
      "prompt": "mcp_server/formatters.py の役割は？",
      "citation": {
        "file": "mcp_server/formatters.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "shared across tools",
        "adr_ref": null,
        "snippet": "Markdown / JSON formatters shared across tools.\ndef format_search_results_md(...): ...\ndef format_answer_md(...): ..."
      },
      "answer": {
        "options": [
          "MCPの認証",
          "各MCPツールが共有する出力整形（Markdown/JSON）",
          "ベクトル検索本体",
          "DB接続"
        ],
        "correct_index": 1
      },
      "explanation": "axis_search / axis_answer / axis_chat 等が共通で使う、結果の Markdown/JSON 整形ユーティリティ。"
    },
    {
      "id": "q_mcpf_002",
      "difficulty": "L1",
      "type": "location",
      "module": "mcp_server/formatters.py",
      "tags": [
        "mcp"
      ],
      "prompt": "検索結果を Markdown に整形する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "mcp_server/formatters.py",
        "symbol": {
          "kind": "function",
          "name": "format_search_results_md"
        },
        "evidence_substring": "def format_search_results_md",
        "adr_ref": null,
        "snippet": "def format_search_results_md(query, filters, results) -> str:\n    lines = []\n    title = f\"Search results for: `{query}`\" if query else \"Axis-only filter results\"",
        "source": "def format_search_results_md(query: str | None, filters: dict, results: list[SearchResult]) -> str:\n    lines = []\n    title = f\"Search results for: `{query}`\" if query else \"Axis-only filter results\"\n    lines.append(f\"# {title}\")\n    if filters:\n        lines.append(f\"\\n**Filters**: {filters}\\n\")\n    lines.append(f\"\\n**{len(results)}** result(s)\\n\")\n    for r in results:\n        lines.append(f\"## [{r.score:.3f}] `{r.id}` — {r.title}\")\n        lines.append(f\"- axes: {r.axes}\")\n        if r.refs:\n            lines.append(f\"- refs: {r.refs}\")\n        lines.append(f\"\\n{r.body_snippet}\\n\")\n    return \"\\n\".join(lines)",
        "start_line": 10,
        "end_line": 23
      },
      "answer": {
        "file": "mcp_server/formatters.py",
        "symbol": "format_search_results_md"
      },
      "grading": {
        "symbol_aliases": [
          "format_search_results_md"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "axis_search が返す Markdown を組み立てる。JSON版は format_search_results_json。"
    },
    {
      "id": "q_df_ingest",
      "difficulty": "L3",
      "type": "dataflow",
      "module": "backend/src/api.py",
      "tags": [
        "live-ingest",
        "pipeline"
      ],
      "prompt": "POST /api/ingest/memo で送ったメモが検索可能になるまでの主要ステップを順に並べよ（候補: post_ingest_memo, _resolve_memo_path, ファイル書き込み, live_ingest(chunk→embed→upsert)）。",
      "citation": {
        "file": "backend/src/api.py",
        "symbol": {
          "kind": "function",
          "name": "post_ingest_memo"
        },
        "evidence_substring": "_resolve_memo_path",
        "adr_ref": null,
        "snippet": "async def post_ingest_memo(req):\n    path = _resolve_memo_path(req, knowledge_dir)\n    path.write_text(markdown)\n    _live_ingest_path(path)   # chunk -> embed -> upsert into Chroma (no rebuild)",
        "source": "async def post_ingest_memo(\n    req: MemoIngestRequest,\n    x_axis_token: str | None = Header(default=None, alias=\"X-Axis-Token\"),\n) -> MemoIngestResponse:\n    \"\"\"Live ingest one memo. Same X-Axis-Token gate as /api/ingest.\n\n    Two modes:\n      * ``markdown``: full YAML+md text; written to ``<knowledge_dir>/<id>.md``\n        (overwrites if it exists — re-ingest is upsert).\n      * ``path``: relative or absolute path under ``knowledge_dir`` of an\n        already-saved file.\n\n    Either way, the file is loaded, chunked, embedded and merged into the\n    running ChromaDB collection — and the KnowledgeGraph is rebuilt — so\n    ``POST /api/search`` and ``GET /api/graph`` show the new memo\n    immediately, with no ``build_index --rebuild`` and no backend restart.\n    \"\"\"\n    _require_ingest_token(x_axis_token)\n    if not req.markdown and not req.path:\n        raise HTTPException(\n            status_code=400, detail=\"one of 'markdown' or 'path' is required\"\n        )\n\n    knowledge_dir = Path(_state.get(\"knowledge_dir\", \"./examples/knowledge\"))\n    target = _resolve_memo_path(req, knowledge_dir)\n    try:\n        result = _live_ingest_path(target)\n    except HTTPException:\n        raise\n    except Exception as e:  # noqa: BLE001\n        raise HTTPException(status_code=500, detail=f\"ingest failed: {e}\") from e\n    _rebuild_graph_state()\n    return MemoIngestResponse(\n        doc_id=result.doc_id,\n        saved_path=str(target),\n        parents=result.parents,\n        children=result.children,\n        deleted_existing=result.deleted_existing,\n        indexed=True,\n    )",
        "start_line": 444,
        "end_line": 483
      },
      "answer": {
        "sequence": [
          "post_ingest_memo",
          "_resolve_memo_path",
          "write",
          "live_ingest"
        ]
      },
      "grading": {
        "mode": "subsequence",
        "pass_threshold": 0.66,
        "aliases": [
          [
            "post_ingest_memo",
            "endpoint",
            "受領"
          ],
          [
            "_resolve_memo_path",
            "path",
            "パス解決"
          ],
          [
            "write",
            "書き込み",
            "保存",
            "write_text"
          ],
          [
            "live_ingest",
            "_live_ingest_path",
            "chunk",
            "embed",
            "upsert",
            "索引"
          ]
        ]
      },
      "pool": [
        "_resolve_memo_path",
        "live_ingest",
        "post_ingest_memo",
        "write"
      ],
      "explanation": "受領→保存先パス解決→Markdown書き込み→live ingest（chunk→embed→Chromaにupsert、リビルド無し）で約1秒で検索可能に（spec_056）。"
    },
    {
      "id": "q_df_chat",
      "difficulty": "L3",
      "type": "dataflow",
      "module": "backend/src/rag.py",
      "tags": [
        "conversational-rag",
        "pipeline"
      ],
      "prompt": "Conversational RAG（履歴付き）でフォローアップ質問に答えるまでの主要ステップを順に並べよ（候補: rewrite_question, engine.search, build_context, generate, 履歴保存）。",
      "citation": {
        "file": "backend/src/rag.py",
        "symbol": {
          "kind": "method",
          "name": "RAGPipeline.chat"
        },
        "evidence_substring": "rewrite_question",
        "adr_ref": "ADR-018",
        "snippet": "def chat(self, question, session_id):\n    standalone = rewrite_question(history, question)\n    results = self._engine.search(standalone)\n    context = build_context(results)\n    text = self._call_generation(...)  # original question used for generation\n    store.append(session_id, ...)",
        "source": "    def chat(\n        self,\n        question: str,\n        *,\n        session_id: str | None = None,\n        filters: dict[str, Any] | None = None,\n        top_k: int = 5,\n        max_tokens: int = 1024,\n        store: ConversationStore | None = None,\n        rewriter_enabled: bool = True,\n        rewriter_model: str = \"gemini-2.5-flash\",\n        history_turns: int = 6,\n    ) -> ChatResponse:\n        \"\"\"Single-turn chat that keeps history under ``session_id``.\n\n        Steps:\n        1. Look up (or create) the session in ``store``.\n        2. Rewrite the question into a standalone query using up to\n           ``history_turns`` turns of prior context.\n        3. Retrieve with the rewritten query, generate with Claude (or the\n           dummy fallback) — the generation prompt sees the *original*\n           question plus the last 3 turns of history.\n        4. Append user + assistant messages back into the session.\n        \"\"\"\n        # Use ``is None`` rather than ``store or ...`` — ConversationStore\n        # defines __len__, so an empty store is falsy and would silently\n        # fall through to the module default.\n        if store is None:\n            store = get_default_store()\n        session = store.get_or_create(session_id)\n\n        history = store.get_history(\n            session.session_id, last_n_turns=history_turns\n        )\n        rewritten = rewrite_question(\n            question,\n            history,\n            model_name=rewriter_model,\n            enabled=rewriter_enabled,\n        )\n        retrieval_query = rewritten or question\n\n        results = self._engine.search(retrieval_query, filters=filters, top_k=top_k)\n\n        if self._use_dummy:\n            ans = _dummy_answer(question, results)\n        else:\n            ans = self._generate_chat_answer(\n                question=question,\n                results=results,\n                history=history,\n                max_tokens=max_tokens,\n            )\n        # spec_048: chat path goes through the same gap detection — the\n        # rewritten question is what hit the index, but we want the\n        # user-facing string in the report so it's actionable.\n        self._record_llm_gap(question, ans.text, results)\n\n        # Persist turn — sources as plain dicts so the store stays\n        # JSON-friendly (the API returns them via asdict()).\n        sources_payload = [_source_to_dict(s) for s in ans.sources]\n        store.append(session.session_id, Message(role=\"user\", content=question))\n        store.append(\n            session.session_id,\n            Message(role=\"assistant\", content=ans.text, sources=sources_payload),\n        )\n\n        return ChatResponse(\n            session_id=session.session_id,\n            question=question,\n            rewritten_question=rewritten if rewritten != question else None,\n            answer=ans.text,\n            sources=ans.sources,\n            cited_ids=ans.cited_ids,\n            is_dummy=ans.is_dummy,\n            model=ans.model,\n        )",
        "start_line": 625,
        "end_line": 701
      },
      "answer": {
        "sequence": [
          "rewrite_question",
          "search",
          "build_context",
          "generate",
          "append"
        ]
      },
      "grading": {
        "mode": "subsequence",
        "pass_threshold": 0.6,
        "aliases": [
          [
            "rewrite_question",
            "rewrite",
            "書き換え",
            "standalone"
          ],
          [
            "search",
            "engine.search",
            "検索"
          ],
          [
            "build_context",
            "context",
            "文脈"
          ],
          [
            "generate",
            "生成",
            "llm"
          ],
          [
            "append",
            "履歴保存",
            "保存",
            "store",
            "conversation"
          ]
        ]
      },
      "pool": [
        "build_context",
        "rewrite_question",
        "append",
        "search",
        "generate"
      ],
      "explanation": "follow-upを standalone に書き換え→検索→文脈構築→（元質問で）生成→履歴に保存。検索は書き換え後、生成は元質問。"
    },
    {
      "id": "q_rev_003",
      "difficulty": "L2",
      "type": "freetext",
      "module": "backend/src/api.py",
      "tags": [
        "reverse",
        "startup"
      ],
      "prompt": "次のコードの役割を一文で説明せよ:\n\n    @asynccontextmanager\n    async def lifespan(app):\n        # build graph, engine, stores\n        yield\n        # dispose",
      "citation": {
        "file": "backend/src/api.py",
        "symbol": {
          "kind": "function",
          "name": "lifespan"
        },
        "evidence_substring": "build graph, engine, stores",
        "adr_ref": null,
        "snippet": "@asynccontextmanager\nasync def lifespan(app): # build graph, engine, stores at startup; yield; dispose at shutdown",
        "source": "async def lifespan(app: FastAPI) -> AsyncIterator[None]:\n    configure_logging()\n    logger.info(\"Initializing axis-knowledge-rag API...\")\n    store = VectorStore(path=settings.chroma_db_path)\n    app_cfg = load_app_config()\n    embedder = make_embedder(app_cfg.embedder)\n    normalizer = Normalizer.from_config(load_axes_config())\n    # spec_051 HIGH-1: detect embedder ↔ index dim mismatch at startup.\n    # Empty store (probe returns None) skips silently — first ingest will\n    # set the dim. A mismatch is fatal because every subsequent query\n    # would crash inside Chroma with an opaque shape error.\n    try:\n        store_dim = store.probe_dim()\n    except Exception as e:  # noqa: BLE001\n        logger.warning(\"dim verification skipped: %s\", e)\n        store_dim = None\n    if store_dim is not None and store_dim != embedder.dim:\n        raise RuntimeError(\n            f\"Embedding dim mismatch: chroma store has dim={store_dim}, \"\n            f\"embedder ({type(embedder).__name__}) reports dim={embedder.dim}. \"\n            f\"config.yml で embedder.backend を変更した場合は \"\n            f\"`PYTHONPATH=. python3 -m scripts.build_index examples/knowledge --rebuild` \"\n            f\"を実行して index を rebuild してください。\"\n        )\n    pd = app_cfg.retrieval.parent_doc\n    if pd.enabled and not store.has_parents():\n        logger.warning(\n            \"parent_doc.enabled=true but parents.json is missing — falling back \"\n            \"to legacy file-level search. Run scripts/build_index.py --rebuild \"\n            \"--mode parent_doc to populate it.\"\n        )\n        parent_doc_enabled = False\n    else:\n        parent_doc_enabled = pd.enabled\n    graph: KnowledgeGraph | None = None\n    if app_cfg.graph.enabled:\n        # spec_042 LOW #5: building the graph walks the knowledge dir + parses\n        # frontmatter, which can stall lifespan for seconds on 1000+ doc\n        # corpora. Run it on the default executor so the event loop stays\n        # responsive (e.g. for liveness probes).\n        loop = asyncio.get_running_loop()\n        try:\n            graph = await loop.run_in_executor(\n                None, build_default_graph, app_cfg.graph.knowledge_dir\n            )\n            logger.info(\"knowledge graph stats: %s\", graph.stats())\n        except Exception as e:  # noqa: BLE001\n            logger.warning(\"failed to build knowledge graph: %s — disabling /api/graph\", e)\n            graph = None\n    # spec_048: build the gap store first so both the search engine\n    # (no_results / low_score) and the RAG pipeline (llm_no_info) can\n    # share the same backing SQLite. Disabled config → both hooks become\n    # no-ops without any further conditionals at the call sites.\n    gap_store = make_gap_store(app_cfg.gap)\n    if gap_store is not None:\n        logger.info(\"gap store: %s\", type(gap_store).__name__)\n    else:\n        logger.info(\"gap store: disabled (gap.enabled=false)\")\n    engine = SearchEngine(\n        store,\n        embedder,\n        normalizer,\n        parent_doc_enabled=parent_doc_enabled,\n        top_k_children=pd.top_k_children,\n        graph=graph,\n        gap_store=gap_store,\n        gap_low_score_threshold=app_cfg.gap.low_score_threshold,\n    )\n    rag = RAGPipeline(\n        engine,\n        context_max_chars=app_cfg.rag.context_max_chars,\n        backend=make_generation_backend(app_cfg.generation),\n        gap_store=gap_store,\n    )\n    chat_store = make_conversation_store(app_cfg.chat)\n    configure_default_store(chat_store)\n    logger.info(\n        \"chat store: %s (backend=%s)\",\n        type(chat_store).__name__,\n        app_cfg.chat.storage.backend,\n    )\n    feedback_store = make_feedback_store(app_cfg.feedback)\n    if feedback_store is not None:\n        logger.info(\"feedback store: %s\", type(feedback_store).__name__)\n    else:\n        logger.info(\"feedback store: disabled (feedback.enabled=false)\")\n    _state[\"engine\"] = engine\n    _state[\"rag\"] = rag\n    _state[\"embedder\"] = embedder\n    # spec_056: keep the live VectorStore + Normalizer so /api/ingest/memo can\n    # add chunks against the same collection the engine is querying — drop+\n    # recreate would invalidate the engine's collection handle.\n    _state[\"store\"] = store\n    _state[\"normalizer\"] = normalizer\n    _state[\"retrieval_cfg\"] = app_cfg.retrieval\n    _state[\"axes_cfg\"] = load_axes_config()\n    _state[\"chat_store\"] = chat_store\n    _state[\"chat_cfg\"] = app_cfg.chat\n    _state[\"graph\"] = graph\n    _state[\"graph_cfg\"] = app_cfg.graph\n    _state[\"knowledge_dir\"] = app_cfg.graph.knowledge_dir\n    _state[\"feedback_store\"] = feedback_store\n    _state[\"feedback_cfg\"] = app_cfg.feedback\n    _state[\"gap_store\"] = gap_store\n    _state[\"gap_cfg\"] = app_cfg.gap\n    try:\n        yield\n    finally:\n        try:\n            chat_store.close()\n        except Exception:  # noqa: BLE001\n            logger.warning(\"chat store close failed\", exc_info=True)\n        if feedback_store is not None:\n            try:\n                feedback_store.close()\n            except Exception:  # noqa: BLE001\n                logger.warning(\"feedback store close failed\", exc_info=True)\n        if gap_store is not None:\n            try:\n                gap_store.close()\n            except Exception:  # noqa: BLE001\n                logger.warning(\"gap store close failed\", exc_info=True)\n        _state.clear()",
        "start_line": 68,
        "end_line": 190
      },
      "answer": {
        "model_answer": "FastAPI 起動時にグラフ・検索エンジン・各ストアを1度だけ構築し、終了時に後始末する起動/終了フック。",
        "required_keywords": [
          {
            "any_of": [
              "起動",
              "startup",
              "立ち上げ",
              "開始"
            ]
          },
          {
            "any_of": [
              "1度",
              "一度",
              "1回",
              "once",
              "初期化",
              "構築"
            ]
          },
          {
            "any_of": [
              "グラフ",
              "エンジン",
              "ストア",
              "engine",
              "store"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.66
      },
      "explanation": "逆向き問題：lifespan＝起動時1回の初期化＋終了時後始末、という役割を言語化させる。"
    },
    {
      "id": "q_rev_004",
      "difficulty": "L2",
      "type": "freetext",
      "module": "frontend/src/lib/graphClient.ts",
      "tags": [
        "reverse",
        "graphrag"
      ],
      "prompt": "次の型の in_degree / out_degree は何を表すか説明せよ:\n\n    interface GraphNode { id; title; axes; in_degree; out_degree }",
      "citation": {
        "file": "frontend/src/lib/graphClient.ts",
        "symbol": {
          "kind": "interface",
          "name": "GraphNode"
        },
        "evidence_substring": "in_degree",
        "adr_ref": "ADR-024",
        "snippet": "export interface GraphNode { id: string; title: string; axes; in_degree: number; out_degree: number }"
      },
      "answer": {
        "model_answer": "in_degree=その文書が他から参照されている数（被参照/入次数）、out_degree=その文書が refs で参照している数（参照/出次数）。",
        "required_keywords": [
          {
            "any_of": [
              "入次数",
              "被参照",
              "参照されている",
              "in"
            ]
          },
          {
            "any_of": [
              "出次数",
              "参照している",
              "refs",
              "out"
            ]
          },
          {
            "any_of": [
              "参照",
              "リンク",
              "辺",
              "エッジ",
              "edge"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.66
      },
      "explanation": "逆向き問題：有向グラフの入次数/出次数＝被参照数/参照数。ノードサイズ(val)にも使われる。"
    },
    {
      "id": "q_ing_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/ingester.py",
      "tags": [
        "ingester"
      ],
      "prompt": "ingester.py の役割は？",
      "citation": {
        "file": "backend/src/ingester.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "raw text → axis-knowledge-rag YAML frontmatter Markdown",
        "adr_ref": null,
        "snippet": "AI-powered ingester: raw text → axis-knowledge-rag YAML frontmatter Markdown.\nReuses the Claude API client pattern from rag.py."
      },
      "answer": {
        "options": [
          "ベクトル索引の構築",
          "生テキストをAIで YAML frontmatter付きMarkdown に変換する",
          "APIの認証",
          "グラフ描画"
        ],
        "correct_index": 1
      },
      "explanation": "Slack抜粋・議事録などの生メモを、axis のナレッジ形式（YAML+Markdown）へAI変換する入口。"
    },
    {
      "id": "q_ing_002",
      "difficulty": "L4",
      "type": "mcq",
      "module": "backend/src/ingester.py",
      "tags": [
        "ingester",
        "design"
      ],
      "prompt": "ANTHROPIC_API_KEY 未設定時の Ingester の挙動と、その設計意図は？",
      "citation": {
        "file": "backend/src/ingester.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "falls back to a deterministic mock",
        "adr_ref": null,
        "snippet": "When ANTHROPIC_API_KEY is not set (or force_dummy=True), falls back to a deterministic mock so the CLI and tests run offline."
      },
      "answer": {
        "options": [
          "例外を投げて停止する",
          "決定的なモックにフォールバックし、CLI/テストがオフラインで動くようにする",
          "Geminiに自動切替する",
          "空ファイルを出力する"
        ],
        "correct_index": 1
      },
      "explanation": "embedder の DUMMY と同じ思想：キー無しでも構造確認・CI が回る。決定的＝テストが安定。"
    },
    {
      "id": "q_ing_003",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/ingester.py",
      "tags": [
        "ingester"
      ],
      "prompt": "knowledge ディレクトリを見て次の doc ID を採番する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/ingester.py",
        "symbol": {
          "kind": "function",
          "name": "_next_doc_id"
        },
        "evidence_substring": "def _next_doc_id",
        "adr_ref": null,
        "snippet": "def _next_doc_id(knowledge_dir: Path) -> str:\n    # scan existing doc_NNN ids and return the next one",
        "source": "def _next_doc_id(knowledge_dir: Path) -> str:\n    \"\"\"Back-compat wrapper that returns only the next id from `_scan_knowledge_dir`.\"\"\"\n    return _scan_knowledge_dir(knowledge_dir)[0]",
        "start_line": 79,
        "end_line": 81
      },
      "answer": {
        "file": "backend/src/ingester.py",
        "symbol": "_next_doc_id"
      },
      "grading": {
        "symbol_aliases": [
          "_next_doc_id",
          "next_doc_id"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "_scan_knowledge_dir / _existing_doc_ids と連携して doc_011 のような連番を払い出す。"
    },
    {
      "id": "q_web_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/ingest_web.py",
      "tags": [
        "browser-extension",
        "ingest"
      ],
      "prompt": "ingest_web.py（POST /api/ingest の裏側）は何をどう保存する？",
      "citation": {
        "file": "backend/src/ingest_web.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "web_YYYYMMDD_HHMMSS",
        "adr_ref": "ADR-027",
        "snippet": "the browser extension sends URL + title + body (and optionally a user-selected snippet), and we persist a new examples/knowledge/web_YYYYMMDD_HHMMSS_<slug>.md file that the existing indexer can pick up untouched."
      },
      "answer": {
        "options": [
          "DBに直接INSERTする",
          "拡張から来た URL+title+body を web_日時_<slug>.md として knowledge ディレクトリに保存する",
          "S3にアップロードする",
          "メモリにのみ保持"
        ],
        "correct_index": 1
      },
      "explanation": "ファイルとして置くだけで既存のインデクサがそのまま拾える、という疎結合な設計（spec_046）。"
    },
    {
      "id": "q_web_002",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/ingest_web.py",
      "tags": [
        "browser-extension"
      ],
      "prompt": "キャプチャしたWebページをYAML+Markdownとして書き出す本体関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/ingest_web.py",
        "symbol": {
          "kind": "function",
          "name": "save_web_page"
        },
        "evidence_substring": "def save_web_page",
        "adr_ref": "ADR-027",
        "snippet": "def save_web_page(...):\n    # build frontmatter via _yaml_dump, slugify filename, write to knowledge dir",
        "source": "def save_web_page(\n    *,\n    url: str,\n    title: str,\n    body: str,\n    selected_text: str | None = None,\n    knowledge_dir: str | Path = \"./examples/knowledge\",\n    now: datetime | None = None,\n) -> Path:\n    \"\"\"Persist a captured web page as a YAML-frontmatter Markdown file.\n\n    Filename: ``web_YYYYMMDD_HHMMSS_<slug>.md`` (UTC). ``selected_text``,\n    when provided and non-empty, takes priority over ``body`` so the user's\n    deliberate selection wins over the full-page dump.\n    \"\"\"\n\n    knowledge_dir = Path(knowledge_dir)\n    knowledge_dir.mkdir(parents=True, exist_ok=True)\n    captured_at = (now or datetime.now(UTC)).astimezone(UTC)\n    slug = _slugify(title or \"\")\n    fname = f\"web_{captured_at:%Y%m%d_%H%M%S}_{slug}.md\"\n    path = knowledge_dir / fname\n\n    selected = (selected_text or \"\").strip()\n    main_content = selected if selected else (body or \"\").strip()\n\n    frontmatter: dict[str, Any] = {\n        \"id\": path.stem,\n        \"title\": title,\n        \"source\": \"browser-extension\",\n        \"url\": url,\n        \"captured_at\": captured_at.isoformat(),\n        \"axes\": {\n            \"category\": \"Web\",\n            \"topic\": \"未分類\",\n            \"level\": \"中級\",\n        },\n        \"refs\": [],\n    }\n    fm = _yaml_dump(frontmatter)\n    md = f\"---\\n{fm}\\n---\\n\\n# {title}\\n\\nsource: {url}\\n\\n{main_content}\\n\"\n    path.write_text(md, encoding=\"utf-8\")\n    return path",
        "start_line": 85,
        "end_line": 127
      },
      "answer": {
        "file": "backend/src/ingest_web.py",
        "symbol": "save_web_page"
      },
      "grading": {
        "symbol_aliases": [
          "save_web_page"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "_slugify / _yaml_dump を使ってファイル名とfrontmatterを組み立てて保存する。"
    },
    {
      "id": "q_yml_001",
      "difficulty": "L1",
      "type": "location",
      "module": "scripts/yamlize.py",
      "tags": [
        "cli",
        "ingester"
      ],
      "prompt": "生テキスト1ファイルをYAML付きMarkdownへ変換するCLI（python -m scripts.yamlize）の実装ファイルは？",
      "citation": {
        "file": "scripts/yamlize.py",
        "symbol": {
          "kind": "function",
          "name": "main"
        },
        "evidence_substring": "python -m scripts.yamlize",
        "adr_ref": null,
        "snippet": "CLI: convert a single raw text file to YAML-frontmatter Markdown.\nUsage: python -m scripts.yamlize input.txt --suggested-category 議事録",
        "source": "def main(argv: list[str]) -> int:\n    configure_logging()\n    p = argparse.ArgumentParser(description=\"raw text → YAML-frontmatter Markdown (axis-knowledge-rag)\")\n    p.add_argument(\"input\", help=\"Path to raw text file, or '-' for stdin\")\n    p.add_argument(\"--output\", \"-o\", help=\"Output Markdown file path. Default: stdout\")\n    p.add_argument(\"--knowledge-dir\", default=\"./examples/knowledge\")\n    p.add_argument(\"--suggested-category\", default=None)\n    p.add_argument(\"--max-tokens\", type=int, default=1500)\n    p.add_argument(\"--force-dummy\", action=\"store_true\", help=\"Force DUMMY mode even if ANTHROPIC_API_KEY is set\")\n    args = p.parse_args(argv[1:])\n\n    raw = sys.stdin.read() if args.input == \"-\" else Path(args.input).read_text(encoding=\"utf-8\")\n\n    opts = IngestOptions(\n        knowledge_dir=args.knowledge_dir,\n        suggested_category=args.suggested_category,\n        max_tokens=args.max_tokens,\n    )\n    ingester = Ingester(force_dummy=args.force_dummy)\n    result = ingester.ingest(raw, opts)\n    md = render_markdown(result)\n\n    if args.output:\n        Path(args.output).write_text(md, encoding=\"utf-8\")\n        print(\n            f\"[ok] wrote {args.output} (id={result.id}, title={result.title!r})\",\n            file=sys.stderr,\n        )\n    else:\n        sys.stdout.write(md)\n    return 0",
        "start_line": 21,
        "end_line": 51
      },
      "answer": {
        "file": "scripts/yamlize.py",
        "symbol": "main"
      },
      "grading": {
        "symbol_aliases": [
          "main",
          "yamlize"
        ],
        "accept_file_only_partial": 0.7
      },
      "explanation": "単発変換のCLI。バッチ版は scripts/yamlize_dir.py。中身は Ingester を呼ぶ薄いラッパ。"
    },
    {
      "id": "q_sch_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/schemas.py",
      "tags": [
        "api",
        "schema"
      ],
      "prompt": "backend/src/schemas.py の役割と、フロントエンドとの関係は？",
      "citation": {
        "file": "backend/src/schemas.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "Pydantic schemas for the FastAPI layer",
        "adr_ref": null,
        "snippet": "Pydantic schemas for the FastAPI layer.\nclass SearchRequest(BaseModel): ...\nclass SearchResponse(BaseModel): ...  # frontend/src/lib/api.ts mirrors these types"
      },
      "answer": {
        "options": [
          "DBのテーブル定義",
          "FastAPI のリクエスト/レスポンス型（Pydantic）。frontend の api.ts がこれをミラーする",
          "CSSの型",
          "テストのfixture"
        ],
        "correct_index": 1
      },
      "explanation": "API境界の型を一元定義。フロントのTS型はこれを手動ミラーし、双方の契約を揃える。"
    },
    {
      "id": "q_cfg_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/config.py",
      "tags": [
        "config"
      ],
      "prompt": "config.py の設定はどう構成されている？",
      "citation": {
        "file": "backend/src/config.py",
        "symbol": {
          "kind": "module",
          "name": "dataclasses"
        },
        "evidence_substring": "@dataclass(frozen=True)",
        "adr_ref": null,
        "snippet": "@dataclass(frozen=True)\nclass ParentDocConfig: ...\n@dataclass(frozen=True)\nclass TimeDecayConfig: ...\n@dataclass(frozen=True)\nclass ChatConfig: ...\n@dataclass(frozen=True)\nclass GenerationConfig: ...  # 機能ごとの小さなconfigを AppConfig が束ねる"
      },
      "answer": {
        "options": [
          "1つの巨大dict",
          "機能ごとの frozen dataclass（ParentDoc/TimeDecay/Chat/Generation等）を AppConfig が束ねる",
          "環境変数のみ",
          "XMLファイル"
        ],
        "correct_index": 1
      },
      "explanation": "config.yml を _build_app_config が機能別の不変dataclassに詰める。型安全＋意図しない書き換え防止。"
    },
    {
      "id": "q_cfg_002",
      "difficulty": "L2",
      "type": "fill_blank",
      "module": "backend/src/config.py",
      "tags": [
        "config",
        "python"
      ],
      "prompt": "設定dataclassを不変（書き換え不可）にしているデコレータ引数の空欄を埋めよ:\n    @dataclass(____=True)",
      "citation": {
        "file": "backend/src/config.py",
        "symbol": {
          "kind": "class",
          "name": "ParentDocConfig"
        },
        "evidence_substring": "@dataclass(frozen=True)",
        "adr_ref": null,
        "snippet": "@dataclass(frozen=True)\nclass ParentDocConfig:",
        "source": "class ParentDocConfig:\n    \"\"\"Settings for the small-to-big (parent / child) retrieval path.\"\"\"\n\n    enabled: bool = True\n    chunk_strategy: str = \"h2\"  # currently only \"h2\" is implemented\n    max_child_tokens: int = 256\n    top_k_children: int = 20\n    top_n_parents: int = 5\n    storage: str = \"sqlite\"  # spec_037: \"sqlite\" (default) | \"json\" (legacy v0.7)",
        "start_line": 56,
        "end_line": 64
      },
      "answer": {
        "accepted": [
          "frozen"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "frozen=True で属性代入が例外に。設定が実行中に書き換わる事故を型レベルで防ぐ。"
    },
    {
      "id": "q_integ_002",
      "difficulty": "L2",
      "type": "location",
      "module": "backend/src/integrity.py",
      "tags": [
        "integrity",
        "graph"
      ],
      "prompt": "refs の循環参照を検出する関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/integrity.py",
        "symbol": {
          "kind": "function",
          "name": "_find_cycles"
        },
        "evidence_substring": "def _find_cycles",
        "adr_ref": null,
        "snippet": "def _find_cycles(graph: dict[str, list[str]]) -> list[list[str]]:\n    \"\"\"Simple DFS cycle detection. Returns a list of cycle paths.\"\"\"",
        "source": "def _find_cycles(graph: dict[str, list[str]]) -> list[list[str]]:\n    \"\"\"Simple DFS cycle detection. Returns a list of cycle paths.\"\"\"\n    WHITE, GRAY, BLACK = 0, 1, 2\n    color: dict[str, int] = defaultdict(int)\n    cycles: list[list[str]] = []\n    stack: list[str] = []\n\n    def visit(node: str) -> None:\n        if color[node] == GRAY:\n            # Found cycle: rewind stack until we find node again\n            idx = stack.index(node)\n            cycles.append(stack[idx:] + [node])\n            return\n        if color[node] == BLACK:\n            return\n        color[node] = GRAY\n        stack.append(node)\n        for nxt in graph.get(node, []):\n            visit(nxt)\n        stack.pop()\n        color[node] = BLACK\n\n    for n in list(graph.keys()):\n        if color[n] == WHITE:\n            visit(n)\n    return cycles",
        "start_line": 50,
        "end_line": 75
      },
      "answer": {
        "file": "backend/src/integrity.py",
        "symbol": "_find_cycles"
      },
      "grading": {
        "symbol_aliases": [
          "_find_cycles",
          "find_cycles"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "IntegrityChecker.check が refs グラフに対して呼ぶ。DFSベースの循環検出。"
    },
    {
      "id": "q_integ_003",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/integrity.py",
      "tags": [
        "integrity",
        "algorithm"
      ],
      "prompt": "_find_cycles が使っているアルゴリズムは？",
      "citation": {
        "file": "backend/src/integrity.py",
        "symbol": {
          "kind": "function",
          "name": "_find_cycles"
        },
        "evidence_substring": "WHITE, GRAY, BLACK = 0, 1, 2",
        "adr_ref": null,
        "snippet": "WHITE, GRAY, BLACK = 0, 1, 2\n# DFS: GRAY(訪問中)のノードに再訪したら循環。stackを巻き戻して循環パスを記録。",
        "source": "def _find_cycles(graph: dict[str, list[str]]) -> list[list[str]]:\n    \"\"\"Simple DFS cycle detection. Returns a list of cycle paths.\"\"\"\n    WHITE, GRAY, BLACK = 0, 1, 2\n    color: dict[str, int] = defaultdict(int)\n    cycles: list[list[str]] = []\n    stack: list[str] = []\n\n    def visit(node: str) -> None:\n        if color[node] == GRAY:\n            # Found cycle: rewind stack until we find node again\n            idx = stack.index(node)\n            cycles.append(stack[idx:] + [node])\n            return\n        if color[node] == BLACK:\n            return\n        color[node] = GRAY\n        stack.append(node)\n        for nxt in graph.get(node, []):\n            visit(nxt)\n        stack.pop()\n        color[node] = BLACK\n\n    for n in list(graph.keys()):\n        if color[n] == WHITE:\n            visit(n)\n    return cycles",
        "start_line": 50,
        "end_line": 75
      },
      "answer": {
        "options": [
          "BFSで最短経路",
          "3色（WHITE/GRAY/BLACK）のDFS：訪問中(GRAY)ノードへの再訪＝循環",
          "Union-Find",
          "トポロジカルソートのみ"
        ],
        "correct_index": 1
      },
      "explanation": "古典的な3色DFS。GRAY=現在の探索パス上、そこに戻ってきたら循環としてパスを記録。"
    },
    {
      "id": "q_rev_005",
      "difficulty": "L3",
      "type": "freetext",
      "module": "backend/src/integrity.py",
      "tags": [
        "reverse",
        "algorithm"
      ],
      "prompt": "次のコードが何をしているか、GRAY の意味に触れつつ説明せよ:\n\n    color[node] = GRAY\n    stack.append(node)\n    for nxt in graph.get(node, []):\n        visit(nxt)   # color[nxt]==GRAY なら循環として記録\n    stack.pop()\n    color[node] = BLACK",
      "citation": {
        "file": "backend/src/integrity.py",
        "symbol": {
          "kind": "function",
          "name": "_find_cycles"
        },
        "evidence_substring": "color[node] = GRAY",
        "adr_ref": null,
        "snippet": "color[node] = GRAY\nstack.append(node)\nfor nxt in graph.get(node, []):\n    visit(nxt)\nstack.pop()\ncolor[node] = BLACK  # GRAY=on current DFS path; revisiting GRAY means a cycle",
        "source": "def _find_cycles(graph: dict[str, list[str]]) -> list[list[str]]:\n    \"\"\"Simple DFS cycle detection. Returns a list of cycle paths.\"\"\"\n    WHITE, GRAY, BLACK = 0, 1, 2\n    color: dict[str, int] = defaultdict(int)\n    cycles: list[list[str]] = []\n    stack: list[str] = []\n\n    def visit(node: str) -> None:\n        if color[node] == GRAY:\n            # Found cycle: rewind stack until we find node again\n            idx = stack.index(node)\n            cycles.append(stack[idx:] + [node])\n            return\n        if color[node] == BLACK:\n            return\n        color[node] = GRAY\n        stack.append(node)\n        for nxt in graph.get(node, []):\n            visit(nxt)\n        stack.pop()\n        color[node] = BLACK\n\n    for n in list(graph.keys()):\n        if color[n] == WHITE:\n            visit(n)\n    return cycles",
        "start_line": 50,
        "end_line": 75
      },
      "answer": {
        "model_answer": "DFSで探索中のノードをGRAY（現在のパス上）に塗り、子を辿ってGRAYに再訪したら循環として記録。探索が終わったらBLACKにして二度と見ない。",
        "required_keywords": [
          {
            "any_of": [
              "dfs",
              "深さ優先",
              "探索"
            ]
          },
          {
            "any_of": [
              "循環",
              "サイクル",
              "cycle"
            ]
          },
          {
            "any_of": [
              "訪問中",
              "パス上",
              "gray",
              "再訪",
              "戻って"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.66
      },
      "explanation": "逆向き×アルゴリズム読解。GRAY=現在の探索パス、への再訪が循環の証拠、という核を言語化させる。"
    },
    {
      "id": "q_cc_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "frontend/src/lib/chatClient.ts",
      "tags": [
        "frontend",
        "conversational-rag"
      ],
      "prompt": "チャット画面をリロードしても会話が続くのはなぜ？",
      "citation": {
        "file": "frontend/src/lib/chatClient.ts",
        "symbol": {
          "kind": "module",
          "name": "module header"
        },
        "evidence_substring": "keeps\n// the active session_id in localStorage",
        "adr_ref": "ADR-022",
        "snippet": "// Chat API client. Wraps /api/chat for the conversational RAG path and keeps\n// the active session_id in localStorage so reloading /chat preserves the conversation."
      },
      "answer": {
        "options": [
          "Cookieに全履歴を保存",
          "session_id を localStorage に保持し、サーバ側ストアの履歴を同じセッションで引き続き使う",
          "URLに履歴を埋め込む",
          "リロードでは続かない"
        ],
        "correct_index": 1
      },
      "explanation": "クライアントはIDだけ保持、履歴本体はサーバ側 ConversationStore（sqlite）。リロード→同じsession_idでPOST。"
    },
    {
      "id": "q_cc_002",
      "difficulty": "L2",
      "type": "fill_blank",
      "module": "frontend/src/lib/chatClient.ts",
      "tags": [
        "frontend"
      ],
      "prompt": "chatClient が session_id 保存に使う localStorage キーは？\n    const STORAGE_KEY = ____;",
      "citation": {
        "file": "frontend/src/lib/chatClient.ts",
        "symbol": {
          "kind": "const",
          "name": "STORAGE_KEY"
        },
        "evidence_substring": "STORAGE_KEY = \"axis-chat-session-id\"",
        "adr_ref": null,
        "snippet": "const STORAGE_KEY = \"axis-chat-session-id\";",
        "source": "const STORAGE_KEY = \"axis-chat-session-id\";\n\nexport interface ChatRequest {\n  question: string;\n  session_id?: string | null;\n  filters?: Record<string, string | number>;\n  top_k?: number;\n}\n\nexport interface ChatResponse {\n  session_id: string;\n  question: string;\n  rewritten_question: string | null;\n  answer: string;\n  cited_ids: string[];\n  sources: SearchResultPayload[];\n  is_dummy: boolean;\n  model: string | null;\n}\n\nexport async function postChat(body: ChatRequest): Promise<ChatResponse> {\n  const res = await fetch(`${API_BASE}/api/chat`, {\n    method: \"POST\",\n    headers: { \"Content-Type\": \"application/json\" },",
        "start_line": 8,
        "end_line": 31
      },
      "answer": {
        "accepted": [
          "axis-chat-session-id",
          "\"axis-chat-session-id\""
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "axis-chat-session-id。明示リセットされるまで同じ会話を継続する。"
    },
    {
      "id": "q_fbc_001",
      "difficulty": "L1",
      "type": "location",
      "module": "frontend/src/lib/feedbackClient.ts",
      "tags": [
        "frontend",
        "feedback"
      ],
      "prompt": "👍/👎 を /api/feedback に送るフロント関数は？（ファイルとシンボル）",
      "citation": {
        "file": "frontend/src/lib/feedbackClient.ts",
        "symbol": {
          "kind": "function",
          "name": "postFeedback"
        },
        "evidence_substring": "export async function postFeedback",
        "adr_ref": "ADR-028",
        "snippet": "// spec_047: 👍 / 👎 feedback client. Posts to /api/feedback, mirroring schemas.py:FeedbackRequest.\nexport async function postFeedback(payload: FeedbackPayload): Promise<FeedbackResponse> { ... }",
        "source": "export async function postFeedback(\n  payload: FeedbackPayload,\n): Promise<FeedbackResponse> {\n  const res = await fetch(`${API_BASE}/api/feedback`, {\n    method: \"POST\",\n    headers: { \"Content-Type\": \"application/json\" },\n    body: JSON.stringify(payload),\n  });\n  if (!res.ok) {\n    throw new Error(\n      `feedback POST failed: ${res.status} ${await res.text()}`,\n    );\n  }\n  return (await res.json()) as FeedbackResponse;\n}",
        "start_line": 19,
        "end_line": 33
      },
      "answer": {
        "file": "frontend/src/lib/feedbackClient.ts",
        "symbol": "postFeedback"
      },
      "grading": {
        "symbol_aliases": [
          "postFeedback"
        ],
        "accept_file_only_partial": 0.6
      },
      "explanation": "spec_047 のフロント側。バックエンドの FeedbackStore に記録される。"
    },
    {
      "id": "q_fbc_002",
      "difficulty": "L2",
      "type": "mcq",
      "module": "frontend/src/lib/feedbackClient.ts",
      "tags": [
        "frontend",
        "feedback"
      ],
      "prompt": "FeedbackPayload の rating が取り得る値は？",
      "citation": {
        "file": "frontend/src/lib/feedbackClient.ts",
        "symbol": {
          "kind": "interface",
          "name": "FeedbackPayload"
        },
        "evidence_substring": "rating: 1 | -1 | 0",
        "adr_ref": null,
        "snippet": "export interface FeedbackPayload {\n  query?: string | null;\n  doc_id?: string | null;\n  rating: 1 | -1 | 0;\n  session_id?: string | null;\n}"
      },
      "answer": {
        "options": [
          "0〜5 の星",
          "1（👍）/ -1（👎）/ 0（取り消し）",
          "true / false",
          "任意の整数"
        ],
        "correct_index": 1
      },
      "explanation": "リテラル型 1 | -1 | 0 で取り得る値を型レベルで固定。"
    },
    {
      "id": "q_gs_001",
      "difficulty": "L2",
      "type": "location",
      "module": "frontend/src/components/GraphSidebar.tsx",
      "tags": [
        "frontend",
        "graphrag"
      ],
      "prompt": "3Dグラフでノードをクリックすると隣接docを出すサイドバーのコンポーネントは？（ファイルとシンボル）",
      "citation": {
        "file": "frontend/src/components/GraphSidebar.tsx",
        "symbol": {
          "kind": "component",
          "name": "GraphSidebar"
        },
        "evidence_substring": "fetchNeighborsBidirectional",
        "adr_ref": "ADR-032",
        "snippet": "export function GraphSidebar({ docId, onClose }: Props) {\n  useEffect(() => { ... fetchNeighborsBidirectional(docId, 1, 20) ... }, [docId]);",
        "source": "export function GraphSidebar({ docId, onClose }: Props) {\n  const [data, setData] = useState<NeighborSet | null>(null);\n  const [error, setError] = useState<string | null>(null);\n\n  useEffect(() => {\n    if (!docId) {\n      setData(null);\n      setError(null);\n      return;\n    }\n    setData(null);\n    setError(null);\n    let cancelled = false;\n    fetchNeighborsBidirectional(docId, 1, 20)\n      .then((res) => {\n        if (!cancelled) setData(res);\n      })\n      .catch((e) => {\n        if (!cancelled) setError(String(e));\n      });\n    return () => {\n      cancelled = true;\n    };\n  }, [docId]);",
        "start_line": 12,
        "end_line": 35
      },
      "answer": {
        "file": "frontend/src/components/GraphSidebar.tsx",
        "symbol": "GraphSidebar"
      },
      "grading": {
        "symbol_aliases": [
          "GraphSidebar"
        ],
        "accept_file_only_partial": 0.7
      },
      "explanation": "docId を受けて双方向の隣接を取得・表示。レイアウト修正の経緯は ADR-032。"
    },
    {
      "id": "q_gs_002",
      "difficulty": "L2",
      "type": "mcq",
      "module": "frontend/src/components/GraphSidebar.tsx",
      "tags": [
        "frontend",
        "graphrag"
      ],
      "prompt": "fetchNeighborsBidirectional の『双方向（bidirectional）』が指すものは？",
      "citation": {
        "file": "frontend/src/components/GraphSidebar.tsx",
        "symbol": {
          "kind": "function",
          "name": "fetchNeighborsBidirectional"
        },
        "evidence_substring": "fetchNeighborsBidirectional(docId, 1, 20)",
        "adr_ref": "ADR-030",
        "snippet": "fetchNeighborsBidirectional(docId, 1, 20)  // refsで参照している先(out)と、参照されている元(in)の両方の隣接を取得"
      },
      "answer": {
        "options": [
          "上下スクロールの両方向",
          "その doc が参照している先（out）と、参照されている元（in）の両方の隣接",
          "過去と未来のバージョン",
          "日本語と英語"
        ],
        "correct_index": 1
      },
      "explanation": "refs は有向だが、人間の関心は「繋がり全部」。out/in 両方向を出す（ADR-030 bidirectional refs）。"
    },
    {
      "id": "q_oll_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/embedder.py",
      "tags": [
        "embedder",
        "ollama"
      ],
      "prompt": "OllamaEmbedder の既定モデルと埋め込み次元は？",
      "citation": {
        "file": "backend/src/embedder.py",
        "symbol": {
          "kind": "class",
          "name": "OllamaEmbedder"
        },
        "evidence_substring": "bge-m3``, 1024 dim",
        "adr_ref": "ADR-026",
        "snippet": "class OllamaEmbedder:\n    \"\"\"Ollama embedder backend (e.g. ``bge-m3``, 1024 dim).\"\"\"\n    DEFAULT_MODEL = \"bge-m3\"\n    DEFAULT_URL = \"http://localhost:11434\"",
        "source": "class OllamaEmbedder:\n    \"\"\"Ollama embedder backend (e.g. ``bge-m3``, 1024 dim).\n\n    Talks to ``ollama`` via the official Python client. The package is\n    ``optional`` — install with ``pip install -e \".[ollama]\"``. If the\n    import / network call fails at construction, ``make_embedder()`` falls\n    back to ``DummyEmbedder`` and logs a warning.\n    \"\"\"\n\n    DEFAULT_MODEL = \"bge-m3\"\n    DEFAULT_URL = \"http://localhost:11434\"\n\n    def __init__(\n        self,\n        *,\n        model: str = DEFAULT_MODEL,\n        url: str = DEFAULT_URL,\n    ) -> None:\n        import ollama  # raises ImportError if optional extras not installed\n\n        self._client = ollama.Client(host=url)\n        self._model = model\n        self._url = url\n        # Probe once so we expose a stable ``dim`` for downstream code that\n        # relies on it (e.g. vector store dimensionality checks).\n        self._dim = self._probe_dim()\n\n    @property\n    def is_dummy(self) -> bool:\n        return False\n\n    @property\n    def dim(self) -> int:\n        return self._dim\n\n    @property\n    def model_name(self) -> str:\n        return self._model\n\n    def embed(self, text: str) -> list[float]:\n        resp = self._client.embeddings(model=self._model, prompt=text)\n        return list(resp[\"embedding\"])\n\n    def embed_batch(self, texts: Sequence[str]) -> list[list[float]]:\n        # Ollama embeddings endpoint is single-prompt; loop sequentially.\n        return [self.embed(t) for t in texts]\n\n    def _probe_dim(self) -> int:\n        resp = self._client.embeddings(model=self._model, prompt=\"probe\")\n        return len(resp[\"embedding\"])",
        "start_line": 194,
        "end_line": 243
      },
      "answer": {
        "options": [
          "text-embedding-3 / 1536",
          "bge-m3 / 1024次元（URLは localhost:11434）",
          "all-MiniLM / 384",
          "Gemini embedding / 768"
        ],
        "correct_index": 1
      },
      "explanation": "オンプレ経路の既定は bge-m3（1024次元）。Geminiの768と次元が違うため、起動時にプローブして整合を確認。"
    },
    {
      "id": "q_oll_002",
      "difficulty": "L4",
      "type": "mcq",
      "module": "backend/src/embedder.py",
      "tags": [
        "embedder",
        "ollama",
        "design"
      ],
      "prompt": "ollama パッケージを optional 依存にし、構築失敗時に Dummy へフォールバックする設計の狙いは？",
      "citation": {
        "file": "backend/src/embedder.py",
        "symbol": {
          "kind": "class",
          "name": "OllamaEmbedder"
        },
        "evidence_substring": "falls\n    back to ``DummyEmbedder``",
        "adr_ref": "ADR-026",
        "snippet": "The package is ``optional`` — install with ``pip install -e \".[ollama]\"``. If the\n    import / network call fails at construction, ``make_embedder()`` falls\n    back to ``DummyEmbedder`` and logs a warning.",
        "source": "class OllamaEmbedder:\n    \"\"\"Ollama embedder backend (e.g. ``bge-m3``, 1024 dim).\n\n    Talks to ``ollama`` via the official Python client. The package is\n    ``optional`` — install with ``pip install -e \".[ollama]\"``. If the\n    import / network call fails at construction, ``make_embedder()`` falls\n    back to ``DummyEmbedder`` and logs a warning.\n    \"\"\"\n\n    DEFAULT_MODEL = \"bge-m3\"\n    DEFAULT_URL = \"http://localhost:11434\"\n\n    def __init__(\n        self,\n        *,\n        model: str = DEFAULT_MODEL,\n        url: str = DEFAULT_URL,\n    ) -> None:\n        import ollama  # raises ImportError if optional extras not installed\n\n        self._client = ollama.Client(host=url)\n        self._model = model\n        self._url = url\n        # Probe once so we expose a stable ``dim`` for downstream code that\n        # relies on it (e.g. vector store dimensionality checks).\n        self._dim = self._probe_dim()\n\n    @property\n    def is_dummy(self) -> bool:\n        return False\n\n    @property\n    def dim(self) -> int:\n        return self._dim\n\n    @property\n    def model_name(self) -> str:\n        return self._model\n\n    def embed(self, text: str) -> list[float]:\n        resp = self._client.embeddings(model=self._model, prompt=text)\n        return list(resp[\"embedding\"])\n\n    def embed_batch(self, texts: Sequence[str]) -> list[list[float]]:\n        # Ollama embeddings endpoint is single-prompt; loop sequentially.\n        return [self.embed(t) for t in texts]\n\n    def _probe_dim(self) -> int:\n        resp = self._client.embeddings(model=self._model, prompt=\"probe\")\n        return len(resp[\"embedding\"])",
        "start_line": 194,
        "end_line": 243
      },
      "answer": {
        "options": [
          "ライセンス上の制約",
          "Ollamaを使わない人に依存を強制せず、未導入/停止中でもアプリ全体は起動し続けられるようにするため",
          "速度向上",
          "Windows非対応のため"
        ],
        "correct_index": 1
      },
      "explanation": "optional extras + graceful fallback。オンプレ機能は使う人だけ、入れてない/落ちてる時も全体は動く。"
    },
    {
      "id": "q_oll_003",
      "difficulty": "L2",
      "type": "location",
      "module": "backend/src/embedder.py",
      "tags": [
        "embedder",
        "ollama"
      ],
      "prompt": "OllamaEmbedder が起動時に一度だけ埋め込み次元を調べるメソッドは？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/embedder.py",
        "symbol": {
          "kind": "method",
          "name": "OllamaEmbedder._probe_dim"
        },
        "evidence_substring": "self._dim = self._probe_dim()",
        "adr_ref": null,
        "snippet": "# Probe once so we expose a stable ``dim`` for downstream code that\n# relies on it (e.g. vector store dimensionality checks).\nself._dim = self._probe_dim()",
        "source": "    def _probe_dim(self) -> int:\n        resp = self._client.embeddings(model=self._model, prompt=\"probe\")\n        return len(resp[\"embedding\"])",
        "start_line": 241,
        "end_line": 243
      },
      "answer": {
        "file": "backend/src/embedder.py",
        "symbol": "_probe_dim"
      },
      "grading": {
        "symbol_aliases": [
          "_probe_dim",
          "probe_dim"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "モデルにより次元が違う（bge-m3=1024等）ので、1回埋め込んで実次元を確定させ、下流の次元チェックに使う。"
    },
    {
      "id": "q_adr20_001",
      "difficulty": "L4",
      "type": "mcq",
      "module": "docs/adr/ADR-020-citation-highlighting.md",
      "tags": [
        "citation",
        "design"
      ],
      "prompt": "出典マーカーを doc ID（[doc_017]）からインライン [N]（1始まり）へ変えた理由は？（ADR-020）",
      "citation": {
        "file": "docs/adr/ADR-020-citation-highlighting.md",
        "symbol": {
          "kind": "section",
          "name": "Context/Decision"
        },
        "evidence_substring": "Perplexity 風",
        "adr_ref": "ADR-020",
        "snippet": "doc ID は人間が見て直感的に「何番目の出典か」が分からない。ポートフォリオで Perplexity 風の \"クリックして対応する出典に飛ぶ\" 体験を最小コストで実現するため、回答テキストにインライン [N] マーカー (1-indexed) を埋め、UI 側でクリッカブル化する。"
      },
      "answer": {
        "options": [
          "doc IDが長すぎてトークンを消費するから",
          "[N] なら人間が出典リストと直感的に対応づけられ、クリックで該当出典へ飛ぶPerplexity風UXを最小コストで作れるから",
          "LLMがdoc IDを出力できないから",
          "SEOのため"
        ],
        "correct_index": 1
      },
      "explanation": "「doc_017は何番目？」問題の解消。1-indexed の [N] は出典リストの並びと一致し、クリック動線が自然になる。"
    },
    {
      "id": "q_adr20_002",
      "difficulty": "L1",
      "type": "fill_blank",
      "module": "docs/adr/ADR-020-citation-highlighting.md",
      "tags": [
        "citation"
      ],
      "prompt": "インライン引用マーカー [N] の N は何始まり？（数字で）",
      "citation": {
        "file": "docs/adr/ADR-020-citation-highlighting.md",
        "symbol": {
          "kind": "section",
          "name": "Decision"
        },
        "evidence_substring": "1-indexed",
        "adr_ref": "ADR-020",
        "snippet": "回答テキストにインライン [N] マーカー (1-indexed) を埋め、N は出典リストの index と一致させる。"
      },
      "answer": {
        "accepted": [
          "1",
          "1始まり",
          "1-indexed"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "1始まり。0-indexedにしない＝人間の「1番目の出典」という感覚に合わせる。"
    },
    {
      "id": "q_rev_006",
      "difficulty": "L2",
      "type": "freetext",
      "module": "frontend/src/components/GraphSidebar.tsx",
      "tags": [
        "reverse",
        "react"
      ],
      "prompt": "次の useEffect 内のパターンの目的を説明せよ:\n\n    let cancelled = false;\n    fetchNeighborsBidirectional(docId, 1, 20).then((d) => { if (!cancelled) setData(d); });\n    return () => { cancelled = true; };",
      "citation": {
        "file": "frontend/src/components/GraphSidebar.tsx",
        "symbol": {
          "kind": "component",
          "name": "GraphSidebar"
        },
        "evidence_substring": "let cancelled = false",
        "adr_ref": null,
        "snippet": "let cancelled = false;\nfetchNeighborsBidirectional(docId, 1, 20).then((d) => { if (!cancelled) setData(d); });\nreturn () => { cancelled = true; };  // ignore stale async results after unmount / docId change",
        "source": "export function GraphSidebar({ docId, onClose }: Props) {\n  const [data, setData] = useState<NeighborSet | null>(null);\n  const [error, setError] = useState<string | null>(null);\n\n  useEffect(() => {\n    if (!docId) {\n      setData(null);\n      setError(null);\n      return;\n    }\n    setData(null);\n    setError(null);\n    let cancelled = false;\n    fetchNeighborsBidirectional(docId, 1, 20)\n      .then((res) => {\n        if (!cancelled) setData(res);\n      })\n      .catch((e) => {\n        if (!cancelled) setError(String(e));\n      });\n    return () => {\n      cancelled = true;\n    };\n  }, [docId]);",
        "start_line": 12,
        "end_line": 35
      },
      "answer": {
        "model_answer": "docIdが変わる/アンマウント後に届いた古い非同期結果を捨て、stale なデータで state を上書きする競合（race）を防ぐクリーンアップ。",
        "required_keywords": [
          {
            "any_of": [
              "古い",
              "stale",
              "前の",
              "遅れて"
            ]
          },
          {
            "any_of": [
              "無視",
              "捨て",
              "反映しない",
              "上書きしない",
              "キャンセル"
            ]
          },
          {
            "any_of": [
              "非同期",
              "fetch",
              "結果",
              "レース",
              "race",
              "競合",
              "アンマウント"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.66
      },
      "explanation": "逆向き×Reactイディオム。クリーンアップでフラグを立て、遅延して届いた結果のsetStateを抑止する定石。"
    },
    {
      "id": "q_li_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/live_ingest.py",
      "tags": [
        "live-ingest",
        "idempotent"
      ],
      "prompt": "live ingest が取り込み前に必ず VectorStore.delete_doc を呼ぶのはなぜ？",
      "citation": {
        "file": "backend/src/live_ingest.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "re-ingest an upsert",
        "adr_ref": null,
        "snippet": "the only difference is scope (one file) and that we ``delete_doc`` first to make re-ingest an upsert rather than a DuplicateIDError."
      },
      "answer": {
        "options": [
          "容量削減のため",
          "同じdoc_idの再取り込みをupsert化し、重複ID エラーとチャンク二重化を防ぐ（冪等性）",
          "削除ログを残すため",
          "Chromaの仕様で必須だから"
        ],
        "correct_index": 1
      },
      "explanation": "先に消してから入れる＝2回呼んでも結果が同じ（冪等）。DuplicateIDErrorも起きない。"
    },
    {
      "id": "q_li_002",
      "difficulty": "L4",
      "type": "mcq",
      "module": "backend/src/live_ingest.py",
      "tags": [
        "live-ingest",
        "design"
      ],
      "prompt": "live ingest が loader.load_document と chunker.chunk_markdown を“再利用”している設計意図は？",
      "citation": {
        "file": "backend/src/live_ingest.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "parent/child structure stays identical",
        "adr_ref": null,
        "snippet": "Reuses ``loader.load_document`` + ``chunker.chunk_markdown`` so the parent/child structure stays identical to a full rebuild — the only difference is scope (one file)."
      },
      "answer": {
        "options": [
          "コード行数を減らすためだけ",
          "フルリビルドと完全に同じparent/child構造を保証し、ライブ取り込み分だけ構造が違う事故を防ぐため",
          "速度が10倍になるから",
          "テストを書かなくて済むから"
        ],
        "correct_index": 1
      },
      "explanation": "経路が違っても成果物の構造は同一、が肝。差は範囲（1ファイル）と事前delete_docだけ。"
    },
    {
      "id": "q_li_003",
      "difficulty": "L1",
      "type": "location",
      "module": "backend/src/live_ingest.py",
      "tags": [
        "live-ingest"
      ],
      "prompt": "単一Markdownファイルを稼働中の索引へ取り込む関数は？（ファイルとシンボル）",
      "citation": {
        "file": "backend/src/live_ingest.py",
        "symbol": {
          "kind": "function",
          "name": "ingest_file"
        },
        "evidence_substring": "def ingest_file",
        "adr_ref": null,
        "snippet": "def ingest_file(path, *, store, embedder, normalizer, max_child_tokens=256) -> LiveIngestResult:\n    \"\"\"Load, chunk, embed one Markdown file and merge into the running store.\"\"\"",
        "source": "def ingest_file(\n    path: Path,\n    *,\n    store: VectorStore,\n    embedder: Embedder,\n    normalizer: Normalizer,\n    max_child_tokens: int = 256,\n) -> LiveIngestResult:\n    \"\"\"Load, chunk, embed one Markdown file and merge into the running store.\n\n    Idempotent — calling twice with the same ``doc_id`` does not double the\n    chunk count because ``VectorStore.delete_doc`` runs first.\n\n    The frontmatter dict handed to ``chunk_markdown`` mirrors what\n    ``scripts.build_index`` passes for parent_doc mode so child metadata\n    (``axes`` / ``normalized_axes`` / ``title``) stays consistent.\n    \"\"\"\n    doc = load_document(path, normalizer=normalizer)\n\n    fm = {\n        \"title\": doc.title,\n        \"axes\": doc.axes,\n        \"normalized_axes\": doc.normalized_axes,\n        \"tags\": doc.tags,\n        \"refs\": doc.refs,\n        \"path\": str(doc.path),\n    }\n    parents, children = chunk_markdown(\n        doc.id, doc.body, fm, max_child_tokens=max_child_tokens\n    )\n    deleted = store.delete_doc(doc.id)\n\n    if not children:\n        # Empty body — still surface the parent so re-ingest stays consistent.\n        store.add_chunks(parents, [], [])\n        return LiveIngestResult(\n            doc_id=doc.id, parents=len(parents), children=0, deleted_existing=deleted\n        )\n\n    embeddings = embedder.embed_batch([normalizer(c.text) for c in children])\n    store.add_chunks(parents, children, embeddings)\n    logger.info(\n        \"live ingest: %s → %d parents / %d children (deleted_existing=%d)\",\n        doc.id, len(parents), len(children), deleted,\n    )\n    return LiveIngestResult(\n        doc_id=doc.id,\n        parents=len(parents),\n        children=len(children),\n        deleted_existing=deleted,\n    )",
        "start_line": 34,
        "end_line": 84
      },
      "answer": {
        "file": "backend/src/live_ingest.py",
        "symbol": "ingest_file"
      },
      "grading": {
        "symbol_aliases": [
          "ingest_file"
        ],
        "accept_file_only_partial": 0.5
      },
      "explanation": "/api/ingest/memo と /api/ingest の両方がこれを使う。戻り値は LiveIngestResult。"
    },
    {
      "id": "q_bi_001",
      "difficulty": "L2",
      "type": "mcq",
      "module": "scripts/build_index.py",
      "tags": [
        "build-index",
        "config"
      ],
      "prompt": "build_index の --mode auto（既定）は何を見てモードを決める？",
      "citation": {
        "file": "scripts/build_index.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "pick based on ``config.yml``",
        "adr_ref": null,
        "snippet": "``--mode auto`` (default): pick based on ``config.yml`` → ``retrieval.parent_doc.enabled``."
      },
      "answer": {
        "options": [
          "環境変数 MODE",
          "config.yml の retrieval.parent_doc.enabled",
          "ファイル数が100以上か",
          "GPUの有無"
        ],
        "correct_index": 1
      },
      "explanation": "configがparent_doc有効ならparent_docモード、無効ならlegacy（ファイル単位）。"
    },
    {
      "id": "q_bi_002",
      "difficulty": "L2",
      "type": "fill_blank",
      "module": "scripts/build_index.py",
      "tags": [
        "build-index"
      ],
      "prompt": "v0.6挙動（ファイル単位embedding）で索引を作るモード名は？\n    --mode ____ : file-level embedding (v0.6 behavior)",
      "citation": {
        "file": "scripts/build_index.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "--mode legacy``: file-level embedding",
        "adr_ref": null,
        "snippet": "* ``--mode legacy``: file-level embedding (v0.6 behavior).\n* ``--mode parent_doc``: chunk each .md into H2-section parents + children."
      },
      "answer": {
        "accepted": [
          "legacy"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "legacy=ファイル丸ごと、parent_doc=H2親＋子チャンク。後方互換のために両方残す。"
    },
    {
      "id": "q_df_build",
      "difficulty": "L3",
      "type": "dataflow",
      "module": "scripts/build_index.py",
      "tags": [
        "build-index",
        "pipeline"
      ],
      "prompt": "build_index（parent_docモード）が索引を作るまでの主要ステップを順に並べよ（候補: load_directory, chunk_markdown, embed(children), add_chunks）。",
      "citation": {
        "file": "scripts/build_index.py",
        "symbol": {
          "kind": "module",
          "name": "module docstring"
        },
        "evidence_substring": "chunk each .md into H2-section parents",
        "adr_ref": "ADR-017",
        "snippet": "``--mode parent_doc``: chunk each .md into H2-section parents + paragraph-level children, embed the children, and write parents to ``parents.db`` next to the Chroma directory."
      },
      "answer": {
        "sequence": [
          "load_directory",
          "chunk_markdown",
          "embed",
          "add_chunks"
        ]
      },
      "grading": {
        "mode": "subsequence",
        "pass_threshold": 0.75,
        "aliases": [
          [
            "load_directory",
            "load",
            "読み込み",
            "loader"
          ],
          [
            "chunk_markdown",
            "chunk",
            "チャンク",
            "分割"
          ],
          [
            "embed",
            "埋め込み",
            "embedding",
            "embedder"
          ],
          [
            "add_chunks",
            "upsert",
            "書き込み",
            "保存",
            "chroma"
          ]
        ]
      },
      "pool": [
        "embed",
        "add_chunks",
        "load_directory",
        "chunk_markdown"
      ],
      "explanation": "読み込み→H2親＋子に分割→子だけembed→add_chunksで子をChroma・親をparents.dbへ。"
    },
    {
      "id": "q_vs_004",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/vector_store.py",
      "tags": [
        "parent-doc",
        "scoring"
      ],
      "prompt": "query_with_parents で parent に与えられるスコアは？",
      "citation": {
        "file": "backend/src/vector_store.py",
        "symbol": {
          "kind": "method",
          "name": "VectorStore.query_with_parents"
        },
        "evidence_substring": "best (max) cosine similarity",
        "adr_ref": "ADR-017",
        "snippet": "\"\"\"Retrieve children, dedup by ``parent_id``, return top N parents.\nScore per parent is the best (max) cosine similarity among its matched children — gives the parent its strongest hit's score.\"\"\"",
        "source": "    def query_with_parents(\n        self,\n        embedding: list[float],\n        *,\n        top_k_children: int = 20,\n        top_n_parents: int = 5,\n        where: dict[str, Any] | None = None,\n    ) -> list[tuple[ParentChunk, float]]:\n        \"\"\"Retrieve children, dedup by ``parent_id``, return top N parents.\n\n        Score per parent is the best (max) cosine similarity among its\n        matched children — gives the parent its strongest hit's score.\n        \"\"\"\n        raw = self.query_children(\n            embedding, n_results=top_k_children, where=where\n        )\n        metadatas = raw.get(\"metadatas\", [[]])[0] or []\n        distances = raw.get(\"distances\", [[]])[0] or []\n\n        best: dict[str, float] = {}\n        for md, dist in zip(metadatas, distances, strict=True):\n            pid = (md or {}).get(\"parent_id\")\n            if not pid:\n                continue\n            score = max(0.0, min(1.0, 1.0 - float(dist)))\n            if pid not in best or score > best[pid]:\n                best[pid] = score\n\n        ranked = sorted(best.items(), key=lambda kv: kv[1], reverse=True)\n        top_pids = [pid for pid, _ in ranked]\n        fetched = {p.parent_id: p for p in self._parent_storage.get_many(top_pids)}\n\n        out: list[tuple[ParentChunk, float]] = []\n        for pid, score in ranked:\n            parent = fetched.get(pid)\n            if parent is None:\n                logger.warning(\"parent_id %s referenced by a child has no entry in parents\", pid)\n                continue\n            out.append((parent, score))\n            if len(out) >= top_n_parents:\n                break\n        return out",
        "start_line": 366,
        "end_line": 407
      },
      "answer": {
        "options": [
          "子スコアの平均",
          "ヒットした子のうち最大（best）のコサイン類似度",
          "子の数",
          "親本文の長さで正規化した値"
        ],
        "correct_index": 1
      },
      "explanation": "平均でなく最大＝「一番強くヒットした子」を親の代表スコアにする。局所的に強い一致を尊重。"
    },
    {
      "id": "q_vs_005",
      "difficulty": "L2",
      "type": "mcq",
      "module": "backend/src/vector_store.py",
      "tags": [
        "parent-doc",
        "storage"
      ],
      "prompt": "add_chunks で child を Chroma に入れる際、parent との紐付けはどう保持される？",
      "citation": {
        "file": "backend/src/vector_store.py",
        "symbol": {
          "kind": "method",
          "name": "VectorStore.add_chunks"
        },
        "evidence_substring": "written into each child's",
        "adr_ref": "ADR-023",
        "snippet": "``parent_id`` is written into each child's metadata so search can deduplicate and look up the parent text. Parents are persisted via ``_parent_storage`` (SQLite by default).",
        "source": "    def add_chunks(\n        self,\n        parents: list[ParentChunk],\n        children: list[ChildChunk],\n        child_embeddings: list[list[float]],\n    ) -> None:\n        \"\"\"Persist parents + embed children for parent-document retrieval.\n\n        Children are stored in the same Chroma collection as legacy\n        Documents (different id namespace — child ids carry ``#``), keyed\n        by their ``child_id``. ``parent_id`` is written into each child's\n        metadata so search can deduplicate and look up the parent text.\n\n        Parents are persisted via ``_parent_storage`` (SQLite by default)\n        and also cached in-memory.\n        \"\"\"\n        if len(children) != len(child_embeddings):\n            raise ValueError(\"children and embeddings length mismatch\")\n\n        self._parents = {p.parent_id: p for p in parents}\n        self._parent_storage.upsert_many(parents)\n\n        if not children:\n            return\n\n        ids = [c.child_id for c in children]\n        metadatas: list[dict[str, Any]] = []\n        documents: list[str] = []\n        for c in children:\n            md: dict[str, Any] = {\n                \"parent_id\": c.parent_id,\n                \"doc_id\": c.doc_id,\n                \"kind\": \"child\",\n                \"token_estimate\": c.token_estimate,\n            }\n            md.update(_flatten_axes(c.metadata.get(\"axes\", {}) or {}))\n            normalized_axes = c.metadata.get(\"normalized_axes\") or {}\n            for k, v in normalized_axes.items():\n                md[f\"axis_{k}_norm\"] = v\n            title = c.metadata.get(\"title\")\n            if isinstance(title, str):\n                md[\"title\"] = title\n            metadatas.append(md)\n            documents.append(c.text)\n\n        self._collection.add(\n            ids=ids,\n            embeddings=child_embeddings,\n            documents=documents,\n            metadatas=metadatas,\n        )",
        "start_line": 300,
        "end_line": 350
      },
      "answer": {
        "options": [
          "別テーブルの外部キー",
          "各childのmetadataに parent_id を書き込む（検索時にdedupと親本文の引き当てに使う）",
          "ファイル名の規約のみ",
          "紐付けは保持されない"
        ],
        "correct_index": 1
      },
      "explanation": "childのmetadata内parent_idが唯一のリンク。検索→parent_idでまとめ→parents.dbから親本文を取得。"
    },
    {
      "id": "q_adr22_001",
      "difficulty": "L4",
      "type": "mcq",
      "module": "docs/adr/ADR-022-session-persistence.md",
      "tags": [
        "conversational-rag",
        "design"
      ],
      "prompt": "v0.7 の in-memory 会話ストアで実際に顕在化した問題は？（ADR-022）",
      "citation": {
        "file": "docs/adr/ADR-022-session-persistence.md",
        "symbol": {
          "kind": "section",
          "name": "Context"
        },
        "evidence_substring": "Worker affinity",
        "adr_ref": "ADR-022",
        "snippet": "1. Worker affinity — workers>1 だと同じsession_idが別プロセスに行き履歴が空に見える。2. No durability — 再起動で全会話消失。3. Single-host ceiling — ノードをまたいでスケールできない。"
      },
      "answer": {
        "options": [
          "メモリ使用量が多すぎた",
          "worker間でセッションが共有されない／再起動で消える／単一ホスト限界、の3点",
          "暗号化されていなかった",
          "履歴が長すぎてLLMに入らなかった"
        ],
        "correct_index": 1
      },
      "explanation": "「一番安いもので出す→限界が見えたら永続化」の段階設計。sqlite(WAL)既定への移行理由。"
    },
    {
      "id": "q_adr22_002",
      "difficulty": "L2",
      "type": "mcq",
      "module": "docs/adr/ADR-022-session-persistence.md",
      "tags": [
        "conversational-rag"
      ],
      "prompt": "uvicorn --workers を2以上にした時、in-memory ストアで何が起きていた？",
      "citation": {
        "file": "docs/adr/ADR-022-session-persistence.md",
        "symbol": {
          "kind": "section",
          "name": "Context"
        },
        "evidence_substring": "about 50 % of the time",
        "adr_ref": "ADR-022",
        "snippet": "Subsequent requests for the same session_id land on a different worker about 50 % of the time and see an empty history."
      },
      "answer": {
        "options": [
          "毎回エラーになる",
          "同じsession_idのリクエストが約50%の確率で別workerに当たり、履歴が空に見える",
          "履歴が2倍になる",
          "何も起きない"
        ],
        "correct_index": 1
      },
      "explanation": "プロセスごとに別のdictを持つため。sqlite共有でworker横断の一貫性を回復。"
    },
    {
      "id": "q_adr25_001",
      "difficulty": "L4",
      "type": "mcq",
      "module": "docs/adr/ADR-025-3d-graph-visualization.md",
      "tags": [
        "3d",
        "design"
      ],
      "prompt": "リスト表示が揃っているのに、わざわざ /graph（3D可視化）を追加した理由は？（ADR-025）",
      "citation": {
        "file": "docs/adr/ADR-025-3d-graph-visualization.md",
        "symbol": {
          "kind": "section",
          "name": "Context"
        },
        "evidence_substring": "Lists hide topological",
        "adr_ref": "ADR-025",
        "snippet": "The existing UI surfaces all present knowledge as lists. Lists hide topological features (clusters, hubs, isolated docs) that frontmatter authors already encoded through their refs: choices."
      },
      "answer": {
        "options": [
          "3Dの方が処理が速いから",
          "リストでは見えない位相的特徴（クラスタ・ハブ・孤立doc）を、refsが既に持つ構造として見せるため",
          "モバイル対応のため",
          "リストはメモリを食うから"
        ],
        "correct_index": 1
      },
      "explanation": "refs: に既に埋まっている「構造」を人間に返す。クラスタ＝テーマ群、ハブ＝中心文書、孤立＝放置doc が一目で分かる。"
    },
    {
      "id": "q_rev_007",
      "difficulty": "L3",
      "type": "freetext",
      "module": "backend/src/vector_store.py",
      "tags": [
        "reverse",
        "parent-doc"
      ],
      "prompt": "次のロジックが何をしているか説明せよ:\n\n    best = {}\n    for md, dist in zip(metadatas, distances):\n        pid = md.get(\"parent_id\")\n        score = max(0.0, min(1.0, 1.0 - dist))\n        if pid and score > best.get(pid, -1):\n            best[pid] = score",
      "citation": {
        "file": "backend/src/vector_store.py",
        "symbol": {
          "kind": "method",
          "name": "VectorStore.query_with_parents"
        },
        "evidence_substring": "best: dict[str, float] = {}",
        "adr_ref": "ADR-017",
        "snippet": "best: dict[str, float] = {}\nfor md, dist in zip(metadatas, distances, strict=True):\n    pid = (md or {}).get(\"parent_id\")\n    score = max(0.0, min(1.0, 1.0 - float(dist)))  # keep the max child score per parent_id",
        "source": "    def query_with_parents(\n        self,\n        embedding: list[float],\n        *,\n        top_k_children: int = 20,\n        top_n_parents: int = 5,\n        where: dict[str, Any] | None = None,\n    ) -> list[tuple[ParentChunk, float]]:\n        \"\"\"Retrieve children, dedup by ``parent_id``, return top N parents.\n\n        Score per parent is the best (max) cosine similarity among its\n        matched children — gives the parent its strongest hit's score.\n        \"\"\"\n        raw = self.query_children(\n            embedding, n_results=top_k_children, where=where\n        )\n        metadatas = raw.get(\"metadatas\", [[]])[0] or []\n        distances = raw.get(\"distances\", [[]])[0] or []\n\n        best: dict[str, float] = {}\n        for md, dist in zip(metadatas, distances, strict=True):\n            pid = (md or {}).get(\"parent_id\")\n            if not pid:\n                continue\n            score = max(0.0, min(1.0, 1.0 - float(dist)))\n            if pid not in best or score > best[pid]:\n                best[pid] = score\n\n        ranked = sorted(best.items(), key=lambda kv: kv[1], reverse=True)\n        top_pids = [pid for pid, _ in ranked]\n        fetched = {p.parent_id: p for p in self._parent_storage.get_many(top_pids)}\n\n        out: list[tuple[ParentChunk, float]] = []\n        for pid, score in ranked:\n            parent = fetched.get(pid)\n            if parent is None:\n                logger.warning(\"parent_id %s referenced by a child has no entry in parents\", pid)\n                continue\n            out.append((parent, score))\n            if len(out) >= top_n_parents:\n                break\n        return out",
        "start_line": 366,
        "end_line": 407
      },
      "answer": {
        "model_answer": "ヒットした子チャンクをparent_idごとにまとめ、各親に「最も良い子のスコア（距離を1-distで類似度化した最大値）」を代表スコアとして残す集約処理。",
        "required_keywords": [
          {
            "any_of": [
              "parent",
              "親",
              "parent_id"
            ]
          },
          {
            "any_of": [
              "子",
              "child",
              "チャンク"
            ]
          },
          {
            "any_of": [
              "最大",
              "max",
              "最良",
              "ベスト",
              "一番"
            ]
          }
        ],
        "forbidden_keywords": []
      },
      "grading": {
        "min_keyword_coverage": 0.66
      },
      "explanation": "逆向き×集約ロジック読解。子→親のdedupと max スコア代表化が Parent Document 検索の心臓部。"
    },
    {
      "id": "q_li_004",
      "difficulty": "L1",
      "type": "fill_blank",
      "module": "backend/src/live_ingest.py",
      "tags": [
        "live-ingest"
      ],
      "prompt": "LiveIngestResult のフィールドの空欄を埋めよ:\n    doc_id / parents / children / ____（事前削除した既存チャンク数）",
      "citation": {
        "file": "backend/src/live_ingest.py",
        "symbol": {
          "kind": "class",
          "name": "LiveIngestResult"
        },
        "evidence_substring": "deleted_existing",
        "adr_ref": null,
        "snippet": "@dataclass(frozen=True)\nclass LiveIngestResult:\n    doc_id: str\n    parents: int\n    children: int\n    deleted_existing: int",
        "source": "class LiveIngestResult:\n    doc_id: str\n    parents: int\n    children: int\n    deleted_existing: int",
        "start_line": 27,
        "end_line": 31
      },
      "answer": {
        "accepted": [
          "deleted_existing"
        ]
      },
      "grading": {
        "casefold": true
      },
      "explanation": "冪等性の証跡。再ingest時はここに削除された旧チャンク数が入る。"
    }
  ]
}